# 数字相加计算器

## 项目简介
这是一个简单的Python项目，实现了一个核心函数用于计算两个数的和。项目包含主逻辑文件`add.py`和单元测试文件`test_add.py`，可直接运行验证功能正确性。

## 功能描述
### 核心功能
实现`add`函数，支持以下特性：
- 接受两个数字参数（整数或浮点数）
- 返回它们的数学和
- 包含类型注解确保参数合法性
- 添加异常处理防止非法输入

### 技术实现
采用纯Python实现，无外部依赖。通过类型注解进行参数校验，使用try-except块处理潜在错误。

## 使用示例
### 1. 命令行使用