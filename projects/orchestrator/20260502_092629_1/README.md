# Add Two Numbers Calculator

一个简单的 Python 项目，实现两个数的加法运算功能。该项目包含核心计算逻辑和单元测试，适用于基础数学运算场景。

## 项目功能
提供 `add` 函数用于计算两个数的和，支持整数和浮点数输入。函数会自动处理以下情况：
- 输入参数类型检查（仅接受数字类型）
- 非数字输入时返回错误提示
- 支持正数、负数和零的运算

## 文件结构