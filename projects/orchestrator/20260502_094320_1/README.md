# 两个数之和计算器

## 项目简介
本项目实现了一个简单的核心功能：计算两个数的和。包含主逻辑文件 `add.py`、单元测试文件 `test_add.py` 和项目说明文档 `README.md`，遵循 Python 简洁高效的设计理念。

## 核心功能
### `add.py` 中的 `add_numbers` 函数
- 功能：接收两个数字参数（支持整数、浮点数），返回它们的和
- 特性：
  - 类型注解支持
  - 基本错误处理（非数字输入）
  - 可处理正负数、整数和浮点数的混合计算