# requirements.txt for the Blog CMS Backend (FastAPI)

# Core dependencies
fastapi==0.110.0
uvicorn>=0.15.0
python-multipart>=0.0.11
sqlalchemy>=1.4.30
pydantic>=1.10.10
passlib>=1.7.2
python-jwt>=2.3.0
python-dotenv>=0.20.0
sqlalchemy-utils>=0.38.4
# Optional dependencies for development
pytest>=7.0.0
httpx>=0.23.0
# Markdown support
python-markdown>=3.3.0
# Async support
anyio>=3.6.0
# Security
cryptography>=40.0.2
# WebSockets for real-time features (if needed)
websockets>=10.4
# For serving static files
starlette>=0.13.8
# For rate limiting
fastapi-ratelimiter>=0.5.0
# For CORS
fastapi.middleware.cors>=0.5.0
# For Swagger UI customization
# (included in FastAPI by default)