# VS Code E2E Fixture

This workspace is opened by the Extension Development Host test runner.

The runner uses an isolated temporary VS Code user-data directory and disables
GPU/shared-memory acceleration to keep the test environment resource-safe.
