# CodingMatrix 全项目演化路径 · 总索引

> 版本：v3.0 | 日期：2026-08-02 | 性质：**演化路径总索引**（非执行任务）
>
> 索引结构：**总索引（本文件）→ 大系统演化文件 → 子系统演化文件 + 具体演化路径**。
> 每个大系统一份演化文件，大系统文件内列出其子系统演化文件与演化路径。详见「大系统索引」。

## 大系统索引

| 大系统 | 演化文件 | 子系统 | 推演状态 |
|--------|---------|--------|---------|
| **A. Agent 引擎** | [AGENT-EVOLUTION.md](AGENT-EVOLUTION.md) | 15（A1-A15） | ✅ 已推演（3 批） |
| **B. 前端 Agent 界面** | [AGENT-FRONTEND.md](AGENT-FRONTEND.md) | 3（B1-B3） | ✅ 已有详细文档 |
| **C. 对话接口（Chat）** | [CHAT.md](CHAT.md) | 2（C1-C2） | ✅ 已有详细文档 |
| **D. PPT 生成** | [PPT-FEATURE.md](PPT-FEATURE.md) | 3（D1-D3） | ✅ 已有详细文档 |
| **E. 图片生成** | [IMAGE-GENERATION.md](IMAGE-GENERATION.md) | 2（E1-E2） | ✅ 已有详细文档 |
| **F. 注释与文档一致性** | [COMMENT-NORMALIZATION.md](COMMENT-NORMALIZATION.md) | 2（F1-F2） | ✅ 已有详细文档 |
| **G. 上下文压缩** | [AGENT-CONTEXT-COMPRESSION.md](AGENT-CONTEXT-COMPRESSION.md) | 1（作为 Agent A4 专项） | ✅ 已有详细文档 |
| **H. 服务与工具层（G1-G14）** | [SERVICES-EVOLUTION.md](SERVICES-EVOLUTION.md) | 14（H1-H14） | ✅ 已推演 |

> 演化四阶段：**拆分解耦 → 统一收敛 → 智能增强 → 平台化**，每阶段向后兼容。

---

## I. 模块深扫索引（每模块一份详细演化文档）

> **模式**：一次只深扫一个小模块，质量优先。每个模块的详细演化文档（作用/功能 + bug 代码 + 修改建议→目的）存放于 `modules/` 目录，本表为索引链接。
> **顺序**：从上而下（依赖底层 → 上层；P0 优先）。扫描时依据具体事实修改对应文档。
> **状态**：`✅ 已完成` / `🔬 扫描中` / `⬜ 待扫描` / `⬜ 待建`
>
> 模板：[modules/_TEMPLATE.md](modules/_TEMPLATE.md)
> **13/13 扫描完成后的演化汇总**：[EVOLUTION.md](EVOLUTION.md)（四层演化蓝图 + 关键问题确定性分级：已实测 / 实码可证 / 待实测）

| 顺序 | 模块 | 路径 | 行数 | 关键关注（已知候选） | 状态 | 详细文档 |
|------|------|------|------|---------------------|------|---------|
| 1 | executor.py | app/agent/executor.py | 451 | P0 §9.1 单例闭包；B2 success 判定；B3 search_files 漏注册 | ✅ 已完成 | [modules/executor.md](modules/executor.md) |
| 2 | tools.py | app/agent/tools.py | 1292 | **P0 T1 python 沙箱 os NameError**；T2 cwd 前缀绕过；T3 OOM 防护失效；8 消费方/无 DB 依赖 | ✅ 已完成 | [modules/tools.md](modules/tools.md) |
| 3 | react_agent.py | app/agent/react_agent.py | 200 | **P0 RA1 工具调用契约断裂**（wrapper 不兼容 project_path 注入）；RA2 context 丢弃；RA3 无 action 恒失败 | ✅ 已完成 | [modules/react_agent.md](modules/react_agent.md) |
| 4 | react_engine.py | app/agent/react_engine.py | 770 | **P0 RE1 空串短路**（project_path="" → 循环不执行）；RE2 工具签名契约；RE3 同步无超时；RE4 result_count 恒 0 | ✅ 已完成 | [modules/react_engine.md](modules/react_engine.md) |
| 5 | llm_client.py | app/agent/llm_client.py | 397 | **P1 LC1 成本恒记 0**（config 无 cost 字段）；LC2 信号量顺序饿死；LC3 流式无超时 | ✅ 已完成 | [modules/llm_client.md](modules/llm_client.md) |
| 6 | llm_caller.py | app/utils/aicloud/llm_caller.py | 438 | **P0 LCL1 直连信号量顺序+泄漏**；LCL2 流式 fallback 用错 Key；LCL3 fallback 语义不一致 | ✅ 已完成 | [modules/llm_caller.md](modules/llm_caller.md) |
| 7 | specialist_base.py | app/agent/specialist_base.py | 294 | **P1 SB1 full 模式+无 memory**（每轮 4 次 LLM）；**SB2 测试收集失败**（残留旧常量）；SB3 下标脆弱 | ✅ 已完成 | [modules/specialist_base.md](modules/specialist_base.md) |
| 8 | spec_first_generator.py | app/agent/spec_first_generator.py | 536 | **P1 SFG1 返回值语义不符**（只查 OpenAPI）；SFG2 链断裂；SFG3 OpenAPI 不走 model_config | ✅ 已完成 | [modules/spec_first_generator.md](modules/spec_first_generator.md) |
| 9 | spec_first_generate.py | app/agent/orchestrator_generation/spec_first_generate.py | 2383 | **P1 SPFG1 断点续传 10 字节跳过未验证**；SPFG2 JS 误判；SPFG3 沙箱只修 .py；SPFG5 重构硬编码 python；**v0.2 重扫新增（第一百零七轮）：SPFG11 [P2] 动态拓扑分支「清理不符合项目语言的文件」静默物理 unlink 生成产物（file_plan 未规划扩展名即删，普通分支无此清理，use_dynamic_topology 默认 True 即默认触发）**；**SPFG12 [P2] 启发式同名删除不看内容（根目录 vs src/ 同名、功能同名跨目录优先 src/app 保留删其余，Django manage.py 误删，GO2 家族）**；**SPFG13 [P2] _validate_project_completeness is_complete 不含 empty_files（TG4 同族 spec-first 链复现）**；**SPFG14 [P2] 动态拓扑断点续传跳过文件直接 update_file_validation(True,[]) 标记验证通过**；SPFG15 [P3] 文件名修复 rename 不更新 dep_graph；SPFG16 [P3] 贪婪跨块；SPFG17 [P3] 四处直连 call_llm（LCL1 家族）；SPFG18 [P3] 硬编码 Qwen/Qwen3-8B + glm-z1-9b | ✅ 已完成（v0.2 重扫） | [modules/spec_first_generate.md](modules/spec_first_generate.md) |
| DockerRunner | app/utils/docker_runner.py | 802 | **DR1 [P2] read_only=True 与 pip install 冲突（静态可证）：根 FS 只读 + tmpfs 仅 /tmp 与 /app → pip 写 /usr/local 被拒 → docker 容器依赖安装路径恒失败（python:3.11-slim 未预装 pytest）**；**DR2 [P2] 安全扫描只告警不阻断：security_warnings 仅 extend logs（:477-478）从不检查非空即终止——os.system/subprocess/eval 命中后仍继续运行测试（DGV1 放行家族）**；**DR3 [P2] run_validation 无实际超时机制：self.timeout 仅被不可达 except asyncio.TimeoutError（:563）引用，全程无 wait_for 包裹 start/exec_run——死循环测试永挂**；**DR4 [P2] 依赖安装只认 requirements.txt（TFC1 docker 侧落点：:531 只 pip install，npm/go/maven 项目无 requirements.txt 跳过安装 → 容器内 npm test 必失败）**；**DR5 [P2] __init__ 同步阻塞 + 资源配置异步竞态（_init_docker_client ping/_pull_image 同步网络 I/O 阻塞事件循环；_load_resource_config create_task 后 run_validation 立即读 config——DB 配置与容器创建竞态，纯同步上下文 get_running_loop 抛错被吞配置全跳过）**；**DR6 [P2] execute_code 任务接线即崩（跨模块）：code_tasks.py:112-114 传 code=/language= 而 run_validation 签名是 project_path/...→ TypeError（DG1/SCT1 接线即崩家族）**；**DR7 [P2] validate_project 任务谎报成功（跨模块）：project_tasks.py:88-95 构造 runner 从不使用，无任何验证执行，固定返回 {"status":"success"}（TR1/MAR8 成功态谎报家族）**；DR8 [P3] 容器内 /app 是 bind mount 到宿主 project_path（rw）——测试写文件回写宿主（TR2 镜像）；DR9 [P3] ALLOWED_PACKAGES ~200 项本模块零消费死数据 + 标准库列进 pip 名单（shutil/subprocess）+ 三份独立副本（AiProjectCode:235/helpers:24，SCT6 家族）；DR10 [P3] FORBIDDEN_PATTERNS 三份独立副本（test_runner:34/guardrails:187）；DR11 [P3] orchestrator_testing:254 调 docker_runner.cleanup() 方法不存在 → 恒 AttributeError 被 :253-256 except 静默吞（死调用掩盖契约缺失）；DR12 [P3] 安全扫描只 skip 整行 # 行内尾注释/字符串内 eval( 误报（TR5 非 AST 家族）；DR13 [P3] can_run_container 无锁计数竞态 + _exec_command 输出无界收集 + mem_reservation dataclass 字段漂移 | ✅ 已完成 | [modules/docker_runner.md](modules/docker_runner.md) |
| ServiceContainerManager | app/utils/service_container_manager.py | 524 | **SCM1 [P2] cleanup_containers 缓存判断写反——服务容器 TTL 内永不停止（全库确认）：_start_and_register 每次启动都写健康缓存（:198-202 container_id=本次 id）→ :414 cached.container_id==container_id 恒 True → continue 跳过停止，缓存复用的容器同样跳过——docker 测试每次结束调 cleanup（docker_runner finally :585）但测试时长<300s TTL → 服务容器全部泄漏资源累积**；**SCM2 [P2] 健康命令失败当健康通过（DGV1 放行家族）：:312-313 exec 健康命令 10s 持续失败仅 warning 后 return True（「TCP 已通」覆盖「应用层未就绪」）——ES 启动中/DB 未接受连接仍判健康 → 测试在依赖未就绪时运行**；**SCM3 [P2] _start_container 健康检查失败仍返回 container_id（全库确认）：:252-257 health 失败仅 warning 仍 return container_id → 调用方以为服务就绪（与 SCM2 叠加健康检查整体空转）**；SCM4 [P3] detect_project_services 子串假阳性（:517 image_key in content.lower()——注释/服务名/镜像名 redis/mongo/mysql 全命中，PP8/BE1 家族）+ pyproject.toml 项目漏检测；SCM5 [P3] _port_is_open_async 同步 socket connect_ex 阻塞事件循环（最多 15s/服务，TR5 家族）；SCM6 [P3] _find_available_port 并发竞态 + bind 失败仅 port+1 不验证（MCP1 家族）；SCM7 [P3] startup_timeout 被 min(…,15) 硬编码截断——ES 配置 45s 被截到 15s（TFC4 家族）；SCM8 [P3] wait_for_health 与 _start_container 双健康检查（缓存使 docker_runner 侧第二次检查基本空转）；SCM9 [P3] _generate_test_env_vars 异常静默返回 {}（EC3 家族）——服务连接变量缺失无告警 | ✅ 已完成 | [modules/service_container_manager.md](modules/service_container_manager.md) |
| Guardrails | app/utils/guardrails.py | 452 | **GRD1 [P2] Prompt 注入检测全库零消费——防护承诺从未兑现（全库确认）：PromptInjectionDetector/check_prompt_safety（10 条注入模式正则+敏感词+结构异常+评分分级完整实现）全 app/ 零引用——六项声称防护能力仅 2 项接线（速率+磁盘），注入检测/路径安全/会话 ID 验证三套全为死能力（SCT5/UPL1 能力未接线家族）**；**GRD2 [P2] 内存级限流跨进程失效 + 全局单例无界增长（MCP1 家族）：check_rate_limit 活跃于 modify/stream 端点（:254/:519 429 阻断）但 InMemoryRateLimiter 是进程内 dict + threading.Lock——多 worker 每进程独立计数限流可分摊绕过；_entries 按 user_id 无界增长（攻击者随机 key 累积，cleanup 依赖 check 调用频率）；get_guardrail_context 单例无锁**；GRD3 [P3] check_disk_space("./projects") 相对路径 CWD 漂移 + 检查失败静默放行（:282-283 异常 is_low_space=False 不阻止，DGV1 放行家族——「磁盘充足」与「检查失败」两态消费方不可区分）；GRD4 [P3] 限流默认 max_requests=10/60s 过严——stream 端点每用户每分钟 10 次流式，连续对话误伤 429；GRD5 [P3] 注入检测正则误报面大（一旦接线即误伤）：`(execute|run|eval)\s*(code|command|...)` 命中「run python script」、中文「告诉系统」命中、奇数 ```/markdown 表格判结构异常——接线前需重设计（FCT3/PP8 家族）；GRD6 [P3] validate_session_id 同名单函数两处异构（guardrails 版零消费 vs schemas.py:25 独立实现活跃，SCT6 双轨家族）；GRD7 [P3] PathSecurityChecker/SessionIdValidator 零消费 + FORBIDDEN_PATTERNS `^/` 绝对路径全拒/`\.(env|ini|conf|cfg)$` 配置全拒——接线即误伤项目内 .env（FCT3 同源）；GRD8 [P3] 限流/磁盘检查同步调用阻塞 async 端点（shutil.disk_usage 同步 I/O，TR5 家族） | ✅ 已完成 | [modules/guardrails.md](modules/guardrails.md) |
| AgentSkills | app/utils/agent_skills.py | 415 | **ASK1 [P2] 五个认知 Skill 全库零消费——「为 Agent 注入 5 个认知能力」从未接线（全库确认）：get_skills_manager() 唯一调用点 helpers.py:197 位于 get_agent_knowledge_base() 内而后者零调用（code_tasks.py:171 仅 import）；prompt_loader 侧 load_cognitive_skills_prompt 亦零调用——5 个 Skill 类 + 管理器全部方法零引用，三个 YAML（keyword_triggers/anti_patterns/review_checklist）从不加载，能力未接线家族模块级全死（GRD1 类级延伸）**；ASK2 [P3] post_modify_check 假阳性设计——detect_patterns("", code) before 为空 → 全部代码行判「新增行」→ 含 import/def/class/@router/async def 即全 pattern 命中（TR1/MAR8 家族）；ASK3 [P3] KeywordDetectionSkill 无词边界子串触发——keyword.lower() in user_input.lower() +「实现/add/优化」超宽词任意出现即判触发（PP8/FCT3 家族）；ASK4 [P3] AntiPatternSelfCheck 正则直扫整个代码文本不排除注释/字符串（password= 在注释中同样命中）+ YAML pattern 直接当正则 + re.error 静默跳过（DR12 家族）；ASK5 [P3] RiskSelfAssessmentSkill 依赖计数子串匹配错算（target in file_path 短名跨文件命中、多 target 同文件虚高——与 code_tasks.py:267-271 _find_affected_files 同源双副本）；ASK6 [P3] 三个 YAML 相对路径 CWD 漂移 + .exists() False 静默空降级（GRD3/EC3 家族）；ASK7 [P3] MultiAngleReviewSkill.review 占位实现（全 pending_review + 空 notes，实际审查逻辑从未实现）+ get_all_skills_context 仅返回元数据「技能名片」不含规则内容 | ✅ 已完成 | [modules/agent_skills.md](modules/agent_skills.md) |
| GuardContracts | app/utils/guard_contracts.py | 286 | **GC1 [P2] 守护合约 existence 检查无变更前基线——「保护项删除」判定语义失真（全库确认）：:177-198 check_file 只接收变更后 content（code_tasks.py:243 read_text 后传入），:203-212 existence `pattern not in content` 即报「保护项可能已被删除」——无法证明删除，变更后本不含该保护项的文件必然误报（TR1/MAR8 假阳性家族）**；**GC2 [P2] 守护合约违规只记录不阻断——CRITICAL「禁止修改」无强制力（DGV1 放行家族）：code_tasks.py:235-245 违规仅 append 进 guard_violations 结果 dict 返回，无抛异常/中断/确认流程——GC-001 认证核心函数违规修改照样落盘，「需用户确认」承诺未兑现**；GC3 [P3] check_type="signature" 实际不检签名——:216 `(def|class)\s+name\s*[\(:]` 只查函数名存在性，`def foo(a,b)`→`def foo(a)` 仍匹配（签名变更漏报），与 existence 双模式实际等效；GC4 [P3] 便捷函数 check_file_against_contracts/get_applicable_rules 零消费（code_tasks import 但走方法链双路径并存，GRD7/ASK1 能力未接线家族）；GC5 [P3] 10 条规则硬编码无外部化 + allowed_changes 白名单死字段（:25 定义 :202-226 从不引用）+ GC-009/GC-010 NOTICE protected_patterns=[] 空迭代永不触发（空操作）；GC6 [P3] 保护项子串匹配失真——GC-003 保护 "id"（:94）任何代码含 "id" 子串恒存在 → 实际永不触发，字段删除后残留子串同样漏报（FCT3/PP8 家族） | ✅ 已完成 | [modules/guard_contracts.md](modules/guard_contracts.md) |
| RateLimiter | app/utils/rate_limiter.py | 38 | **RL1 [P2] slowapi 限流默认内存存储 + 单进程计数——多 worker 部署失效（GRD2/MCP1 家族）：:14-17 Limiter 未配 storage_uri（slowapi 默认 in-memory）——限流三套全内存级（slowapi + middleware.rate_limiter + guardrails.InMemoryRateLimiter）多进程部署下整套限流体系失效**；**RL2 [P2] key_func=get_remote_address 忽略代理——反向代理后全站共享同一配额（全库确认）：:15 直接用 request.client.host（Nginx 代理后恒为代理内网 IP）→ 全站用户共享 100/minute 全局配额 + apikey/providers 接口级限制按代理 IP 归并——正常并发立即 429 误伤；:27-32 get_client_ip 实现了 X-Forwarded-For 解析却零消费（设计了正确逻辑没用上）**；RL3 [P3] 三层限流并存（slowapi 全局/接口级 + RateLimitMiddleware 多级 tier + guardrails 业务级 10/60s）——同一请求链三套配额叠加误伤、状态互不可见（guardian /admin/rate-limit 只暴露 middleware 层，SCT6 三轨家族）；RL4 [P3] :24 用 slowapi 默认 _rate_limit_exceeded_handler——英文「Rate limit exceeded」文案与项目统一中文错误体系不一致、无 Retry-After；RL5 [P3] get_client_ip 零消费 + X-Forwarded-For 无 trusted-proxy 白名单校验（接线需前置信任设计，DR10/FCT3 家族） | ✅ 已完成 | [modules/rate_limiter.md](modules/rate_limiter.md) |
| AgentCore | app/utils/agent_core.py | 2627 | **AC1 [P2] create_project_file 无路径校验——LLM 可写任意路径（越权写文件）：:1182 aiofiles.open(file_path) 直接用 LLM 工具参数 + :1177 mkdir 任意目录——无 FileOperator._validate_path/无 PathSecurityError——生成主工具可写 /etc/xxx、../outside 等服务进程权限内任意路径；同模块 create_file/edit_file/delete_file（:2551/:2507/:2527）均走 FileOperator 有防护——同一模块两套写入路径主工具无防护**；**AC2 [P2] _parse_tool_calls 尝试4 硬编码 file_path ./projects/user_api/（:2288）无视 output_dir——LLM 直接输出代码块时文件写错位置（GRD3 相对路径漂移家族）**；**AC4 [P2] 内存级会话历史 ConversationHistoryManager（:37-121）重启丢失 + 多 worker 不共享（session 历史 worker A 建 worker B 查不到 → 继续生成失效）+ 清理排序 key 用 messages[-1].timestamp 但消息无 timestamp 字段恒 "" 随机清理（MCP1/GRD2 家族）**；**AC5 [P2] 生成成功判定只看 final 步骤存在（:1994）——验证 runnable False 仍 success（:1953 只发回调不改 success）+ 完成检测为纯文本子串（:1911「完成/success/done」LLM 一句话即结束）——TR1/MAR8 结果谎报家族**；AC3 [P3] _execute_tools :2403 访问不存在的 self.current_output_dir（BaseModel 无此字段）→ AttributeError 被 :2418 except 吞——目录快照逻辑确定性死代码；AC6 [P3] run_full_validation runnable 只计文件验证（:844）——依赖/结构/入口检查结果不计入，无 README/无入口点仍 runnable=True（DGV1 放行）；AC7 [P3] _validate_runtime :569 宿主机 exec 生成代码——无沙箱无资源限制（仅 10s 超时），危险操作直接执行（与 docker_runner 隔离执行矛盾）；AC8 [P3] _check_syntax_warnings :781-785 每个 for 循环无条件报「循环变量可能未使用」——全量误报；AC9 [P3] _validate_security 'open' 列危险函数（:637）+ security 恒 success（:671，:424 只计前三项）——安全检查只告警不阻断（DR2 家族）；AC10 [P3] CodeValidator/ProjectValidator 同名双轨（agent_core 版 vs app/agent/code_validator.py + app/utils/project_validator.py 活跃版，SCT6 家族）+ validate_file 每次重建跑全量 pip freeze（:363/:1243）；AC11 [P3] 完成检测关键词子串误判（:1911「完成」子串中途即结束，PP8 家族）；AC12 [P3] FileModelRouter 关键词子串匹配（:216-225）+ 硬编码 Qwen/DeepSeek 模型名（:164-165，SPFG17 家族） | ✅ 已完成 | [modules/agent_core.md](modules/agent_core.md) |
| FileOperator | app/utils/file_operator.py | 703 | **FO1 [P2] PROTECTED_FILES ".env" 子串匹配误伤——项目内 .env* 文件/目录全拒（FCT3/PP8 家族）：:140-142 `protected_file.lower() in abs_path_str` 子串匹配——路径含 ".env" 即拒（/projects/myapp/.env.example 被拒）——而 SAFE_EXTENSIONS（:59/:71）明确白名单 .env/.env.example——黑白名单矛盾；:147-148 `.env` 扩展名豁免被 :141 前置拦截——豁免死代码**；FO2 [P3] allow_protected_paths=True 完全关闭路径防护（:89/:135-142「危险仅测试用」无强制——生产零调用传 True 潜在误配）；FO3 [P3] 扩展名白名单仅 create 生效（:272）——write/delete/move/copy/list_dir/tree 全 check_extension=False（:234/:296/:323/:349/:383/:586）+ SAFE_EXTENSIONS 超全（.env/.lock/.recipe）——扩展名检查形同虚设；FO4 [P3] FileOperator() 无 base_path 黑名单制范围失控——multi_model_agent:72/file_processing:37 用无边界实例——/home//usr//opt/ 等非系统路径可读写（区别于 AC1 完全无校验）；FO5 [P3] read 全量 readlines 大文件内存 + 无大小上限（:198）；FO6 [P3] _collect_files 点开头全跳（:164）——.github/.env.example 等隐藏内容 search/grep/stats 不可见；FO7 [P3] grep/search errors='ignore' 编码损坏内容静默丢弃（EC3 家族） | ✅ 已完成 | [modules/file_operator.md](modules/file_operator.md) |
| 10 | cross_validator.py | app/agent/cross_validator.py | 1512 | **P2 CV1 is_critical_file 子串假阳性（order_detail/tokenizer/accessibility 误判关键文件→成本×3，实测）**；CV2 模型一致性正则低精度→假 issue 喂 LLM 幻觉修复；CV3 generic import 第三方库集合不全→import_error 假阳性→幻觉生成缺失模块；CV4 三套 API 契约并存 | ✅ 已完成 | [modules/cross_validator.md](modules/cross_validator.md) |
| 11 | dynamic_model_router.py | app/agent/dynamic_model_router.py | 1035 | **P1 DMR1 配置无成本字段（LC1 根因）**；DMR2 熔断阈值 2/3/3 不一致；DEFAULT_FALLBACK_ORDER 重复项 | ✅ 已完成 | [modules/dynamic_model_router.md](modules/dynamic_model_router.md) |
| 12 | mcp_client.py | app/agent/mcp_client.py | 513 | **P1 MCP1 单例竞争**（executor/mixin 双 new 杀连接）；MCP2 闭包依赖全局 _instance；HTTP 无握手 | ✅ 已完成 | [modules/mcp_client.md](modules/mcp_client.md) |
| 13 | error_recovery.py | app/agent/orchestrator_generation/error_recovery.py | 33 | **P1 ERR1+ERR2**：process context 被忽略+success 恒 False → 自动修复完全失效；RA2/RE1 链生产端收尾 | ✅ 已完成 | [modules/error_recovery.md](modules/error_recovery.md) |

> 索引扩展：以上为 Agent 引擎核心 13 模块（按依赖/P0 优先排序）。后续按需扩展至 B-F/H 大系统的模块清单。
> 扫描进度（2026-08-05）：**13/13 全部完成**（executor / tools / react_agent / react_engine / llm_client / llm_caller / specialist_base / spec_first_generator / spec_first_generate / cross_validator / dynamic_model_router / mcp_client / error_recovery）。

## I.2 编排层补扫（2026-08-05，13 模块之外的 orchestrator_generation 与顶层错误恢复）

> 13 模块索引之外、传统生成链路相关文件的补充深扫。涵盖两套增量实现、顶层 ErrorRecoveryLoop、评价模式与组装层。

| 模块 | 路径 | 行数 | 关键发现 | 状态 | 详细文档 |
|------|------|------|---------|------|---------|
| 顶层 ErrorRecoveryLoop | app/agent/error_recovery.py | 797 | ERL1 死代码（_build_error_context 未用）；ERL2 默认模板占位符 {content} 未替换；ERL4 修复 LLM 成本不计入；ERL5 error_classifier/strategy_evaluator 全局单例污染；ERL3 临时文件泄漏；ERL6 修复直写非原子 | ✅ 已完成 | [modules/error_recovery_loop.md](modules/error_recovery_loop.md) |
| IncrementalModifyMixin | app/agent/orchestrator_generation/incremental_modify.py | 1028 | **IM1 [P1] 简单变更/降级重试硬编码 SiliconFlow 模型名绕开 DMR**；IM2 内容启发式误跳过；IM3 增量无语法校验；IM4 依赖图只解析 Python import；IM7 闭包死代码；IM9 依赖图缺失静默回退全量 | ✅ 已完成 | [modules/incremental_modify.md](modules/incremental_modify.md) |
| 传统生成链路 | orchestrator_generation/（traditional_generate 427 + incremental_generate 85 + evaluate_mixin 351 + mixin 146 + coverage_checker 61 + feature_extractor 37） | 1107 | TG1 补文件直连 LLM 非原子；TG2 success 与 test_results 耦合；EV1/EV3 评价模式绕开 model_config/信号量/成本；EV2 评价 JSON schema 混用评分失真；CC1 覆盖率 30% 命中虚高；IG1 增量并发依赖 LLMClient 内部信号量 | ✅ 已完成 | [modules/traditional_generate.md](modules/traditional_generate.md) |
| 评价/组装/会话增量 | evaluate_mixin（351）+ mixin（146）+ incremental_generate（85） | 582 | **EV1 [P2] _evaluate_risks 用 architecture.get("has_backend"/"has_database")——架构返回键集合无此二键（architect.py:537-550）→ 恒 None → missing_api/missing_db_schema 两个 high 风险恒死（实测 fullstack+无 api_spec 风险列表空）**；**EV2 [P2] 架构评价解析恒降级（实测）：_parse_evaluation_json 校验 score/completeness vs 架构 prompt 返回 architecture_quality 等六键不匹配 → 架构评分恒 0，overall 只反映需求评价**；**EV3 [P2] evaluate models_used 无 None 防护（:109-110 vs :43 有防护对比）→ use_dynamic_topology=False 时 AttributeError**；EV4 [P3] 贪婪跨块（MAR5 家族）；EV5 [P3] 风险硬编码阈值（>50 文件 high/>6 技术栈 medium）；EV6 [P3] 评价模式最多 6 次 LLM 调用无整体超时（联想有 TIME_BUDGET 两评价无）；EV7 [P3] fallback score:0 静默低分无降级标记（成功态谎报家族）；IG1 [P3] 增量失败 stash_pop 回滚后 generated_files 成功项仍报告（报告与磁盘不一致）；GM1 [P3] _initialize_components 每次生成全量重建组件+MCP 重连 | ✅ 已完成 | [modules/evaluation_mixin.md](modules/evaluation_mixin.md) |
| LanguageAdapter 体系 | app/agent/adapters/（language_adapter 281 + python 400 + javascript 486 + generic 416 + __init__ 30） | 1613 | **AD1 [P2] Python is_project_module 前缀白名单漏检（实测）：project_prefixes=['app','src','lib','pkg','internal','core'] 白名单外根包（banking_system/myapp）恒判外部 → 项目内模块被当外部依赖，依赖边丢失（IV5 实现侧根因）**；**AD2 [P2] JS is_project_module 无项目白名单（实测）：models/user 恒 False 而 ./models/user True——项目内绝对路径导入恒判外部，与 Python 不对称**；**AD9 [P2] JS resolve_import_to_file 不解析项目内绝对路径（实测）：models/user → [] 依赖边丢失**；**AD11 [P2] Python 相对导入层级丢失（实测）：from ..models 在 app/api/users.py 解析到 app/api/models.py 而非回溯上层——依赖边错配**；**AD12 [P2] Generic _file_plan_data 类级共享可变状态（全库确认）：单例注册 + dependency_graph:181 每次 build 覆写类属性 → 多项目互相污染（SM1/MCP1 家族）**；AD3 [P3] parse_imports 逐行正则多行 import 全漏 + from . import 实测 module=''；AD5 [P3] _guess_imports Go stdlib 未过滤；AD6 [P3] Python extract_definitions 类内方法提为顶层符号；AD8 [P3] detect_language >50% 阈值混合项目回 generic（全栈导入解析退化）；AD10 [P3] JS export default function 漏检 + TS 泛型箭头函数；AD13 [P3] integrity_validator get_adapter('go'/'java') 静默落 generic 无感知 | ✅ 已完成 | [modules/language_adapters.md](modules/language_adapters.md) |
| 错误分析决策链 | app/agent/complexity.py（172）+ error_classifier.py（196）+ strategy_evaluator.py（329） | 697 | **CEC1 strategy_evaluator A/B 框架空转（create_or_update_strategy 零调用，评估链全死路径）**；CEC2 默认模板占位符 {content} 注入缺失（ERL2 细化）；CEC3 错误分类直连 call_llm 无 token/信号量/成本；CEC4 硬编码模型名扩散（deepseek-r1 + 映射兜底）；CEC5 promotion 配对语义不成立；CEC6 策略文件非原子写+全局单例；CEC7 成本估算体系分裂 | ✅ 已完成 | [modules/error_analysis_chain.md](modules/error_analysis_chain.md) |
| DependencyGraph | app/agent/dependency_graph.py | 1340 | **DG1 项目扫描缺参 TypeError（build_from_existing_project 零调用死路径，接线即崩）**；DG2 泛化反推返回 stem 非完整路径边永失；DG3 缺失依赖静默丢弃致 get_missing_files/validate_completeness/add_missing_files 全死逻辑（IM9 根因）；DG4 _break_cycles 递归 >1000 层 RecursionError（实测）；DG5 Kahn 反复 sort O(V²logV)；DG6 deduplicate 启发式误删风险；DG7 上下文预算恒 32768 兜底与模型窗口脱节；DG8 LLM dependencies 用文件名时被 DG3 丢弃；DG9 JS 别名 import 不解析；DG10 API 多段路径合并单文件 | ✅ 已完成 | [modules/dependency_graph.md](modules/dependency_graph.md) |
| CodeValidator | app/agent/code_validator.py | 767 | **CV1 run_full_validation 缓存读写恒失败（合成 key 被当文件路径 open，实测 cache 恒 miss/entries=0）**；CV2 validate_runtime_imports 执行任意模块级代码（副作用实证 print 执行；wait_for 无法打断同步 exec 阻塞事件循环）；CV3 相对导入误报（project_root 只认 src/tests，app/ 主包项目误报缺少依赖，实测）；CV4 前端 API 一致性检查空操作；CV5 OAuth2PasswordBearer 规则过时方向反；CV6 Pipfile 依赖未声明 toml 包；CV7 类级缓存全局共享 + MAX_CACHE_SIZE 未用；CV8 四套验证器并存（CodeValidator/is_valid_code_content/SandboxValidator/agent_core） | ✅ 已完成 | [modules/code_validator.md](modules/code_validator.md) |
| TestRunner | app/agent/test_runner.py | 811 | **TR1「未找到测试文件」直接判定测试通过（无测试文件=success，掩盖生成器没写测试）**；TR2 非 Python 项目在原目录执行测试无任何隔离（JS/Go/Java/Rust 污染原目录，测试固化为预期）；TR3 success 只看 returncode 与解析失败数解耦（部分解析丢失）；TR4 全局信号量 _SemaphoreHolder 跨事件循环共享；TR5 安全扫描仅前 100 py 文件 + 字符串正则（AST 不解析）；TR6 白名单过滤静默丢弃不在名单依赖；TR7 恒注入 sqlite DATABASE_URL 覆盖项目真实配置；TR8 _cleanup 越界删用户项目 __pycache__ | ✅ 已完成 | [modules/test_runner.md](modules/test_runner.md) |
| IntegrityValidator | app/agent/integrity_validator.py | 508 | **IV1 missing_module 不进 missing_files → generate_fixes 不修缺失模块（补缺闭环断，与 DG3 双断，实测）**；**IV2 _check_package_init 中途切 self.language_adapter（有 .js 即整体切 JS 无 python 分支）→ 混合项目后端导入校验+API 提取全失效（实测）**；**IV3 _api_endpoint_exists 前缀误判漏报（实测 /api/users 判 /api/user 存在）**；**IV4 API 契约完全忽略 HTTP method（实测 GET 调 POST 端点零 warning）**；IV5 is_project_module 前缀白名单漏检非 app/src 根包（CV3 同类）；IV6 fallback 硬编码 index.js 入口（实测 python 误报 app/index.js）；IV7 后端 API 正则仅 @app./@router. 不转义；IV8 符号提取正则非 AST；IV9 generate_fixes 父目录跳过死逻辑；IV10 零测试覆盖 | ✅ 已完成 | [modules/integrity_validator.md](modules/integrity_validator.md) |
| APIContractChecker | app/agent/api_contract_checker.py | 501 | **AC1 前端模板字符串 `${id}` 归一化为 `$:id` 与后端 `:id` 不匹配 → missing_backend 误报（实测）**；**AC2 FastAPI APIRouter(prefix=...) 不解析、API_PREFIXES 死常量未接线 → 标准 prefix 写法全盘误判（实测）**；**AC3 前端拼接/具体参数值不归一化为 :param，详情类请求漏/误匹配（实测）**；**AC4 逐行扫描 method 推断只认同行：跨行 fetch 的 POST 误判为 GET（实测）**；AC5 AXIOS 第三模式+分派分支死代码，非 axios 的 .get/.post 全漏检；AC6 消费方每次写文件全量 rglob 扫描 .py；AC7 path_mismatch/param_mismatch 声明不产出 + method_mismatch 单向；AC8 Django path() 假阳性 + include 子路由不拼接；AC9 测试仅 1 个 | ✅ 已完成 | [modules/api_contract_checker.md](modules/api_contract_checker.md) |
| RefinementLoop | app/agent/refinement_loop.py | 584 | **RL1 openapi 路径一致性检查空操作（循环只 pass 从不报 issue，实测 openapi 路径全不存在仍零 issue）**；**RL2 _validate_python_imports 用 importlib.import_module 在 agent 环境导入（环境错位 + 执行任意模块副作用，与 CV2 同源，实测 requests 已装不报）**；**RL3 修复循环 success 只依赖轻量验证（语法/括号/import 存在），spec 符合性与功能正确性零覆盖（「存在≠正确」主线）**；RL4 LLM 空返回/未改变/异常 break 后兜底返回 remaining_issues=[] 语义失真；RL5 node -c 临时文件泄漏 + 每轮 5s 阻塞；RL6 每次修复实例化 SpecFirstGenerator + 异常静默吞 spec context；RL7 model 检查字符串包含非 AST + 温度硬编码；RL8 测试仅 1 个 | ✅ 已完成 | [modules/refinement_loop.md](modules/refinement_loop.md) |
| OrchestratorFiles | app/agent/orchestrator_files.py | 888 | **OF1 _validate_and_review_file 直接读写 validator 私有 _validation_cache + 只 ast.parse（CodeValidator 完整验证被绕开，第 5 处验证器并存）**；**OF2 git stash 回滚全局栈语义（非 git 项目回滚不完整 + 多层 stash pop/drop 顺序错乱风险）**；OF3 无效内容恢复路径 extract_engineer_content 缺 expected_language/llm_caller；OF4 _direct_llm_generate_file 硬编码 DEFAULT_CODE_MODEL 不走 DMR/成本；OF5 _select_alternative_model 硬编码 Qwen 模型名（spec_first 3 处活跃）；OF6 小项目/分层两生成方法 ~60 行重复；OF7 路径规范化只修一种格式 \ 不处理；OF8 review high 后内容已采用但 success=False 语义矛盾；OF9 llm_caller 语言检测每文件定义 + 成本不计；OF10 _apply_patches_incremental 全库无调用方死代码 | ✅ 已完成 | [modules/orchestrator_files.md](modules/orchestrator_files.md) |
| OrchestratorProgress | app/agent/orchestrator_progress.py | 568 | **OP1 成本追踪链路恒零（llm_client _record_usage 读 get_model_config 返回 dict 中不存在的 cost_per_1m_input/output 键 → cost_usd 恒 0，实测 1000+2000 token 成本 0.0；model_registry 的 ln 字段从未接入）**；**OP2 _report_file_event 推全量 content 不截断（本模块定义 MAX_CONTENT_FOR_CONTEXT 3000 却只在 spec_first 验证通道用，进度通道大文件事件可达数 MB）**；OP3 _pending_tasks 类属性跨实例共享（实测 a1 is a2）+ task 异常无人 retrieve 泄漏 warning；OP4 build_progress_event/GenerationProgress 死代码（全库无消费方，进度计算逻辑复制两份）；OP5 _emit_event 统一出口只被 3 个新方法用，其余 10 个 _report_* 各复制 callback 逻辑；OP6 _calculate_changes removed 按行数差计算替换场景失真；OP7 _estimate_complexity low_comments factor 不计分 + 注释只认 # 开头行；OP8 _report_final_metrics 的 _llm_call_count/_retry_count 全库无维护点恒 0 | ✅ 已完成 | [modules/orchestrator_progress.md](modules/orchestrator_progress.md) |
| Tracing | app/agent/tracing.py | 246 | **TT1 模块级 float() 未校验：OTEL_SAMPLING_RATE=abc/空串 → import tracing 抛 ValueError，12 个消费模块（architect/frontend/backend/test_runner/specialist_base/session_manager 等）import 全崩，单点环境变量误设导致整个系统启动失败（实测 architect import 崩）**；**TT2 OTel 启用路径零测试 + 模块级副作用（测试环境未装 opentelemetry 全在 noop 跑，OTEL_ENABLED=1 首次生产开启时 exporter 缺失/endpoint 错配才暴露，且静默降级 noop 无人察觉）**；TT3 get/set_trace_id + inject_trace_context/shutdown_tracing 生产零消费（仅 traced 被 12 处使用，默认 noop 下 get_current_trace_id 恒 None）；TT4 _set_result_attrs 不校验类型 + span_name 静态，同函数数百次调用 span 完全同名无法区分文件/模型；TT5 async 子任务 span context 不传播（create_task 默认新 context，并发子任务 span 脱离父链）；TT6 测试仅 2 个全在 noop 模式，异常记录/类型丢弃/装饰器失败语义零验证 | ✅ 已完成 | [modules/tracing.md](modules/tracing.md) |
| FrameworkDetector | app/agent/framework_detector.py | 188 | **FD1 vitest 分支 output_format 沿用 jest_json + test_command 依赖 scripts.test（vitest 输出与 jest 解析器结构不符 → 测试解析必失败）**；**FD2 任意 package.json "test" script 即判 jest（node --test/mocha/ava 假阳性，实测 node --test→jest）**；**FD3 _parse_ci_config 关键词顺序碰撞（pytest 先于 mvn/go/cargo，monorepo CI 含 pytest+mvn 判 pytest，实测）**；**FD9 test_detect_python_pytest 测的是默认 fallback 而非检测逻辑（requirements.txt 不在任何检查项，测试恒过假阳性）**；FD4 pyproject 字符串包含 pytest 判 pytest（实测注释含即判）+ tox/setup.cfg 只认存在；FD5 源文件 rglob 全量 + go→java→py→rust 顺序敏感（混入 *_test.go 误判 Go）；FD6 Makefile "test" 子串假阳性（实测 VERSION=test→make）无语言证据判 cpp；FD7 CI 只查硬编码 .github/workflows/test.yml 单文件；FD8 detect 返回 Optional 消费方无 None 兜底 | ✅ 已完成 | [modules/framework_detector.md](modules/framework_detector.md) |
| OutputParser | app/agent/output_parser.py | 216 | **OP1 PytestXMLParser 名不副实：format=pytest_xml 但只解析文本正则（真 XML 全 0，实测 pytest XML→0/0）**；**OP2 JUnitXMLParser skipped 计入 passed（tests=10/fail=2/err=1/skip=3 → passed=7 应 4，实测）+ 正则假解析非真 XML**；**OP3 vitest 输出经 jest_json 解析全 0（与 FD1 构成完整失效链：检测端打错格式标签→解析端产出 0/0，实测）**；OP4 GoTestParser 只数 --- PASS:/--- FAIL: 行，包级 FAIL（编译错误/panic）不计（实测 panic→failed=0）；OP5 RustTestParser errors 收集 test result 摘要行噪声；OP6 GenericTextParser 正则形态粗糙（"1 test passed" 漏匹配）；OP7 CppTestParser 纯委托 GenericTextParser 格式层级虚设；OP8 parse 参数名 format 遮蔽内置函数；OP9 JUnitXMLParser/JestJSONParser 零测试且全部用例为文本输入 | ✅ 已完成 | [modules/output_parser.md](modules/output_parser.md) |
| CodePatcher | app/agent/code_patcher.py | 629 | **CP1 精确匹配不验证上下文行：上下文不匹配的 patch 按行号静默应用到错误位置（实测 AAA/BBB/CCC + 上下文 ZZZ → ZZZ/CCC success=True 零警告；fuzzy 反而做上下文比对——精确比模糊更不安全）**；**CP2 @@ -0,0 +1,N @@ 新文件创建 old_start=-1 精确恒失败，fuzzy 靠空上下文恒真碰巧成功且 errors 与 success 并存**；**CP12 多 hunk 顺序应用行号漂移（实测双 hunk 增行 → line7 重复）**；**CP10 CrossFilePatcher primary_result 多文件循环覆盖（primary_file 第一个但 result 留最后一个）+ 整条跨文件补丁链路因 _apply_patches_incremental 无入口不可达（OF10）**；CP3 fuzzy 无上下文 hunk 退化为纯行号应用；CP4 .bak 备份残留 + 只留最后版本；CP5 apply_patch_to_file 非原子写；CP6 _parse_patch 裸空行截断 hunk；CP7 _extract 第三 fallback 响应剩余全当 patch；CP13 测试仅 2 个（多 hunk/新文件/上下文不匹配/fuzzy 零覆盖） | ✅ 已完成 | [modules/code_patcher.md](modules/code_patcher.md) |
| OrchestratorUtils | app/agent/orchestrator_utils.py | 410 | **OU1 成本估算体系分裂 + estimated_files 死变量（CEC7 详化：按 complexity.level 查静态表，30 文件 medium = 5 文件 medium，token/cost 与文件数架构内容脱节，审批实际用此表）**；**OU2 _validate_file_plan 反斜杠死分支（:62 正则不含 \\ → 反斜杠路径先被拒，:66/:71 的 \\ 分支永执行不到）+ UTF-8 中文路径全拒**；**OU3 _check_and_report_api_issues 每文件事件全量 rglob 读对端（AC6 位置确认：orchestrator_utils.py:225-245，N 个前端文件 → N 次全量后端扫描）**；**OU4 _record_learning_data 学习数据空样本（original/fixed 恒空只有 error_message，学不到「怎么修的」，file_type 硬编码 python）**；OU5 _is_anti_pattern 跨模块访问私有 _fix_patterns；OU6 _cache_review_gate risk_level 依赖 LLM 自由输出（缺失兜底 low 恒放行，审查对象是架构摘要 JSON 非代码）；OU7 _wait_for_approval 300s 超时过长 + 超时=拒绝语义不区分；OU8 _call_llm_for_patch 复用 backend_engineer/architect 不走统一 llm_client（LCL1 收敛范围）；OU9 _git_save_snapshot 裸 git init/commit 双快照体系 + --allow-empty 空 commit；OU10 _should_check_api_consistency .py 路径含 api/route 启发式；OU11 _compute_layers priority 缺失默认 3 分层扁平化；OU12 零测试覆盖（mixin 依赖宿主属性不可独立单测） | ✅ 已完成 | [modules/orchestrator_utils.md](modules/orchestrator_utils.md) |
| OrchestratorTesting | app/agent/orchestrator_testing.py | 306 | **OT16 智能测试选择全链路失效（ImpactAnalyzer()/TestSelector() 无参构造但 __init__ 必填 project_root → TypeError 恒被 except 捕获回退 []，test_files 恒空 → docker 命令不带文件 + 本地跑全量，日志每次「测试选择失败」无人察觉，测试选择能力从未生效）**；**OT21 docker 分支跳过失败聚类 + _report_test_results 事件（:35-37 直接 return，两路径输出结构不一致 = TR 双路径主线具体表现）**；**OT22 docker 分支走 FD-OP 失效链（detected_config.output_format → OutputParser.parse，FD1 vitest→jest_json / OP1 pytest_xml 假解析 / OP3 vitest 全 0 直接命中，解析全 0 但 success=result.success 可能 True 语义矛盾 + skipped 不计 total）**；OT18 _detect_test_command 顺序碰撞（npm test script 遮蔽 playwright）+ 命令硬编码 cd /app；OT24 _cluster_test_failures 正则 test_name 未 escape（参数化名含 [ ] ( ) → 正则异常聚类静默失效）+ 每调用实例化；OT25 _report_test_results skipped 字段填 errors 数语义错位；OT2 docker summary errors 字段双语义漂移（ValidationResult.errors 被 parsed.errors 覆盖）；OT20 零单元测试；OT26 _collect_all_tests 绝对路径与 _select_tests 相对路径基准不一致 | ✅ 已完成 | [modules/orchestrator_testing.md](modules/orchestrator_testing.md) |
| Orchestrator | app/agent/orchestrator.py | 138 | **OA12 dependency_graph 开关死配置 + dependency_graph_obj 死属性（开关 :52/:83 全库零消费；dependency_graph_obj :126 全库零赋值恒 None → orchestrator_files.py:342/364/630 依赖上下文注入与全文件枚举恒空；依赖图实际在 architect:826/spec_first:222/:249/traditional:161 内局部 build_from_architecture，共享依赖图从未建立——DG 详档补充：不仅两套构建方法死代码，编排器消费属性也未接线）**；OA1 输出目录归一化分叉（./projects 字符串前缀判断 + 1/xxx 拼 PROJECTS_BASE_DIR vs ./projects 留 CWD 两套相对路径语义 + ImportError 静默 pass）；OA4 CostTracker 实例化（OP1 恒零位置确认）；OA3 analyzer/complexity 双对象近义命名；OA6 __init__ 延迟导入固定实例化 GitOperations/SnapshotManager；OA13 __init__ 24 参数注入过载；OA2 子组件全 None 由 _initialize_components 注入（dependency_graph_obj 不在清单 = OA12 成因） | ✅ 已完成 | [modules/orchestrator.md](modules/orchestrator.md) |
| SessionManager | app/agent/session_manager.py | 582 | **SM14 小变更文件在 state.unchanged_files 重复（:515 进 unchanged 又进 small_changes，:525 再拼 → 实测 embedding 分支 state.unchanged_files=['a.py','a.py']）**；**SM2/SM3 增量检测复用能力从未生效（update_file_status :236 全库零消费 → fs.content_hash 恒空 + fs.content_embedding 恒 None → detect_incremental_changes :500/:508 恒 False → 所有已存在文件恒判 changed，断点续传复用/小变更跳过死能力，embedding 分支仅手工设字段可达）**；**SM10 DB 唯一真相源声明 vs 实际不同步面（_sync_to_db 只在 complete/cancel/cleanup 调用，create/pause/resume/update 不写 DB）**；SM4 detect_incremental_changes 读文件无异常处理单文件失败中断；SM1 asyncio.Queue 在 dataclass 跨事件循环风险；SM9 create_session 默认 session_id 秒级时间戳同秒冲突覆盖；SM8 SESSION_DIR=./sessions 相对 CWD 漂移；SM11 两套 DB 访问（注入 factory vs 全局 async_session import）；SM13 get_session_status 不含 hash/embedding 不可观测 | ✅ 已完成 | [modules/session_manager.md](modules/session_manager.md) |
| Architect | app/agent/architect.py | 987 | **AR3 _extract_json_with_llm 恒失败（实测：Specialist.call_llm 返回 str 但 :362 response.get("choices") 按 dict 契约 → AttributeError 恒被 except 捕获返回 None → 降级链 = 解析失败→LLM 辅助提取（恒 None）→默认 3 文件架构，LLM 输出非标准 JSON 时架构设计整体退化，v1.11 两套 LLM 契约主线第 N 处混淆）**；**AR8 expand_file_plan 依赖 DG3 死方法（:830 get_missing_files 恒空 → :831 依赖图完整性补充分支永不执行，只能靠 LLM 分批补充，与 DG 详档闭环）**；**AR2 _validate_and_enhance_api_spec 默认 api_spec 只有 /api/v1/health（无业务 API 定义即继续生成 → 后续 API 契约校验无参照物形同虚设）**；AR4 默认 db_schema 硬编码 users 表 + 已有表强制补 id/created_at；AR6 前端补充条件缺陷（has_frontend_types True 时缺 css/js 永不补）+ 多语言单适配器；AR9 expand_file_plan while 循环无批次上限多轮 LLM 串行；AR10 _generate_batch_files 单次调用无重试；AR12 prompt f-string 双重转义示例非标准 JSON；AR14 design_architecture 只捕获 ValueError 异常面窄；AR16 未知语言 dep_file 退化为 README.md | ✅ 已完成 | [modules/architect.md](modules/architect.md) |
| ImpactAnalyzer | app/agent/impact_analyzer.py | 205 | **IA1 新旧同名（modified）符号同时出现在 new_symbols 与 modified_symbols（:86 过滤只排 added 语义，实测 helper 既「新增 2 个符号」又「修改 1 个符号」自相矛盾）**；**IA2 只解析 Python（:133 ast.parse 非 .py 全失败，实测 app.js AST 解析失败符号全盲——「精准影响分析」对多语言项目只对 .py 生效）**；**IA3 名不副实——docstring 声称识别受影响文件范围，实现只做符号 diff 不做影响传播（无引用者/依赖图扫描），TestSelector 即便拿到 ChangeSummary 也无受影响文件列表**；IA7 无 old_versions 时未变文件既有符号也进 new_symbols（:71 全量累积，「新增」语义失真）；IA4 _has_dynamic_imports 子串假阳性（:169 getattr( 普通反射/注释 __import__ 都判 True）；IA5 符号粒度只有 FunctionDef/ClassDef（AsyncFunctionDef 未处理） | ✅ 已完成 | [modules/impact_analyzer.md](modules/impact_analyzer.md) |
| TestSelector | app/agent/test_selector.py | 215 | **TS6 最小充分集从未实现（实测三层结果恒≈全量：修改 src/foo.py / src/auth.py / 测试文件本身三种输入都返回全部 3 个测试——冒烟层 :179-185 不足 5 个补足全量 + 高依赖层 rglob 全选 + :59-62 空回退兜底，智能选择是装饰性的）**；**TS1 同目录层映射错位（:94 os.path.join(test_dir, file_dir) 把 src/foo.py 映射到 tests/src/ 不存在 → 第一层恒空，未做 src→tests 前缀替换/扁平命名匹配）**；**TS2 高风险命中=全选 + 子串匹配假阳性（:126 any(risk in f) 无路径边界，auth 命中 authentication.py；:137 命中即 rglob 全部测试非「相关」测试）**；TS4 naming_convention 只认 test_*.py/*_test.py 两种 Python 命名（JS *.test.js/Go *_test.go 全漏）；TS5 冒烟关键字硬编码英文 smoke/core/basic（:156 不可配置） | ✅ 已完成 | [modules/test_selector.md](modules/test_selector.md) |
| FailureClusterer | app/agent/failure_clusterer.py | 219 | **FC2 位置解析只认标准 Python traceback（:186 正则 File \"...\", line N 不匹配 pytest 短格式 `tests/test_x.py:12: in test_x`，实测 error_location 恒空 → 聚类退化为仅按 error_type，位置维度全丢——TR 链实际输出恰是 pytest 短格式）**；FC1 聚类键含绝对路径 + 精确行号（:98 同类型同文件不同行分簇、跨环境路径分簇；实测同根因同抛错点能正确聚 1 簇，问题在归一化）；FC3 关键词只有 1 个（:197 最后一行前 50 字符，空行结尾恒空）；FC5 :76/:86 result['name'] 直接索引缺键 KeyError | ✅ 已完成 | [modules/failure_clusterer.md](modules/failure_clusterer.md) |
| ProjectProfiler | app/agent/project_profiler.py | 791 | **PP10 外部包污染 high_dependency（:547 _is_project_module 只查绝对路径+stdlib 前缀，无点外部包 flask/requests 全判项目模块，实测 5 文件 import flask → high_dependency=['flask.py','requests.py']，下游 TS2 高风险判定失真）**；**PP8 风险关键字超短子串假阳性（:507/:509 any(kw in content) ——'db' in "web dashboard" 命中，实测 src/webapp.py 进 data_critical；'db'/'session' 同时出现在 SECURITY/DATABASE 两组）**；PP5 _is_test_dir 子串匹配（:335 实测 contest/latested 判测试目录，Go test_dir_names 空恒 False）；PP15 typescript 分支死代码（:635-638/:579，实测传 typescript 回退 python 分支不可达）；PP6 _module_to_filename 多语言映射近似（JS ./foo.js、Go 原样）；PP3 detect_project_language 单语言选择 + manifest 只查根（monorepo 子包不算，与 OU10 同族）；PP14 test_location 取 os.walk 首个测试目录（:612 多目录不确定） | ✅ 已完成 | [modules/project_profiler.md](modules/project_profiler.md) |
| Utils | app/agent/utils.py | 1383 | **UT5 沙箱验证失效静默通过（:973-978 bwrap 缺失 FileNotFoundError/超时 → return True,[]，实测本环境 bwrap 不存在 → validate_in_sandbox 恒 (True,[])——spec_first/traditional 生成链路的 sandbox_ok 全部空转，「存在≠正确」在验证执行端的直接证据）**；**UT10 LLM 语言检测 "NO" 子串假阳性（:1066 "NO" in result.upper()，实测 LLM 回答 "Note: this is Python code" 误判语言不匹配 → 合法代码被拒触发恢复流程）**；**UT6 _generate_script_with_ai async 上下文 get_event_loop().run_until_complete 必抛 RuntimeError（:862-865 → 未注册扩展名恒降级括号匹配验证器）**；UT14 JS/Go/Rust 验证器忽略 level（import/run 等价 syntax，与 FD1/OP3 同主线）；UT7 GenericSandboxValidator 括号匹配无意义（:741 引号内括号也计）+ run 级执行任意代码（:472-557，与 CV2 同族）；UT13 validate_syntax_for_extension 只覆盖 .json/.py 其他恒 True（:320-338）；UT11 repr(files) 嵌入生成脚本（:404/:576/:627 内容含特殊转义破坏脚本） | ✅ 已完成 | [modules/utils.md](modules/utils.md) |
| BackendEngineer | app/agent/backend_engineer.py | 356 | **BE1 _infer_file_type_from_path 子串假阳性（:43-62 'api'/'app'/'db' 子串——实测 therapeutic/api.py→api、capital/service.py→api（真实 service 误判）、apple.py→entry、happened.py→entry、web/db_utils.py→database；file_type 决定 project_spec 约束注入 :130，错误类型→错误框架/存储/术语约束）**；**BE7 analyze 工具访问键 'function' 错误（:310 list_files_tool['function'] 恒 KeyError——SPECIALIST_TOOLS 键是 'fn'（tools.py:1184），except 吞掉 → project_files 恒空 → analyze 的「先用工具读取项目结构」从未生效；对照 generate_file 走 ReActEngine _execute_tool 用 ["fn"]（react_engine.py:208）正确——同一工具表两处不同键）**；BE5 SYSTEM_PROMPT property 每次调用都重新加载（:17 get_skill + 文件读） | ✅ 已完成 | [modules/backend_engineer.md](modules/backend_engineer.md) |
| StrategyEvaluator | app/agent/strategy_evaluator.py | 329 | **SE1 A/B 框架策略库恒空（create_or_update_strategy 全库无调用方，repair_strategies.json 全盘不存在；实测空库 get_strategy_template→(None,None)，record(strategy_id=None) 只 append history；error_recovery 侧 if strategy_id: 保护使评估记录也不落——80/20、variant 探索、promotion 全死路径，修复模板恒走 _build_default_fix_template 从未被 A/B 优化）**；**SE2 promotion 连续更优判定要求严格交替配对（_check_strategy_promotion :244-246 只在 (candidate,main) 相邻计分、:236 需≥6条、:232 全局窗口被跨 error_type 稀释——即使有数据也难触发）**；**SE3 strategy_learner.py（399 行 Q-learning）全库死代码（rg 零消费方、不引用 evaluator——Evaluator-optimizer 两侧评估无数据 SE1/学习无入口 SE3 都未落地）**；SE4 全模块用 print 而非 logger（:71/:83/:264）；SE5 _save_strategies 无锁并发写；SE6 策略库默认相对路径依赖工作目录 | ✅ 已完成 | [modules/strategy_evaluator.md](modules/strategy_evaluator.md) |
| SpecCache | app/agent/spec_cache.py | 605 | **SC1 磁盘索引懒加载死代码→进程重启缓存全丢（_ensure_index_loaded/async_save/async_lookup/async_clear 全库无调用方，消费方 spec_first:46、traditional:34 走同步 lookup 从不加载 index.json；实测 save 后新实例 lookup→None 且 _cache=0，磁盘数据完整读侧不读）**；**SC2 dependency_graph 缓存不持久化 + async_save 缺参（:431 存内存但 _save_entry:538-551 与 _save_index:171-187 序列化均无 dependency_graph 键，实测落盘与 index 无该键→spec_first:129-132 from_dict({}) 恒空图，DG3 补缺主线缓存路径断供）**；**SC3 Jaccard 0.85 阈值恒不达→spec_first 模糊命中形同虚设（:46 lookup 不传 vector→恒 Jaccard 降级；实测「用户管理系统/登录注册/Flask/MySQL」相似对 0.5、「博客系统 vs 博客平台」0.25，粗粒度关键词 Jaccard 结构到不了 0.85）**；SC4 tech_keywords 双份拷贝（:212 vs :265 DRY）；SC5 双 save 点 tech_stack 来源不一致（spec_first key_technologies vs orchestrator_utils architecture.tech_stack）；SC6 Jaccard 用 requirement_preview 前 200 字符截断 | ✅ 已完成 | [modules/spec_cache.md](modules/spec_cache.md) |
| LanguageDetector | app/agent/language_detector.py | 775 | **LD1 C# 关键词 \bc#\b 边界失效（:69+:266——实测 detect("请用 C# 写一个项目")→python 0.30 默认降级、detect("用C#开发工具")→python 0.30（c 前中文 \w 边界失败），而 "ASP.NET Core"→csharp 正常；# 是 \W，\b 需 \w↔\W 交界，最常见 C# 表达全漏，且 .cs 扩展名也不在 ext map，两条自救路径皆断）**；**LD2 扩展名正则吞中文（:285 `\.(\w+)(?:\s|，|。|,|\.|$)`——\w 含中文、分隔符白名单无中文，实测 "app.py文件"→ext='py文件' 漏配、"main.py 和 utils.js"→正常，中文需求「xx.py文件」提法全数漏配）**；**LD3 LLM 辅助检测死代码（_detect_with_llm_sync/_detect_with_llm 全库无调用方，docstring 宣称的策略 5 冲突时 LLM 裁决从未接线，detection_method 恒 "rule"，_check_language_conflict 只写 evidence 不改结果）**；**LD4 LLM 分支 typescript 必被拒（:594-605 lang_aliases={"ts":"typescript"} 但 valid_languages 无 "typescript"——被并入 javascript 列表 :51，LLM 返回 TS 即 return None，与规则层「typescript→javascript」语义不一致）**；**LD5 csharp 规则 needs_clarification=True（:748-762 get_language_specific_rules("csharp") 因 LANGUAGE_EXTENSION_MAP 缺 csharp 键返回「未知语言」，architect 放弃内置规则，与 LD1 叠加时 C# 需求完全无语言约束）**；LD6 \bgo\b 匹配英文动词（实测 "Please go to the market"→go 0.95）；LD7 冲突检测只记录不裁决（与 LD3 同源）；LD8 adapter_map 含 "typescript" 但语言表无此键（与 LD4 同源） | ✅ 已完成 | [modules/language_detector.md](modules/language_detector.md) |
| FrontendEngineer | app/agent/frontend_engineer.py | 326 | **FE1 _infer_file_type_from_path 子串假阳性（:47-52 'page'/'view'/'component'/'test'/'spec' 子串——实测 preview.js→frontend_page（view 命中 preview，普通 JS 组件误判页面）、interval/page.tsx→frontend_page（page 命中 interval）；:43-46 .html/.css 用 endswith 精确匹配正确，:47 起退化子串）**；**FE7 analyze 工具访问键 'function' 错误（:280 list_files_tool['function'] 恒 KeyError，SPECIALIST_TOOLS 键是 'fn'——与 BE7 同款，analyze 结构读取恒空；generate_file 走 ReActEngine ["fn"] 正确）**；FE5 SYSTEM_PROMPT property 每次调用重新加载（同 BE5） | ✅ 已完成 | [modules/frontend_engineer.md](modules/frontend_engineer.md) |
| ReviewChain | app/agent/code_reviewer.py（168）+ ai_reviewer.py（223）+ multi_angle_review.py（331） | 722 | **CR2 版本兼容检查子串假阳性 + 本环境版本误用（:146/:152 removed_api in code 子串，实测注释「Middleware」→假 issue「[fastapi v0.136.1] 已移除」、字符串「OAuth2PasswordBearer」→假建议，干净代码→[]，合法代码被 needs_fix 拦入修复；判断基准是本环境已装版本非用户运行环境）**；**CR1 四套审查实现并存（认知修正：CodeReviewer 活跃：specialist call_llm 返回 str + re 解析 + 裸 Dict / AIReviewer 孤儿：app.utils dict + pydantic ReviewResult / multi_angle_review.py 3 角色 LLM 孤儿（MAR1）/ MultiAngleReviewSkill YAML checklist 模板死链——原「3 角色」定性错误，实测 agent_skills.py:66-139 无 LLM 调用）**；**CR3 AIReviewer 唯一实例化点 multi_model_agent.py:71 在 re-export 壳（生成链实际用 CodeReviewer）**；CR4 prompt 每次 get_skill；CR5 VERSION_RULES 仅 4 库；CR6 .claude/skills/orchestrator 路径；零直接测试 | ✅ 已完成 | [modules/review_chain.md](modules/review_chain.md) |
| MultiAngleReview | app/agent/multi_angle_review.py | 331 | **MAR1 顶层模块零消费方（孤儿模块，「能力未接线」家族第七例）：multi_angle_review/parallel_multi_review/_review_with_role/devil_advocate_review/parse_* 全库零调用，3 角色 LLM 并行审查（docstring 声称「严格：+多视角审查」）从未接线**；**MAR2 「多角度审查」四实现同名并存（CR1 认知修正实测：agent_skills.MultiAngleReviewSkill 是 YAML checklist 模板 6 类无 LLM 调用、唯一消费方 pre_modify_review 零调用仅元数据展示；真正 3 角色 LLM 是本模块且孤儿）**；**MAR3 魔鬼代言人双副本 JSON 契约不一致（devil_advocate.py 活：challenges/target_item/challenge，消费链 mixin:112→evaluate_mixin+association_endpoints；本模块 :238 死副本：reviews/target/issue+role——同一概念两套输出键）**；MAR4 architect 死参数（:248 起不引用）+ STRICT 不传（:125 vs :121）；MAR5 正则贪婪 \{[\s\S]*\} 跨块误吞（:210/:300/devil_advocate.py:66 同款）；MAR6 DEVILS_ADVOCATE_MODEL 硬编码 + 三处直连 call_llm（LCL1）；MAR7 LIGHT 模式声称「契约检查+交叉验证」实际直接 return []（:114-117）；MAR8 失败与零问题同返回 []（成功态家族）；MAR9 role 索引依赖 REVIEW_ROLES dict 顺序（:166） | ✅ 已完成 | [modules/multi_angle_review.md](modules/multi_angle_review.md) |
| FeedbackLearner | app/agent/feedback_learner.py | 428 | **FL1 _build_error_regex 把错误消息拆成单词 OR→反模式拦截整单误伤（:278-282 "|".join(findall)，消费方 orchestrator_utils:21 re.search(pattern, requirement) 匹配需求文本；实测 error_message="module 'flask' has no attribute 'Foo'" → pattern=`module|flask|has|no|attribute`，_is_anti_pattern("用 flask 写一个用户系统")→True 整单拒绝，语义错位 + OR 拆词放大误伤面）**；**FL2 is_anti_pattern 二态失真（失败≥3 且成功率<0.3 硬阈值，低频永不触发、达标即整单误杀，无缓冲档）**；**FL3 同步 _find_relevant_patterns 死代码且与 async 版（:208）逻辑分叉（缺 is_anti_pattern 过滤）**；FL4 async_record_fix/async_save_patterns 死代码（SC1 async 家族）；FL5 ./data/learning_data 相对路径 + session_records 不持久化；FL6 跨模块访问私有 _fix_patterns；FL7 逐条串行 embedding；零测试 | ✅ 已完成 | [modules/feedback_learner.md](modules/feedback_learner.md) |
| JsonParser | app/agent/json_parser.py | 345 | **JP1 顶层标量 JSON 不 raise 直接返回非 Dict/list（实测 safe_parse_json('null')→None、'123'→123、'true'→True、'"abc"'→'abc'，违反 docstring「Raises ValueError」契约；11 个消费方按 Dict/list 假设使用，extract_json_field 宽 except 吞 AttributeError 静默当字段缺失）**；**JP2 截断修复静默补全返回不完整数据当完整（实测 '{"tool":"read_file","params":{"path":"x"' → 补全成功但字段值中途截断时值不完整零提示；test_truncated_json_object 把补全当期望行为=测试固化错误预期 TR2 家族）**；JP3 `<thinking>`/`<thought>`/`<reasoning>` 变体标签不清理（实测依赖层 3 花括号提取间接兜底，数组 JSON 场景仍受影响）；JP4 模块级单例无锁；JP5 extract_json_field except (ValueError, Exception) 冗余宽吞 | ✅ 已完成 | [modules/json_parser.md](modules/json_parser.md) |
| Memory | app/agent/memory.py | 580 | **MEM1 embedding 恒 None → 语义搜索 search_async 恒返回空（MemoryEntry.embedding :49 默认 None，全模块无 .embedding= 赋值点；search_async :209/:310 if entry.embedding 恒 False → 正常路径恒空、比字符串 fallback 更差；本环境 get_embedding 异常使恒回退掩盖 MEM1）**；**MEM2 压缩阈值 15 远小于 max_entries=100 → 对话 16 条即摘要化细节丢失、max_entries 虚设 + _is_compressed 死字段 + 摘要嵌套退化（实测 17 条→[summary+6]、再压摘要内容混入噪声词）**；**MEM3 get_embedding 无 key 保护（AiCodeUtil.py:126 f"Bearer {settings.SILICONFLOW_API_KEY}" 空 key 生成 "Bearer " 非法 header，实测每次 search_async 先失败一次再回退）**；**MEM4 get_with_context 参数名 max_tokens 实际按字符截断（:175 len() 与 max_tokens 比较，英文 4 字符≈1 token 浪费窗口）**；MEM5 中文关键词提取无效 + 停用词噪声（实测摘要含 "AI, 20, 条用户消息"）；MEM6 纯内存无持久化 + 秒级 session_id 冲突（SM9 家族）；MEM7 KnowledgeMemory 同 key 覆盖旧知识 importance 降级；零测试 | ✅ 已完成 | [modules/memory.md](modules/memory.md) |
| ConversationStore | app/agent/conversation_store.py | 317 | **CS1 append_message 「读-改-写」非原子 → 并发丢消息（实测并发 3 条仅存 1 条 last-write-wins）+ async 上下文 Redis miss 时 _load_from_db_sync 短路（:104 loop.is_running→[]）Redis 被覆盖为单条**；**CS2 DB 写失败仍写 Redis（:155 db_success 不短路）→ 幽灵消息破坏「DB 权威」声明（:5），且 Redis 命中不回填 DB 使「下次读取回填」注释（:164）不成立**；**CS3 compress_history clear+re-append 非事务中途失败丢历史 + 压缩期并发 append 被 clear 误删 + 全库零调用死代码**；**CS4 _estimate_tokens docstring 声称双语规则实现仅 len//2（:37）→ 英文 token 高估 2 倍历史过早丢弃**；CS5 async 方法内同步 Redis 阻塞事件循环（redis.from_url 非 asyncio）；CS6 get_history/get_history_async 双实现重复（sync 短路是 CS1 源头）；CS7 模块级单例无锁；CS8 truncate_history 轮次 ×2 假设（tool/system 混入后不准）；零测试 | ✅ 已完成 | [modules/conversation_store.md](modules/conversation_store.md) |
| CloudLearningHub | app/agent/cloud_learning_hub.py | 336 | **CLH1 生产代码零消费方（死代码模块）——§5.1 学习闭环四组件（strategy_learner/user_preference_learner/fix_pattern_cache/cloud_learning_hub）全部零生产调用方，知识共享层从未接线**；**CLH2 cache_dir 参数虚设（实测：传 cache_dir=tmp 后读写走模块级 CLOUD_CACHE_DIR「./data/cloud_learning」，tmp 空且文件落盘报 Errno 2 被 except 吞；相对 CWD 漂移；测试全部断言内存态掩盖落盘失败 TR2 家族）**；**CLH3 质量门槛「1 票成功即高质量」（实测上传 success=True → quality_score=0.7 恰达阈值即可检索；无最小样本量，失败稀释需 7:3 才回落）**；**CLH4 上传哈希键（error_type:error_message:fix_description md5）同键重复上传只累加票数不更新内容（fix_example/file_types 改进被丢弃），error_message 含随机细节时同错误重复条目**；CLH5 clear_cache 删共享文件一实例清全实例；CLH6 单例参数忽略后续调用者（ERL5 家族）；CLH7 读写无锁（FL5 家族） | ✅ 已完成 | [modules/cloud_learning_hub.md](modules/cloud_learning_hub.md) |
| ArchitectJsonParser | app/agent/architect_json_parser.py | 15 | **AJP1 包装层无类型保护 → JP1 顶层标量穿透生成链（实测：safe_parse_json('null')→None 后 architect.py:277 architecture["project_type"] 抛 TypeError、ppt_agent.py:258 data.get("slides") 抛 AttributeError 且外层 except ValueError 捕获不了；react_engine 因有 isinstance 检查幸免；JP1 经本层放大为架构/PPT 生成崩溃）**；AJP2 迁移未完成双入口并存（architect/react_engine/ppt_agent 走包装、refinement_loop 直连 json_parser，JP1 修 raise 后行为分裂）；AJP3 经包装二次引用 _get_parser 无锁单例（JP4 家族） | ✅ 已完成 | [modules/architect_json_parser.md](modules/architect_json_parser.md) |
| AgentMemory（models+services） | app/models/agent_memory.py + app/services/agent_memory_service.py | 144+367 | **AGM1 两套记忆体系完全割裂（DB 层与运行时 memory.py 互不相通——runtime 从不落 DB，DB 层 session/memory/reflection/tool_log/model_stats 五表 service 方法生产零调用方，AgentReflection/ToolExecutionLog 全库无写入方；MEM6 纯内存无持久化根因即此）**；**AGM2 embedding 链三断（DB 层 add_memory_entry 无 embedding 参数恒 None、KnowledgeEntry 无 embedding 字段、search_knowledge 只能 ilike 字面匹配——与 MEM1+MEM3 叠加语义搜索三层全灭）**；**AGM3 同一能力三实现并存（会话创建 service.create_session + AiProjectCode.create_agent_session 直接 ORM；知识写入 add_knowledge + accumulate_knowledge 无 knowledge_key 无 dedupe 重复堆积）**；**AGM4 AgentSession.context_summary 死字段 + get_memory_context 按条数 50 截断无 token 预算含 TOOL 条目（CS4/MEM4 家族）**；AGM5 update_model_stats avg 分母含失败请求失真 + ModelUsageStats 无成本字段（OP1 schema 侧）；AGM6 get_memory_context 最新 N 条截断（CS4 家族）；AGM7 log_tool_execution tool_result 硬编码截断 10000 无标记（TR2 家族）；AGM8 update_model_stats/increment_knowledge_usage 并发读改写无行锁（CS1/MCP1 家族）；AGM9 KnowledgeEntry.knowledge_key 无 unique 与 MEM7 同 key 覆盖语义矛盾 | ✅ 已完成 | [modules/agent_memory.md](modules/agent_memory.md) |
| DynamicModelRouter | app/agent/dynamic_model_router.py | 1035 | **DMR1 配置读取失败静默降级（_build_provider_map :58-59/_build_model_id_to_key :76-77 except 返回旧 cache 无日志；模块加载期即构建，配置暂不可用则整会话沿用空映射不自动重试）**；**DMR6 DEFAULT_FALLBACK_ORDER 首尾重复（实测：["Qwen/Qwen3-8B","THUDM/GLM-4-9B-0414","Qwen/Qwen3-8B"] 第三项同第一项，3 级降级实为 2 个不同模型）**；**DMR15 学习路由数据链路断裂（实测：LearningRouter.record_call 生产零调用方，生产只写内存 metrics 不落 sqlite → sqlite 恒 0 条 → has_sufficient_data 恒 False → select_model 永不执行死代码；§5.1 学习闭环第三处数据断裂，与 SE1/CLH1 同构）**；**DMR14 健康感知路由生产零消费方 + enable_health_aware_routing 默认 False + docstring 写 enable_health_awareness 与字段名不一致（实测；参数忽略家族）**；DMR16 select_model 无数据时探索仍触发（实测 20% 概率选非第一候选，被 has_sufficient_data 掩盖）；DMR17 get_context_length/get_model_config 每次调用全量读配置 I/O；DMR18 record_failure 不记 latency 延迟权重失真；DMR19 sync/async 双路径无锁 dict 竞态 + _cleanup VACUUM 锁表 | ✅ 已完成 | [modules/dynamic_model_router.md](modules/dynamic_model_router.md) |
| FixPatternCache | app/agent/fix_pattern_cache.py | 266 | **FPC1 生产代码零消费方（死代码模块）——§5.1 学习闭环四组件（strategy_learner/user_preference_learner/fix_pattern_cache/cloud_learning_hub）全部零生产调用方确认完毕，修复模式复用从未接线**；**FPC2 find_similar_patterns 无关错误类型误命中（实测：缓存 import/ModuleNotFoundError，查询 syntax/IndentationError 完全无关类型仅凭共享词 web/python 即过 0.8 阈值返回 1 个不相关模式；BM25 把 project/file 当打分词、分数无归一化阈值量纲漂移）**；**FPC4 反模式判定需 4 次失败（实测：失败 -0.2 步长从 1.0 起 3 次到 0.4 不达 0.3，第 4 次才触发，docstring「>=3」不符）**；FPC5 30 天衰减作用于成功率本值（统计值被当排序权重，多次减半归零）；FPC6 中文 tokenize 无效（空格 split 无分词器，FL5/MEM5 家族）+ BM25 每查询 O(N) tokenize；FPC7 异步保存线程风暴无节流；FPC8 cache_file 默认相对 CWD + 模块加载即建单例（SE6 家族） | ✅ 已完成 | [modules/fix_pattern_cache.md](modules/fix_pattern_cache.md) |
| VectorIndex | app/agent/vector_index.py | 181 | **VI2 faiss 可选依赖导入模式缺陷（实测 faiss 未安装：模块级 try/except :7-10 但 _create_empty_index :53/load_or_create :36 裸 import → load_or_create 在 :48 try 块外抛未捕获 ImportError；layer2_semantic 外层 try 兜住 → 每轮静默降级 keyword，无外层则崩溃；TR2 家族）**；**VI1 embedding 链四断升级（build_from_metadata/add_project/search 全走 get_embedding，MEM3 入口不可用 → 索引构建与检索全失败；embedding 失效面 = MEM1 内存态 + MEM3 入口 + AGM2 DB 层 + VI1 索引，四层全灭）**；**VI5 SIMILARITY_THRESHOLD=0.35 弱阈值未校准 + _project_to_text 拼 30 条 feature 长文本可能超 embedding 上限**；VI3 build_from_metadata 假设 JSON 顶层为 list（dict 时遍历 keys → AttributeError 静默 count=0）；VI6 search/add_project 并发无锁（MCP1 家族）；VI7 EMBEDDING_DIM=768 硬编码未验证 bce-embedding 维度 | ✅ 已完成 | [modules/vector_index.md](modules/vector_index.md) |
| StrategyLearner | app/agent/strategy_learner.py | 399 | **SL1 生产代码零消费方（死代码模块）——§5.1 学习闭环四组件全部零生产调用方确认完毕，Q-Learning 策略优化从未接线（select_action 无人调用、update 无人喂 reward；与 SE1/FL1/FPC1/CLH1/DMR15 构成学习闭环全链路无一接线）**；**SL2 data_dir 参数虚设（实测：传 data_dir=tmp 读写走模块级 STRATEGY_FILE「./data/strategy_learning」，tmp 空且落盘 Errno 2 被吞；与 CLH2 同款）**；SL3 单步 MDP 使 γ×max_next_q 恒 0（next_state=None 终止态，Q 退化为 reward 均值无时序信用分配）；SL5 ε=0.2 无衰减永不收敛；SL4 _current_state/_current_action 实例状态并发串扰（MCP1 家族）；SL8 每 update 全量写 Q 表无锁（FPC7 家族） | ✅ 已完成 | [modules/strategy_learner.md](modules/strategy_learner.md) |
| UserPreferenceLearner | app/agent/user_preference_learner.py | 487 | **UPL1 生产代码零消费方（死代码模块）——§5.1 学习闭环四组件全部零生产调用方确认完毕，record_modification 无人调用、get_preference_prompt 无人注入；用户个性化从未生效（唯一面向用户反馈的学习组件）**；**UPL2 get_preference_prompt 默认值当用户偏好输出（实测：空画像输出「命名风格：使用下划线命名」「类型注解：使用类型注解」「架构风格：严格分层」——naming 默认 snake_case ≠ "mixed" 恒 True、type_annotations 默认 True、layer_separation 默认 strict ≠ moderate，接线即注入伪偏好污染生成）**；**UPL3 行集合 diff 丢失语义（set(modified_lines)-set(original_lines)：修改一行内容同行进 added+removed、整体重写集合爆炸、格式噪声当命名趋势）**；UPL4 置信度只增不减无纠正路径 + successful_predictions 死字段；UPL5 分析阈值 diff>5 vs 更新阈值 diff>10 不一致（实测新增 7 注释分析 more_comments 不更新偏好）+ 注释正则把字符串内 # 当注释；UPL6 技术变更 6 硬编码框架词注释/字符串误判 + 无置信度；UPL7 单例字典无限增长 + lock 外检查并发竞态（MCP1 家族）；UPL8 _modification_history 不持久化（MEM6 家族） | ✅ 已完成 | [modules/user_preference_learner.md](modules/user_preference_learner.md) |
| CriticalDecisionExtractor | app/agent/critical_decision.py | 332 | **CD1 用户决策从未注入生成 prompt（核心闭环断裂，实测）：get_decision_context_for_prompt 全库零消费方——120 秒等待的决策经 apply_user_choice 记录后只流向 ArchitectureInspector 事后检查基准（spec_first:795）与 ctx metric（:842），生成文件阶段的 prompt 从未收到用户决策（UPL1 用户输入「收集了未生效」家族）**；**CD2 超时/异常/空决策路径全部静默丢弃不填默认值（实测）：spec_first:189-194 三分支只 logger 后 continue，从不调用 skip_remaining_decisions（:326 唯一填默认值路径零消费方）→ 超时后 get_all_choices() 恒空 {}，docstring 宣称「使用默认值」实际静默丢弃（DMR1/MEM1 降级语义不符家族）**；CD3 _analyze_uncertainty 子串/条件启发式误判（"auth" 遇 "authlib"、"rest" 遇 "restful"；dict 分支要求 auth_explicit 等特定键架构师通常不输出）；CD4 枚举 8 类 vs 模板 7 类（deployment_mode 无模板永不触发）+ state_management/caching_strategy 死模板永不产出；CD5 apply_user_choice 不校验选项合法性任意值注入 + impact_files 子串匹配（"auth" 误匹配 "authorization.py"）；CD6 决策状态仅内存每请求新建实例不持久化（SM2 家族） | ✅ 已完成 | [modules/critical_decision.md](modules/critical_decision.md) |
| TopologyScheduler | app/agent/topology_scheduler.py | 521 | **TS1 全局超时强制取消后 success=True（实测确认）：run 超时取消所有任务（:259-263）后 _generate_file_with_retry 的 CancelledError 分支（:330-334）只设 node.error 后 raise，节点保持 GENERATING 不落 FAILED（跳过 :341-344 FAILED 块）；run 返回 success=len(failed_files)==0（:281）只统计 FAILED → 实测 global_timeout=1.0+sleep3s 生成器 → success=True、generated_files 空、completed=0——调度完全失败却报告成功，spec_first:313-319 拿空产物继续（「存在≠正确」执行端，UT5/MEM1 同族）**；TS2 取消路径节点状态残留（外部 cancel 不落 FAILED 永久 GENERATING）；TS3 死参数 timeout_per_file（:97/:104 收而不用）+ HeartbeatTracker._lock（:36）死字段 + interface_errors（:80/:494）恒 0；TS4 spec_first:866 注释「并行度降为 2」实际 max_concurrent=5（免费模型易 429）；TS5 spec_first:892 total+5 进度分母硬编码偏移；TS6 initialize_ready_queue 非幂等（run 复用二次入队重新生成覆盖）；TS7 BLOCKED 恢复 :416 dependency_count 置 0 未保存原值 | ✅ 已完成 | [modules/topology_scheduler.md](modules/topology_scheduler.md) |
| AgentExecutor | app/agent/agent_executor.py | 164 | **AE1 `ai_call` 步骤类型空转（:101-102 返回 pending 占位符不真正执行 AI 调用，task_planner:88 声明 ai_call 合法 → multi_model_agent:238 拿 pending append 当完成，planner 产出 ai_call 步骤即静默跳过 + 测试固化错误预期 test_ai_call_step:127-135）**；**AE2 `execute_analysis` 绕过 FileContract + 工具面含写操作（:153-165 走 ReActEngine + ANALYSIS_TOOLS 完全绕过 multi_model_agent:225 的 FileContract 前置验证；ANALYSIS_TOOLS 含 run_command 且白名单 tools.py:630-648 含 pip install/npm install/echo/git commit 等写操作，docstring「只读操作不修改文件」:116 与实现矛盾；MCP 工具合并 :138-145 也绕过）**；AE3 execute_analysis model 默认值硬编码 Qwen3-8B :110 + call_llm_fn 直连 app.utils.call_llm 不走 LLMClient 信号量/成本（LCL1）；AE4 execute_file_operation delete 无路径保护（:83-84 依赖调用方前置校验）；AE5 MCP 工具合并失败静默（:143-145 logger.debug）；AE6 _ANALYSIS_SYSTEM_PROMPT 声称只读与工具面不一致 | ✅ 已完成 | [modules/agent_executor.md](modules/agent_executor.md) |
| ShadowScanner | app/agent/shadow_scanner.py | 84 | **SHS1 阴影扫描整个能力依附于死路径 + 产出键零消费方（唯一调用点 dependency_graph.build_from_existing_project :965 是 DG1 已证死的生产死路径；result["shadow_dependencies"] :977 全库无任何消费方读取——即使接线结果也无下游使用，「只记录不阻断」实际连记录都没人读，能力未接线家族 UPL1 同类）**；SHS2 getattr_dynamic 正则只匹配字面量属性名（实测 getattr(obj,name_var)/attr="x"/attr=var 全 False 漏检，恰漏最危险的动态反射）；SHS3 env_dependency 不分读写方向（实测 os.environ["PATH"]=x 写、del 删都命中）；SHS4 正则扫非 AST（注释/字符串内 eval( 误报）；SHS5 每次调用 rglob 全项目无增量（asyncio.to_thread 不阻塞是正面设计） | ✅ 已完成 | [modules/shadow_scanner.md](modules/shadow_scanner.md) |
| ConsistencyChecker | app/agent/consistency_checker.py | 208 | **CC1 生产代码零消费方（孤儿模块）——docstring 声称的「增量修改后确认旧行为没被破坏」「文件生成后快速验证」全链路零接线（incremental_modify/orchestrator_files/spec_first 均无 import；health.py:138 的 check_all 是 HealthChecker 独立类无关），能力未接线家族最新成员，且面向的是 IM1 增量破坏旧行为最痛的回归确认场景**；**CC2 签名比较只比参数名（:102-103 只取 arg.arg，实测 def foo(a,b)→def foo(a,b=1) 默认值变化 drift 空、类型注解/装饰器全漏）**；**CC3 单向比较只查删除不回查新增（实测：新增函数/新增依赖/新增端点全不报）**；**CC4 config_files 列表虚设（:113 4 文件只有 requirements.txt 有比对逻辑，实测 package.json 依赖 1.0→2.0 零 drift）**；CC5 OpenAPI 只比 paths 键集合（method/参数/schema 变化全漏）+ yaml import 失败静默转 None | ✅ 已完成 | [modules/consistency_checker.md](modules/consistency_checker.md) |
| FileContract | app/agent/file_contract.py | 141 | **FCT1 降级路径必定产出 ai_call 空转步骤（AE1 触发链闭环，实测链路：task_planner:112/:122/:125 三处降级分支都 return _degrade_step → 构造 type:ai_call :137-138 → agent_executor.execute:101-102 返回 pending 占位 → multi_model_agent:238 append → success:True :245 从不检查 results——LLM 分解响应非 JSON/schema 不合法/异常时任务「降级执行」实际什么都没做还报告成功）**；**FCT2 validate_content 安全检查生产零执行（唯一生产消费方 ai_reviewer.review_file_operation :135 全库零消费方，multi_model_agent:225-236 前置验证只调 validate_path:234 从不调 validate_content——危险代码拦截 16 条模式 :85-108 整个生产链路从未执行）**；**FCT3 protected_files 子串误伤（:59 `protected in abs_path_str` 用子串包含，实测 id_rsa_backup.py 命中 id_rsa、known_hosts_notes.txt 命中 known_hosts——合法项目文件被误判敏感拒绝）**；FCT5 validate_content 正则双面缺陷（rm -rf ./data 漏检 + 安全列表参数 subprocess.call 误伤 + 注释内 eval 误伤）；FCT6 TaskStep.type 声明 4 种 vs execute 只实现 file_operation/ai_call 2 种（code_generation/tool_call 落「未知步骤类型」:104） | ✅ 已完成 | [modules/file_contract.md](modules/file_contract.md) |
| TaskPlanner | app/agent/task_planner.py | 180 | **TP1 decompose 产出面与 execute 执行面错配（FCT6 触发端：prompt :84-88 引导 LLM 产出 4 种步骤类型但 execute 只实现 file_operation/ai_call 2 种——LLM 按 prompt 产出 code_generation/tool_call 步骤即执行失败；test_decompose_success 的 mock 响应恰含 code_generation 掩盖错配）**；**TP2 降级计划与 review 门禁冲突双死（三处降级分支 :112/:122/:125 产出 degraded ai_call 单步骤计划 → review_plan :209 审查：拒绝则「计划审查未通过」成功路径也失败，通过则 execute 空转 pending——两条路都没真正降级执行任务）**；**TP3 默认模型硬编码 deepseek-r1-qwen3-8b :30 + decompose:98/_explore_project:140 直连 app.utils.call_llm 不走 LLMClient 信号量/成本（LCL1 家族）**；TP4 _explore_project 声称「只读工具」:151 但白名单 :154 含 run_command（写操作前缀 pip install/echo/git commit，AE2 同源）；TP5 探索轮数声明 2 轮 :135 实际 max_rounds=3 :164；TP7 空步骤列表报成功（LLM 返回 [] → steps=0 → success:True，test_decompose_with_context 固化空计划）；TP9 degraded 字段被 review_plan 消费为「强制拒绝」——ai_reviewer:189-198 has_degraded 检测含降级步骤即强制 approved=False, risk_level=high（LLM 判 high 保持拒绝、判非 high 强制拒绝），任何含降级步骤的计划最终都拒绝——降级路径在 enable_review=True 下必定「计划审查未通过」（TP2 更新） | ✅ 已完成 | [modules/task_planner.md](modules/task_planner.md) |
| MultiModelAgent | app/agent/multi_model_agent.py | 251 | **MMA1 `if task_type is None` 双重路由死代码（:123-125 已保证非 None，:178-180 恒不可达——route_by_content 与 emit 各重复一份，重构残留）**；**MMA2 `success: True` 恒定（:244-251 从不检查 results——全部步骤 pending/失败/降级都不影响 success，与 AE1 execute pending + FCT1 降级 ai_call 空转 + TP2 review 冲突组成完整闭环，编排端是空转链条最终报喜者）**；**MMA3 reviewer 模型切换每任务重查 + 实例竞态 + review 直连 call_llm（:199-207 每 process 都 get_dynamic_router+get_assignment_with_learning+ModelRegistry.get，切换写 self.reviewer.model 共享实例并发互覆盖 MCP1 家族；AIReviewer.review_plan 直连 app.utils.call_llm :13/:63 不走 LLMClient LCL1）**；MMA4 emit 流式回调失败静默（:118-119 只 warning，stream 场景唯一输出通道故障用户零感知）；MMA5 FileContract 前置验证只对 file_operation 步骤 + 只 validate_path 不 validate_content + 与 execute_analysis 路径 :158 两套校验并存（FCT2/AE2 编排端对应面）；MMA6 _get_semaphore 获取失败静默降级无并发控制（:85-90/:173-174）；MMA7 complexity 参数被计算展示但未参与路由（:121/:188 vs route_dynamic 签名 models.py:259 无 complexity 参数——5×5 矩阵复杂度维度虚设） | ✅ 已完成 | [modules/multi_model_agent.md](modules/multi_model_agent.md) |
| AIReviewer | app/agent/ai_reviewer.py | 223 | **ARV1 降级计划被 review_plan 强制拒绝（TP2 确证）：:189-198 has_degraded 检测含降级步骤即强制 approved=False, risk_level=high（LLM 判 high 保持拒绝、判非 high 强制拒绝）——任何含降级步骤的计划最终都拒绝 → multi_model_agent:210-217 返回「计划审查未通过」任务必定失败，降级路径 enable_review=True 下永不真正降级执行，enable_review=False 时空转报成功——按配置二选一的确定性双死（降级执行承诺完全未兑现）**；**ARV2 review_code/review_file_operation 生产零消费方（孤儿方法，multi_model_agent 只调 review_plan:209；review_code:31-103 代码质量审查从未接线；review_file_operation:105-142 是 FileContract.validate_content 唯一生产消费方，自身孤儿使 FCT2 死链坐实）**；ARV3 降级检测先于 LLM 审查则 LLM 调用纯浪费（:189 has_degraded 后 :193 无条件覆盖 LLM 结果，每降级任务一次无意义 LLM 调用）；ARV4 降级检测逻辑不一致（:193 条件仅 LLM 判非 high 时加「需人工审查」标记，LLM 判 high 时无标记——同是拒绝语义取决于 LLM 偶然输出）；ARV5 review_code except 面过宽 + 与 review_plan 降级严重度不一致（medium vs high :102 vs :222）；ARV6 prompt 无 degraded 字段说明 LLM 盲审降级计划（:154-165） | ✅ 已完成 | [modules/ai_reviewer.md](modules/ai_reviewer.md) |
| Models | app/agent/models.py | 385 | **MDL2 get_role_model 零消费方（「能力未接线」家族第六例）：5×5 角色路由完整实现（COMPLEXITY_LEVELS 校验 + get_assignment_with_learning + role_to_attr + role_fallbacks :288-334）但全库零调用；multi_model_agent:199-207 reviewer 模型切换是内联重复实现（MMA3 竞态点正落在此副本），接线与去重是同一动作**；**MDL4 route_by_content 关键词顺序歧义（实测：'分析文件内容'→reasoning 被 REASONING「分析」:370 抢走 FILE_OPERATION「文件」:373；'帮我检查一下环境配置'→code_review「检查」:367 误伤运维输入——先到先得无优先级）**；MDL1 DEFAULT_*_MODEL 常量语义双轨 + 注释错误（实测 :66 注释「与 ModelRegistry 中 key 对应」错误，4 常量全 name 格式，get() 按 key 查全 None，orchestrator_files:774-779 alt_map 混用字面量）；MDL3 get_role_model complexity 参数虚设（:306-307 只校验不用，MMA7 深化——复杂度两入口都未生效）；MDL5 route_by_content :379 第二 REASONING 分支恒不可达（:370 已覆盖 + `" reasoning"` 拼写残留）；MDL6 TASK_MODEL_MAP 12 枚举只映射 10（REACT/PLANNING 落默认单模型）；MDL7 route_dynamic 每调用导入+实例化 router（:270/:278/:283） | ✅ 已完成 | [modules/models.md](modules/models.md) |
| ArchitectureInspector | app/agent/architecture_inspector.py | 510 | **AI1 [P0] passed 恒 True（六项内建检查 severity 最高 high 无 critical + 唯一消费方 spec_first:797 无参调用 → LLM 审查死路径 → 架构检查门禁从未拦截）**；**AI2 [P0] _check_global_constraints 完全空转（applies_to 层名被当文件路径查 generated_files 恒 miss + "all" 被 continue 跳过 → 安全/权限约束检查从未执行）**；**AI3 [P1] 六类检查四类依赖架构师不产出的 key（layers/dependency_rules/naming_conventions 全库无生产者 → 结构性空转）**；**AI4 [P1] _violates_boundary 正则风格模式字面子串匹配（"import.*sql" 含 `.*` 永不触发 + SELECT/render 裸词命中注释字符串双向失真）**；**AI5 [P1] _check_import_direction 语义反转（allowed_targets 任一不在 import 即报违规，白名单被当「必须全含」）**；AI6 [P2] 文件归属用 "backend"/"frontend" 路径子串 + 框架 markers 子串误判；AI7 [P2] _check_api_style 单向关键词空检；AI8 [P2] _check_file_naming 仅两规则 / score 权重与 passed 口径不一致 / LLM 审查异常静默 / get_violations_by_type 死方法；零测试覆盖 | ✅ 已完成 | [modules/architecture_inspector.md](modules/architecture_inspector.md) |
| ServiceConfigTemplates | app/agent/service_config_templates.py | 461 | **SCT5 [P1] 7 公开函数 6 生产零消费方（「能力未接线」家族第八例：detect/generate 系列仅测试消费，架构师 .env/compose 走 LLM 提示从未调用模板库；SERVICE_TEMPLATES 与 SERVICE_CONTAINER_CONFIGS 双份配置并存）**；**SCT1 [P1] generate_docker_compose 的 depends_on 产出 Python dict 字面量（f-string 输出 `{'condition': 'service_healthy'}`，PyYAML 解析 list 元素为 dict，docker-compose 拒绝——接线即崩，DG1 同类）**；**SCT2 [P2] detect_services_from_requirements 短通用词子串误报（实测 "Users need these services"→elasticsearch、"session stores tokens"→redis；es/cache/search/session store 无词边界）**；SCT3 [P2] 6 模板 connection_code 全用 os.getenv 但缺 import os（复制即 NameError）；SCT4 [P2] service_container_manager:399-402 端口全局子串 replace 误伤密码（实测 pass5672word→pass5000word）；SCT6 [P2] 双份配置漂移风险（image/health 当前一致，手工复制无单向来源）；SCT7 [P2] 空密码产生空 env 行 + SECRET_KEY 弱口令硬编码；测试 32 passed 但 test_generate_docker_compose 只断言子串不解析 YAML（SCT1 未捕获） | ✅ 已完成 | [modules/service_config_templates.md](modules/service_config_templates.md) |
| PPTAgent | app/agent/ppt_agent.py | 420 | **PPT1 [P2] modify_outline 加页请求被 _validate_outline 截断删错页（实测：existing 4 页 + LLM 返回 5 页 → 结果 4 页且原 c2 被删，加页不生效反丢内容——modify 把 len(existing.slides) 当目标页数 + pop(-2) 删倒数第二）**；**PPT2 [P2] generate_outline 不遵守 num_slides（实测：请求 10 页 LLM 返回 3 页即返回 3 页，只有超页收缩无少页补齐）**；**PPT3 [P2] AJP1 认知修正（实测）：null/标量 JSON 在 ppt_agent 路径不崩溃——_validate_outline 内部 except Exception 兜底返回 None，静默降级模板大纲但消耗全部 3 次重试，AJP1「未处理崩溃」断言对当前版本不成立**；PPT4 [P2] _fallback_outline 页数边界不符（实测 num_slides=1/2 仍返回 3 页）；PPT5 [P2] PPT_DEFAULT_MODEL 双份定义（ppt_agent.py:23 + aiGeneratorPptx.py:62）+ 三处直连 call_llm 不走 LLMClient/DMR（LCL1 家族）；PPT6 [P3] quality 参数收而不用（声明空转）；PPT7 [P3] bullets[:6]/image_keywords[:3] 静默截断无日志；PPT8 [P3] bullet 40 字上限无强制；PPT9 [P3] _extract_json_with_llm raw_text[:3000] 截断无标记；零测试覆盖 | ✅ 已完成 | [modules/ppt_agent.md](modules/ppt_agent.md) |
| MultiLanguageParser | app/agent/multi_language_parser.py | 392 | **MLP9 [P2] 被 LanguageAdapter 取代的双轨并存未清理（全库确认）：生产实际消费 adapters/language_adapter.py（architect/cross_validator/integrity_validator/dependency_graph/生成链 5 大消费族），本模块全 app/ 零 import 仅测试引用，但保留完整文档+597 行测试+便捷函数制造「已接线」错觉，删除候选待执行**；**MLP1 [P2] string_regex 定义但从未使用（实测 docstring 内行首 import 被误解析：`doc="""\nimport os\nimport sys\n"""` → {'os.py','sys.py'}，_remove_comments_and_strings 只删注释不删字符串）**；**MLP2 [P2] Go 第三 pattern `"\s*([^"]+)\s*"` 匹配任意双引号字符串（实测 fmt.Println("hello world") → 误报 hello world/foo 为依赖）**；**MLP3 [P2] Go 无捕获组 pattern 返回整个 import 块为单个依赖（实测 import 块 → 垃圾条目，test_multi_import 子集断言掩盖）**；**MLP4 [P2] Java static import 取错捕获组（实测 import static java.lang.Math.PI → 'static .java'，test_static_import 只断言 len>=1 掩盖）**；MLP5 [P3] JS 扩展名补全依赖文件系统 exists() 非确定 + typescript 死分支；MLP6 [P3] C# using 别名误取（using Path = System.IO.Path → 'Path'）；MLP7 [P3] 无锁模块级单例（JP4 家族）；MLP8 [P3] 文档宣称 14 语言实为 13 键 + R 扩展名 .R 不可达（detect_language 已 lower） | ✅ 已完成 | [modules/multi_language_parser.md](modules/multi_language_parser.md) |
| GlobalConstraintParser | app/agent/global_constraint.py | 359 | **GC2 [P2] compatibility/security 约束在全量注入被过滤（实测，安全约束从未进入 prompt）：spec_first 只用 generate_prompt_fragment("all","all")，get_constraints_for_file 判定链 "all" in applies_to（compatibility=["frontend"]/security=["backend","api"] 不含 all）→ file_type in applies_to → _file_matches_category("all",COMPATIBILITY) 全 False → 提取成功但注入丢弃，实测「必须用 FastAPI。兼容 IE11。所有接口必须有权限校验」→ 全量注入仅 FastAPI**；**GC1 [P2] 单句多约束只取第一个（实测）：_classify_constraint 命中第一个 pattern 即 return + 分句正则 [。\n;] 逗号不分句 →「必须使用 FastAPI，兼容 IE11，所有接口必须有权限校验，响应时间不超过 200ms」整段一句只提取 FastAPI，4 约束丢 4**；**GC3 [P2] 普通需求误提取 general 约束（实测）：GLOBAL_KEYWORDS 含「必须/所有/统一」高频词 + pattern 不匹配兜底 general ARCHITECTURE →「必须有登录功能，用户数据统一存在 MySQL」被当全局约束注入**；GC4 [P3] 性能约束 pattern 不可达（GLOBAL_KEYWORDS 无「响应时间/延迟/性能」词，纯性能句被过滤 0 约束）；GC5 [P3] _file_matches_category 5 类恒 True 路径匹配形同虚设；GC6 [P3] get_constraints_for_file/merge_with_decisions/_file_matches_category 生产零消费（merge_with_decisions 与 CD1 决策注入主线相关）；GC7 [P3] constraint_id 用全局 len 非 category 内序号语义混乱；GC8 [P3] 分句正则不含英文句点放大 GC1 | ✅ 已完成 | [modules/global_constraint.md](modules/global_constraint.md) |
| GitOperations + SnapshotManager | app/agent/git_operations.py (346) + snapshot_manager.py (213) | 559 | **GO1 [P2] --allow-empty 使「无变更跳过」永不触发（实测）：commit_snapshot 用 git commit --allow-empty，无任何变更连续两次提交返回不同 hash（均创建空提交）→ git_operations.py:137-139「无变更返回 None」只对失败生效，save_snapshot:98-100「无变更需要提交」成死分支，传统链路每次生成结束（orchestrator_utils:339 _git_save_snapshot）无论有无变更都产出空提交+tag，快照列表被垃圾空快照稀释**；**GO2 [P2] rollback 删除当前分支永远失败（实测）：revert_to_commit 用 reset --hard 回滚后当前分支仍是 feature，delete_branch(current_branch) 删当前检出分支被 git 拒绝返回 False，snapshot_manager:153 静默忽略 → 默认 delete_branch=True 的分支清理永远失败，快照分支永久残留**；**GO7 [P2] rollback 结果谎报（实测）：current_tag 恒 "main"（:158）但回滚后实际仍在 feature 分支 + files_restored 用保存时恒空 files_changed（orchestrator_utils:343 传 []）而非 git 实际恢复文件 → 端点 success=True 但恢复文件列表恒空、分支与返回不符**；GO3 [P3] get_current_branch 非 git 仓库谎报 "main"（实测）；GO4 [P3] get_head_commit 无提交仓库返回字面量 "HEAD" + 零调用方（孤儿方法）；GO5 [P3] list_snapshots 用 \| 分隔符解析，message 含 \| 时截断（实测）；GO6 [P3] 分支已存在时 create_branch 返回 None，save_snapshot 静默 commit 到错误分支；GO8 [P3] finalize_session（会话结束合并）全库零调用方（能力未接线，方法级）；GO9 [P3] init_repo 的 git init returncode 不检查谎报成功；GO10 [P3] commit_snapshot 的 git add returncode 不检查；GO11 [P3] merge_branch 的 git checkout target returncode 不检查；GO12 [P3] revert_to_commit 用 reset --hard 无备份无确认（回滚端点外部暴露）；零测试覆盖（test_v4_8_features.py:336-351 仅测 SnapshotInfo 构造） | ✅ 已完成 | [modules/git_operations.md](modules/git_operations.md) |
| SharedContext | app/agent/shared_context.py | 337 | **SC1 [P2] 依赖管理整条死链（实测）：register_file 全库零调用方 → dependencies 恒 {} → get_dependencies_for/get_dependents_of/get_generation_order 全恒空/退化（实测只 save_file_content 后 order 返回纯注册序），docstring 声称的「依赖关系图」从未被填充——实际排序由 topology_scheduler + dependency_graph:622（活跃）承担，本模块拓扑排序是重复死实现**；**SC2 [P2] save_file_content 使 file_type 恒 "unknown" + depends_on 恒空（实测）：未注册文件建 FileArtifact(file_type="unknown", depends_on=[])（:193-200），主路径 spec_first_generate:613 直接 save_file_content 不传 file_type → 所有经该路径文件 file_type 全 unknown，而真实类型走 dependency_graph file_node.file_type（:341/:902）——同一文件类型双轨，shared_context 字段主路径从未真实填充**；**SC3 [P2] session_id 秒级冲突（实测）：datetime.now().strftime("%Y%m%d_%H%M%S")（:76）同秒多实例同 id，日志事件前缀/序列化标识无法区分（SM9 家族）**；SC4 [P3] GenerationPhase.files_generated 恒 0 死字段（start_phase 收 total 但 complete_phase 从不更新，实测 0/5）；SC5 [P3] get_all_specs_summary [:500]/get_generated_files_summary [:300] 截断无标记（JP2/TR2 家族）；SC6 [P3] to_export_dict 不含文件 content 与 docstring「完整导出」承诺不符（实测，spec_first_generate:837 用作 context_full）；SC7 [P3] update_file_review/get_phase_status/get_dependencies_for/get_dependents_of/get_generation_order 生产零消费（方法级未接线，GC6/SCT5 家族）；SC8 [P3] 新旧两代生成链部分重叠消费同一上下文（spec_first_generator v4.7 阶段/规范 API + spec_first_generate v4.8 文件/metrics API + cross_validator:103-104 只注入不消费）；零测试覆盖 | ✅ 已完成 | [modules/shared_context.md](modules/shared_context.md) |
| DependencyGraphValidator | app/agent/dependency_graph_validator.py | 344 | **DGV2 [P2] 缺失依赖边被 _build_context 过滤（实测，检测目标与数据矛盾）：:125-126 只收录 source in nodes and target in nodes 的边，指向不存在节点的边全被过滤，而系统 prompt :206 要求检测「缺失依赖：目标节点不在节点列表」——实测构造 a.py->nonexistent.py 后 edges 只剩 b.py->a.py、total_edges=1，LLM 永远看不到缺失依赖边，检测能力与输入数据自相矛盾**；**DGV1 [P2] LLM 验证失败全部兜底 passed=True（实测三路径）：(1) 非 JSON 响应 → _parse_response :307/:310 返回 passed=True；(2) issues:null → :313 迭代 None 抛 TypeError 被 validate :92-102 except 吞 → passed=True——LLM 已判 passed=false 的响应崩溃后反变通过；(3) 无 passed 键 :323 默认 True / 顶层标量 null 崩溃兜底 True——spec_first:231 if passed: break 直接跳重试，验证失效静默放行**；**DGV3 [P2] incremental/refactor 模式验证失败零反馈零修复（全库确认）：full 模式失败反馈架构师重新设计（:241-245），但 incremental :277-282 / refactor :2299-2303 只 logger.warning 继续生成——验证门禁在增量/重构路径形同虚设，功能重复/接口不匹配在增量修改场景完全不拦截**；DGV4 [P3] error_count/warning_count 白名单外 issue_type 计数丢失（__post_init__ :40-42 只认 4+3 类型，实测 made_up → error=0/warning=0）；DGV5 [P3] 验证结果不落 ctx/metrics/事件流，门禁状态零可观测（OP 家族）；DGV6 [P3] architecture=None 时空验证（full prompt 只剩「总数 0/0」）浪费一次 LLM 调用；零测试覆盖 | ✅ 已完成 | [modules/dependency_graph_validator.md](modules/dependency_graph_validator.md) |
| SignatureExtractor | app/agent/signature_extractor.py | 251 | **SE2 [P2] 签名文本超预算导致后续依赖全部被 break 丢弃（实测）：dependency_graph.py:841 `preview = signatures if signatures else content[:budget]` 签名存在时完全不按预算截断，:846 `remaining_budget -= len(preview) + 50`——实测核心依赖（priority<=2 分 60% 预算）签名 5298 字节、预算仅 1800 → remaining_budget 变负 → :847 break → 非核心依赖 b.py 完全丢失，注入上下文只剩 1 个依赖——「签名更紧凑」承诺在函数/类多时失效，反而不如退化路径 content[:budget]（至少受控）**；**SE1 [P2] 类方法体内带注解的局部变量被误判为类字段且缩进丢失（实测）：类体收集无「函数体内」状态跟踪，_is_class_field 对 indent > class_indent 的所有行（含方法体内缩进更深行）判字段，实测 def calc 内的 x: int = n + 1 / total: float = x * 2 全被当类字段注入，且固定 2 空格前缀使真实缩进（8 空格）丢失——注入上下文含「伪字段」污染 LLM 类结构认知**；SE3 [P3] .pyi stub 走冷门兜底路径字段全丢（SIGNATURE_PATTERNS 无 .pyi 键但 _is_class_field:217 支持 .pyi，字段逻辑不可达，实测 class User: id: int 仅输出 class User）；SE4 [P3] JS/TS 类方法签名完全不提取（function pattern 只匹配 function name(/const name = (，run(): void {} 语法不匹配，实测 export class Service 的 run/constructor 全丢——8 语言中唯一类方法语法不覆盖者）；SE5 [P3] 多行函数签名截断（单行内括号深度匹配，def long_func( 多行参数 提取为 def long_func( 参数全丢）；SE6 [P3] 嵌套类状态错乱（单一 current_class/class_indent 无栈，嵌套 Inner 覆盖后外层方法被当顶层函数）；SE7 [P3] 顶层签名/字段 [:200] 截断无标记（JP2/TR2 家族）；近零测试覆盖（test_small_model_optimization.py:188 仅断言 "config.py" in context 弱断言） | ✅ 已完成 | [modules/signature_extractor.md](modules/signature_extractor.md) |
| DependencyRules | app/agent/dependency_rules.py | 183 | **DR1 [P2] _infer_file_type 对嵌套目录全部漏配（实测）：dependency_graph.py:928 `path.startswith(pattern) or path.endswith(pattern)` 只对顶层目录/路径尾部生效——实测 app/api/users.py、backend/services/user_service.py、src/utils/helpers.py 全部落到 EXTENSION_TYPE_MAP → `.py` 不在 map → 兜底 'utils'，而 python adapter 用 `f"/{pattern}" in file_path`（python.py:268-269）能正确识别嵌套目录——同一份规则数据两消费方匹配语义不一致，主流行嵌套结构（app/src/backend 前缀）全部漏配**；**DR2 [P2] EXTENSION_TYPE_MAP 缺 .py 键 → Python 文件兜底恒 'utils'（实测）：未命中 PATH_TYPE_RULES 的 .py（顶层 main.py/app.py + DR1 嵌套漏配场景）全部落入 :939 `EXTENSION_TYPE_MAP.get(ext, 'utils')`，而 'utils' 在 DEPENDENCY_RULES 依赖 config/env（:27）——误判文件被注入错误依赖链，且与 python adapter 兜底 'unknown'（python.py:299）语义不一致**；**DR3 [P2] 三套 PATH_TYPE_RULES 并存且 schemas.py 类型冲突（全库确认）：dependency_rules.py:109 types vs python.py:112 schema vs javascript.py:96 schema——同一文件名推断出不同类型（types 依赖 config，schema 依赖 model+types），依赖注入链不同，三份手工复制无单一来源，升级即漂移（SCT6 双份配置家族）**；**DR4 [P2] _auto_add_dependencies 类型全连接爆炸（实测）：:559-566 对无 imports 文件按其 file_type 全连接 DEPENDENCY_RULES 目标类型的所有文件——实测 2 api + 3 service → api/a.py 依赖全部 3 个 service、总边 6（=2×3），test 依赖 model/service/api（:48）→ 测试文件连接全部业务文件，O(n×m) 边爆炸，get_context_for_file 依赖注入上下文被无关边污染**；DR5 [P3] 未知类型兜底 'utils' 语义错位（未知文件被当工具类注入 config/env 依赖，与 adapter 'unknown' 不一致）；DR6 [P3] endswith(pattern) 后缀宽松匹配误报（实测 my_config.py→config、my_utils.py→utils，PP5/FE1 子串家族）；DR7 [P3] view 系目录规则重叠歧义（views/→view 后端 vs src/views/→frontend_page 前端，路径前缀顺序决定结果）；DR8 [P3] 测试 37 用例全结构断言零消费语义（DR1/DR2/DR4 实测可复现但零用例保护，dict(PATH_TYPE_RULES) 转换掩盖顺序语义） | ✅ 已完成 | [modules/dependency_rules.md](modules/dependency_rules.md) |
| ErrorClassifier | app/agent/error_classifier.py | 196 | **EC1 [P2] 多错误拼接 + dict 顺序匹配使分类与实际错误顺序脱节（实测）：error_recovery:200 `"; ".join(_extract_error_messages(errors))` 拼接最多 7 类错误，_rule_based_classification 对整段拼接串按 ERROR_PATTERNS dict 顺序（NameError→...→LogicError）re.search 找第一个匹配——返回类型由 dict 遍历顺序决定而非实际首个错误，实测 "TypeError:...; NameError:..." → 返回 NameError（dict 中 NameError 排 TypeError 前），error_recovery:203-204 用 classification.error_type 查策略模板 → 类型误判直接导致修复策略选错**；**EC2 [P2] 规则覆盖缺口 + LogicError 规则中文不可达（实测）：模式要求精确格式，常见变体漏检（KeyError: 5 数字键不匹配 r"KeyError: '(\w+)'"、name x is not defined 无引号不匹配）→ 大量真实错误落入 LLM 兜底（每修复循环一次 LLM 调用），且 LogicError pattern 是中文（"逻辑错误"等）英文错误消息永不命中——8 类型规则实际 7 种有效**；**EC3 [P2] 模型分类失败全兜底 LogicError confidence=0.5（实测三路径）：缺字段 JSON → ErrorClassification(**result_dict) 抛 TypeError 被 except 吞 / 多 JSON 块 → re.search(r'\{.*\}', DOTALL) 贪婪跨块匹配 json.loads 抛 Extra data / 非 JSON 文本 → json_match None——全兜底 LogicError 且 fix_strategy 指向 deepseek-r1（与 DEFAULT_CODE_MODEL 不符），分类失败被伪装成「业务逻辑错误」误导修复（DGV1 家族镜像）**；**EC4 [P2] classification_history 只写不读死数据 + 全局单例无界增长（全库确认）：add_to_history 被 error_recovery:202 每修复循环调用但全库零读取方（rg 仅 :98/:192 两处），历史数据收集后从未用于决策，模块级全局单例 error_classifier 跨请求共享且 history 无上限增长（SM1/MCP1 家族）**；EC5 [P3] confidence 三轨不可比（规则固定 0.95 / 模型透传未校验 / 失败兜底 0.5）；EC6 [P3] 注释与实现不符（docstring 声称 qwen3.5-4b 实际 DEFAULT_CODE_MODEL=Nex-N2-Pro，LogicError 策略声称 deepseek-r1 不符）；EC7 [P3] 测试弱断言 + 模型路径零覆盖（test_classify_name_error 用 if hasattr(result,'error_type') 条件断言无属性即通过，9 用例全走规则路径，_model_based_classification 零用例，EC1/EC3 无保护）；EC8 [P3] get_fix_strategy_by_type 生产零消费死方法（仅测试引用，error_recovery 用 strategy_evaluator.get_strategy_template，GC6/SCT5 家族） | ✅ 已完成 | [modules/error_classifier.md](modules/error_classifier.md) |
| TestFrameworkConfig | app/agent/test_framework_config.py | 88 | **TFC1 [P2] setup_commands 全库零消费 → 非 Python 项目 Docker/本地测试依赖从未安装（实测链路）：6 预设都定义 setup_commands（pip/npm/mvn/go mod/cargo/make）但全库无任何代码读取该字段——docker_runner 安装依赖硬编码 pip install -r requirements.txt（:536-539），本地 test_runner _install_dependencies 也只对 requirements.txt 做 pip install，npm install/go mod download 从未执行；实测链路 orchestrator_testing:243 install_deps = req.exists() or pkg.exists()（package.json 存在即 True）→ docker_runner:531 条件含 requirements_path.exists() → pkg-only 项目无 requirements.txt 依赖安装被跳过 → 容器内 npm test 在无 node_modules 的 node 镜像必然失败**；TFC2 [P3] get_framework_config 生产零消费死函数（全库 13 处直接 FRAMEWORK_PRESETS dict 索引绕过函数，key 拼错运行时才暴露，DR8/EC8 家族）；TFC3 [P3] custom_args 字段定义零消费（dataclass :26 全库无读写，6 预设无一设置）；TFC4 [P3] 默认 output_format 三处硬编码重复（test_runner:742 与 orchestrator_testing:271 各写 pytest_xml + get_default_config :88，双份配置家族）；TFC5 [P3] 测试仅数量断言零字段级/链路覆盖（test_v4_8_features.py:137-140 只断言 len==6，TFC1 无用例保护，TR2 家族） | ✅ 已完成 | [modules/test_framework_config.md](modules/test_framework_config.md) |
| ProjectMetadata | app/agent/project_metadata.py | 194 | **PM1 [P2] _parse_feature_response JSON 解析路径三缺陷（实测）：(1) features: null → :156 for f in features 抛 TypeError 而 :157 只捕 JSONDecodeError → 功能清单提取静默失败（feature_extractor:35 吞掉返回 None）；(2) features 为 dict → 迭代 keys 后返回 [] 静默空无告警；(3) 多 JSON 块贪婪 \{[\s\S]*\} 跨块匹配（MAR5/EC3 同款）→ json.loads 失败走文本回退**；**PM2 [P2] 降级双路径污染（实测）：多 JSON 块时行级回退 :160-166 把 '{"features": ["f1"]} 补充 {...}' 整行当功能项输出；LLM 双模型失败时 _fallback_feature_list :168-178 用文件名 stem 生成「{filename} 模块」伪功能且无标记写入元数据 → count_with_features 计入 + layer2_semantic 把伪功能当真实历史功能匹配新项目（EC3/DGV1 家族）**；**PM3 [P2] _save 无锁非原子全量写 + 消费方每次实例化重复 load/mkdir（全库确认）：extract_and_save append 后 _save 全量 json.dump 无锁（CS1 读改写家族），并发生成多项目 last-write-wins 丢历史；feature_extractor:14 与 layer2_semantic 四处每次 new ProjectMetadataManager → :20 每次 mkdir+_load 重复 I/O 无单例缓存**；PM4 [P3] _summarize_files :144-145 截断 [:50] 文件 + [:200] 内容无截断标记（JP2/TR2 家族）；PM5 [P3] 模板萃取阈值双处硬编码（trigger_template_extraction 默认 min_projects=15 :181 vs feature_extractor:28 硬编码 15，TFC4 家族）；PM6 [P3] 测试仅 CRUD 零解析/降级覆盖（test_v5_1_requirement_deep.py:85-120 三用例，PM1/PM2 实测可复现零用例保护） | ✅ 已完成 | [modules/project_metadata.md](modules/project_metadata.md) |
| ComplexityAnalyzer | app/agent/complexity.py | 172 | **CMP1 [P2] 英文关键词子串假阳性（实测）：analyze 对 5 组关键词做 any(kw in req_lower) 子串匹配，短英文词命中普通单词——'ui' in 'build'/'guide'/'using' → 判有前端、'api' in 'rapid' → 判有后端，纯英文需求 has_frontend/has_backend 误置 → estimated_files 累加 + 等级跳升 + 下游架构约束/模型分配失真（BE1/FE1/PP8 子串家族）**；**CMP2 [P2] estimated_tokens/estimated_cost_usd 死字段 + 成本估算双轨（全库确认）：ComplexityAnalysis 计算返回 token/cost（:110-125）但全库零消费方（生产只取 level/has_*/risk 等），实际成本估算走 orchestrator_utils._estimate_generation_cost（:306）→ traditional_generate:95-109——同一复杂度两套独立成本估算，complexity 侧结果从未使用（CEC7/OU1 双轨家族 + OP1 恒零叠加）**；**CMP3 [P2] analyze_with_llm 死方法 + docstring 谎言（全库确认）：:169-172 全库零调用，docstring 声称「LLM 校准」实现直接 return cls.analyze，api_key_token 参数未用（SCT5/EC8 死方法家族 + EC6 注释不符家族）；mixin:50/evaluate_mixin:28 实例化无状态 classmethod**；CMP4 [P3] key_technologies 兜底硬编码 ['Python']（实测：Express/Node.js/Go 需求全部 techs=['Python'] 伪技术栈注入架构 prompt，与 LD/PP 冲突）；CMP5 [P3] docstring 注释与实现不符（:17-21 声称 SIMPLE「单文件脚本<50行」实际基础文件数恒 3 永不 SIMPLE 单文件；risk_factors 只是关键词复述无风险分析语义）；CMP6 [P3] 测试伪测试+弱断言（test_agent_capabilities.py:62-75 纯 print 无 assert；test_orchestrator.py:28-58 四用例多值放行，CMP1-CMP4 零用例保护） | ✅ 已完成 | [modules/complexity.md](modules/complexity.md) |
| TemplateExtractor | app/agent/template_extractor.py | 153 | **TE1 [P2] _save_template 用自动萃取结果覆盖手工模板（实测）：:144-150 模板已存在则备份为 {domain}_manual.json 再用自动模板覆盖 {domain}.json——Layer 1 消费路径恰是 {domain}.json（layer1_template.py:19），_manual 备份全库无消费方；实测预置手工 banking.json → _save_template(auto) 后主文件变自动萃取、手工版本降级为无人读取备份——10 个手工模板在领域项目数 ≥15 时被静默替换，「自动质量替换人工质量」信任模型缺陷**；**TE2 [P2] 审核 LLM 失败即丢弃萃取 + 审核标准与萃取标准矛盾（全库确认）：_review_template 异常返回 approved=False（:140）→ extract_template 返回 None 无降级路径；审核要求 core_modules >=5（:114）而萃取要求频率 >=40%（:57）——低频领域萃取结果必然被自己审核拒绝**；**TE3 [P2] _parse_template_response 贪婪跨块 + 无降级（实测）：\{[\s\S]*\} 对多 JSON 块跨块匹配失败（实测 {…} 附加 {…} → json.loads Extra data → None），解析失败即整条萃取链路失败（MAR5/EC3/PM1 家族）**；**TE4 [P2] 萃取输入 all_features[:200] 截断使频率统计失真（全库确认）：:36 只喂前 200 条功能但萃取要求按频率 >=40% 提取 core——频率基于子样本，与 15 项目 × [:20] feature 上限（project_metadata:178）不匹配（JP2/PM4 截断家族）**；TE5 [P3] 模型名硬编码 DEFAULT_REASONING_MODEL + 萃取/审核直连 call_llm（LCL1 家族）；TE6 [P3] _save_template 非原子写 + 手工备份只留最后一份；TE7 [P3] 测试仅 2 解析用例零流程覆盖（TE1-TE4 零用例保护） | ✅ 已完成 | [modules/template_extractor.md](modules/template_extractor.md) |
| orchestrator_requirements 子包 | app/agent/orchestrator_requirements/（10 模块 1036 行） | **OA1 [P2] requirement-association API 恒静默降级 skipped（实测）：association_endpoints:40-47 单独 RequirementAssociationMixin() 实例化，_association_pipeline 第一步 mixin.py:72 self._report_progress 需宿主提供而 mixin 自身不定义 → AttributeError → :56-61 捕获返回 skipped=True（实测复现），补 _report_progress 后 :101 self.architect 仍缺 → 再降级——全库唯一无宿主契约调用路径，测试 FakeMixin:39-40 手工补齐遮蔽**；**OA2 [P2] 反馈记录链路三端全断（实测）：association_endpoints:88 record_feedback 方法不存在 → confirm 500；:105 record_helpfulness(association_id, helpful) 签名需 3 参传 2 参 TypeError → helpfulness 500（补参后 int/bool 当 session_id/requirement 语义错位）；record_choice 全库零调用——联想项选择从未落库**；**OA3 [P2] parse_llm_response 双 JSON 贪婪跨块 + 降级污染（实测）：llm_prompts.py:55 \{[\s\S]*\} 跨块 → json.loads 失败 → 文本降级把整块 JSON 原文当功能项（两合法 JSON 拼接实测 → 2 个伪功能），且把 prompt 说明文字「你是架构顾问。」等当功能项（PM2/MAR5/EC3 家族）**；**OA8 [P2] _cleanup 超限删除量语义错位（全库确认）：feedback_tracker.py:102 db_size//4 是字节数当 LIMIT 行数 ≈524288 恒大于行数 → 超 2MB 首次 cleanup 删光全表；_cleanup 在 __init__:37 每次实例化执行，endpoint 每请求 new tracker 都跑 DELETE**；OA4 [P3] architect 缺失静默空无提示（layer3:27/devil:14 return []）；OA5 [P3] merge key content[:50] 判同项 + 置信度 0.5 地板/0.95 天花板硬编码；OA6 [P3] AssociationItem.devil_review 死字段（devil_advocate 独立列表不 merge 回 item，「反向审视」只展示不生效）；OA7 [P3] DEVILS_ADVOCATE_MODEL 硬编码 vs DUAL_MODEL_* 从 agent_model_config.json 加载（双轨家族）；OA9 [P3] Layer 2 门槛 MIN_HISTORY_PROJECTS=50 与模板萃取 15 不一致 + 静默降级 | ✅ 已完成 | [modules/orchestrator_requirements.md](modules/orchestrator_requirements.md) |
| TraditionalGenerate 生成链 | app/agent/orchestrator_generation/traditional_generate.py（427）+ feature_extractor.py（37）+ coverage_checker.py（61） | **TG1 [P2] _extract_feature_list prompt f-string 未转义花括号——功能清单提取恒 ValueError（实测）：project_metadata.py:99 f-string 内嵌 {"features": [...]} 模板（:107-108 裸 { 未转义 {{），每次调用必然 ValueError（空/非空 dict 均实测复现），prompt 构造在 try 块外（:99 vs try :122）不被捕获 → extract_and_save 中断不落库——传统链每轮收尾功能清单提取从未成功**；**TG2 [P2] feature_extractor 输入恒空（全库确认）：feature_extractor.py:17-21 从 generated_files 读 content/code 键，但 generated_files 六处 append 均只有 {"path","description","success","size"}（orchestrator_files.py:483/:825/:866 + traditional_generate.py:228/:258）→ files_dict 恒空——即使修 TG1，LLM 也只凭空 file_summary 提取，文件内容信息从未传入**；**TG3 [P2] 历史功能数据源恒空 + 模板萃取永不触发（实测，级联）：data/vector_index/project_metadata.json 不存在，结合 TG1 → 项目从不入库 → 模板萃取（≥15）与 Layer 2 联想（≥50）永不触发，TE 详档 TE1「手工模板被自动覆盖」触发前提实际不可达（上游缺陷阻断休眠）**；**TG4 [P2] 完整性检查 is_complete 忽略空文件（全库确认）：_validate_project_completeness_traditional:426 is_complete=missing==0 and invalid==0 不含 empty_files，且 :414 对 empty 文件跳过 invalid 检查——文件生成但内容为空仍判完成（TR1 家族）**；**TG5 [P2] 缓存审查闸门异常/缺评审即放行（全库确认）：_cache_review_gate:44-45 except 放行 + :30-31 reviewer 缺失放行，只判 risk_level=="high" 单维度——缓存架构审查任何异常或未配置审查员都直接命中复用（DGV1 放行家族）**；TG6 [P3] 缓存命中时联想增强需求与缓存架构错配（:55-56 覆盖 requirement 但 :60-63 用 cached.architecture）；TG7 [P3] 静态验证失败仍 success=True（:345 errors==0 and test_results 默认 True，静态验证失败不跑测试恒 True）；TG8 [P3] 完整性补充文件写盘后 generated_files_dict 未更新（沙箱验证用旧 dict）；TG9 [P3] 覆盖率关键词子串匹配无词边界 + 零联想项返回 coverage_rate 1.0 成功态谎报（BE1/FE1/PP8 + MAR8 家族）；TG10 [P3] 补缺失文件走 _direct_llm_generate_file 硬编码模型（OF4 消费点） | ✅ 已完成 | [modules/traditional_generate.md](modules/traditional_generate.md) |
## I.3 模块深扫轮次续报

> 第八十六轮（v1.88，2026-08-14）Models → 第八十七轮（v1.89，2026-08-14）MultiAngleReview → 第八十八轮（v1.90，2026-08-14）ArchitectureInspector → 第八十九轮（v1.91，2026-08-14）ServiceConfigTemplates → 第九十轮（v1.92，2026-08-14）PPTAgent → 第九十一轮（v1.93，2026-08-15）MultiLanguageParser → 第九十二轮（v1.94，2026-08-15）GlobalConstraintParser → 第九十三轮（v1.95，2026-08-15）GitOperations → 第九十四轮（v1.96，2026-08-15）SharedContext → 第九十五轮（v1.97，2026-08-15）DependencyGraphValidator → 第九十六轮（v1.98，2026-08-16）SignatureExtractor → 第九十七轮（v1.98，2026-08-16）DependencyRules → 第九十八轮（v1.99，2026-08-16）ErrorClassifier → 第九十九轮（v1.100，2026-08-16）TestFrameworkConfig → 第一百轮（v1.101，2026-08-16）ProjectMetadata → 第一百零一轮（v1.102，2026-08-16）ComplexityAnalyzer → 第一百零二轮（v1.103，2026-08-16）TemplateExtractor → 第一百零三轮（v1.104，2026-08-17）orchestrator_requirements 子包 → 第一百零四轮（v1.105，2026-08-17）TraditionalGenerate 生成链 → 第一百零五轮（v1.106，2026-08-17）EvaluationMixin+GenerationMixin+IncrementalGenerateMixin 合扫 → 第一百零六轮（v1.107，2026-08-17）LanguageAdapter 体系 → 第一百零七轮（v1.108，2026-08-17）spec_first_generate.py 重扫（v0.2）→ 第一百零八轮（v1.109，2026-08-17）docker_runner.py → 第一百零九轮（v1.110，2026-08-17）service_container_manager.py → 第一百一十轮（v1.111，2026-08-17）guardrails.py → 第一百一十一轮（v1.112，2026-08-17）agent_skills.py → 第一百一十二轮（v1.113，2026-08-17）guard_contracts.py → 第一百一十三轮（v1.114，2026-08-17）rate_limiter.py → 第一百一十四轮（v1.115，2026-08-17）agent_core.py → 第一百一十五轮（v1.116，2026-08-17）file_operator.py → 第一百一十六轮（v1.117，2026-08-17）crypto.py + encryption.py 加密双轨合扫 → 第一百一十七轮（v1.118，2026-08-17）process_guard.py 进程守护链合扫 → 第一百一十八轮（v1.119，2026-08-17）resource_guard.py + dynamic_concurrent.py + dynamic_chunker.py + system_load.py 资源与并发控制合扫 → 第一百一十九轮（v1.120，2026-08-17）cache.py + cache_decorator.py 缓存双轨合扫 → 第一百二十轮（v1.121，2026-08-17）web_search.py + web_search_enhancements.py 网络搜索链合扫 → 第一百二十一轮（v1.122，2026-08-17）json_parser.py + error_handler.py + error_codes.py 错误处理家族合扫 → 第一百二十二轮（v1.123，2026-08-17）security.py + security_audit.py + csrf.py + api_response.py API 安全家族合扫 → 第一百二十三轮（v1.124，2026-08-17）permissions.py + system_config.py + system_monitor.py 权限与配置合扫 → 第一百二十四轮（v1.125，2026-08-17）logging.py + structured_logging.py 日志双轨合扫 → 第一百二十五轮（v1.126，2026-08-17）http_client.py + retry.py + circuit_breaker.py 网络容错家族合扫 → 第一百二十六轮（v1.127，2026-08-17）sentry.py + startup_alert.py 监控告警家族合扫 → 第一百二十七轮（v1.128，2026-08-17）performance_monitor.py + performance_metrics.py 性能监控家族合扫 → 第一百二十八轮（v1.129，2026-08-17）task_manager.py + task_dispatcher.py + resume_manager.py 任务调度家族合扫 → 第一百二十九轮（v1.130，2026-08-17）aicloud/llm_caller.py + prompt_builder.py + prompt_loader.py LLM 家族合扫 → 第一百三十轮（v1.131，2026-08-17）log_archiver.py + pagination.py + math_utils.py 工具件家族合扫 → 第一百三十一轮（v1.132，2026-08-17）vision.py + image_generation.py 视觉家族合扫 → 第一百三十二轮（v1.133，2026-08-17）hot_reload.py + dynamic_package_manager.py + project_validator.py 开发工具家族合扫 → 第一百三十三轮（v1.134，2026-08-17）AiCodeUtil.py 收官（app/utils 顶层全部建档） → 第一百三十四轮（v1.135，2026-08-17）workflow 核心执行链首扫（executor+state_machine+graph_validator+API 消费侧） → 第一百三十五轮（v1.136，2026-08-17）workflow 聚合与分解合扫（result_aggregator+task_decomposer） → 第一百三十六轮（v1.137，2026-08-17）workflow node_types/ 10 节点合扫（base+web_search+llm_call+http_request+code_execution+conditional+file_processing+human_approval+data_transform+chart_generation）。

> 补扫汇总：编排层补扫 8 文件 + 错误分析决策链 3 文件 + DependencyGraph 1 文件 + CodeValidator 1 文件 + TestRunner 1 文件 + IntegrityValidator 1 文件 + APIContractChecker 1 文件 + RefinementLoop 1 文件 + OrchestratorFiles 1 文件 + OrchestratorProgress 1 文件 + Tracing 1 文件 + FrameworkDetector 1 文件 + OutputParser 1 文件 + CodePatcher 1 文件 + OrchestratorUtils 1 文件 + OrchestratorTesting 1 文件 + Orchestrator 1 文件 + SessionManager 1 文件 + Architect 1 文件 + ImpactAnalyzer 1 文件 + TestSelector 1 文件 + FailureClusterer 1 文件 + ProjectProfiler 1 文件 + Utils 1 文件 + BackendEngineer 1 文件 + FrontendEngineer 1 文件 + LanguageDetector 1 文件 + SpecCache 1 文件 + StrategyEvaluator 1 文件 + ReviewChain（code_reviewer/ai_reviewer/multi_angle_review） + FeedbackLearner 1 文件 + CrossValidator（重扫）1 文件 + JsonParser 1 文件 + Memory 1 文件 + ConversationStore 1 文件 + CloudLearningHub 1 文件 + ArchitectJsonParser 1 文件 + AgentMemory（models+services）1 文件 + DynamicModelRouter 1 文件 + FixPatternCache 1 文件 + VectorIndex 1 文件 + StrategyLearner 1 文件 + UserPreferenceLearner 1 文件 + CriticalDecisionExtractor 1 文件 + TopologyScheduler 1 文件 + AgentExecutor 1 文件 + ShadowScanner 1 文件 + ConsistencyChecker 1 文件 + FileContract 1 文件 + TaskPlanner 1 文件 + MultiModelAgent 1 文件 + AIReviewer 1 文件 + Models 1 文件 + MultiAngleReview 1 文件 + ArchitectureInspector 1 文件 + ServiceConfigTemplates 1 文件 + PPTAgent 1 文件 + MultiLanguageParser 1 文件 + GlobalConstraintParser 1 文件 + GitOperations+SnapshotManager 1 文件 + SharedContext 1 文件 + DependencyGraphValidator 1 文件 + SignatureExtractor 1 文件 + DependencyRules 1 文件 + ErrorClassifier 1 文件 + TestFrameworkConfig 1 文件 + ProjectMetadata 1 文件 + ComplexityAnalyzer 1 文件 + TemplateExtractor 1 文件 + orchestrator_requirements 子包 1 文件 + TraditionalGenerate 生成链 1 文件 + 评价/组装/会话增量 1 文件 + LanguageAdapter 体系 1 文件 + spec_first_generate 重扫 1 文件 + DockerRunner 1 文件 + ServiceContainerManager 1 文件 + Guardrails 1 文件 + AgentSkills 1 文件 + GuardContracts 1 文件 + RateLimiter 1 文件 + AgentCore 1 文件 + FileOperator 1 文件 + 加密双轨合扫 1 文件 + 进程守护链合扫 1 文件 + 资源与并发控制合扫 1 文件 + 缓存双轨合扫 1 文件 + 网络搜索链合扫 1 文件 + 错误处理家族合扫 1 文件 + API 安全家族合扫 1 文件 + 权限与配置合扫 1 文件 + 日志双轨合扫 1 文件 + 网络容错家族合扫 1 文件 + 监控告警家族合扫 1 文件 + 性能监控家族合扫 1 文件 + 任务调度家族合扫 1 文件 + LLM 家族合扫 1 文件 + 工具件家族合扫 1 文件 + 视觉家族合扫 1 文件 + 开发工具家族合扫 1 文件 + AiCodeUtil 收官 1 文件 + workflow 核心执行链首扫 1 文件 + workflow 聚合与分解合扫 1 文件 + workflow node_types/ 10 节点合扫 1 文件，新增 P1 4 项（IM1 + SCT1 + SCT5 + CA12）、P2 352 项、P3 518 项。app/utils 顶层全部模块建档完成，workflow/ 子包（核心五文件 executor/state_machine/graph_validator/result_aggregator/task_decomposer + node_types/ 10 节点）全部建档完成，下一阶段转 aicloud/ 子包（21 文件）。两套增量实现（IncrementalModify 依赖图驱动 vs IncrementalGenerate 会话驱动）与两套错误恢复（顶层 ErrorRecoveryLoop vs 子包 ErrorRecoveryMixin）是演化收敛重点；strategy_evaluator A/B 框架接线是 Evaluator-optimizer 方向（EVOLUTION.md §5.3）的前提；dependency_graph 完整性三方法（DG3）修复即激活架构补缺主线；code_validator 四套验证器归位（CV8）与验证执行任意代码（CV2）是验证闭环归位前提；test_runner 是验证栈的运行执行器，TR1「无测试文件=通过」与 TG2/IM2 同属「存在≠正确」验证语义主线；integrity_validator 与 DependencyGraph 的补缺闭环**双断**（IV1 + DG3）；api_contract_checker 与 integrity_validator._validate_api_contracts、cross_validator CV4 构成三套 API 契约校验并存；refinement_loop 是修复循环执行器，RL1 空操作 + RL3 轻量成功使修复循环验证端只有语法级；orchestrator_files 是文件生成编排核心（spec_first 与传统链路共同文件出口），OF1 使 CodeValidator 完整验证绕开（CV8 五处并存），OF2 回滚与写安全主线相关，OF10 跨文件补丁方向缺接线；orchestrator_progress 是全部生成链路的事件出口与成本追踪，OP1 使成本金额全链路恒零（token 计数正确但 USD 永远 0），OP2 使进度通道与验证通道 content 截断语义不一致；tracing 是 12 模块公共追踪依赖，TT1 使单点环境变量误设全系统启动失败，TT2 启用路径零验证，OTel 集成可观测性能力默认部署下未接线；framework_detector 是测试执行栈的语言/框架决策上游，FD1/FD2 使 JS 系项目输出格式解析失真、FD9 揭示测试假阳性（fallback 兜底让「检测」用例恒过）；output_parser 是测试结果展示链路，OP1-OP4 使多语言测试结果解析失真（真 XML 全 0 / skipped 虚高 / vitest 全 0 / Go panic 漏报），OP3 与 FD1 闭环构成 JS 系测试判定失效链；code_patcher 是增量修改的补丁应用器，CP1 使 LLM 幻觉补丁静默破坏代码、CP12 多 hunk 漂移、CP10 跨文件补丁整链路不可达（OF10 死代码下游）；cross_validator 是 spec_first 链跨文件语义验证层，CV1 使普通文件被双模型交叉验证（成本×3），CV2/CV3 假阳性喂给 LLM 产生幻觉修复（CP1 输入端）；language_detector 是全库生成链入口决策，LD1/LD2 漏检→错误语言→错误代码→UT5 验证空转不拦截（「存在≠正确」检测端+验证端双失效叠加）；spec_cache 使成本「省」侧失效（SC1 重启全丢 + SC3 Jaccard 不达），与 CV1「贵」侧放大两头挤压；strategy_evaluator 与 feedback_learner 成对构成反馈闭环两端缺陷（SE1 评估无输入 + FL1 记录拦截误伤）；review_chain 是唯一两套 LLM 契约并存的横切层（CR1），CR2 假阳性使拦截侧过严；service_config_templates 是「能力未接线」家族第八例（SCT5 6/7 公开函数零生产消费方），且模板数据自身有缺陷（SCT1 depends_on Python repr / SCT2 关键词误报 / SCT3 缺 import os）——接线与修缺陷需同步，否则「接线即崩」，SERVICE_TEMPLATES 与 SERVICE_CONTAINER_CONFIGS 双份配置是收敛对象；ppt_agent 是 PPT 附属生成链路，页数语义正常/降级两路径都不遵守请求（PPT2 少页不补 + PPT4 fallback 边界不符），模型名/LLM 调用双份实现未收敛到统一 LLM 层（PPT5 LCL1 家族），且 AJP1 对 ppt_agent「崩溃」断言过时（PPT3 实测静默降级，文档需修正）；multi_language_parser 是「被取代型孤儿」的极端双轨（生产正主 LanguageAdapter 体系 5 大消费族全量接线，本模块全 app/ 零 import 仅测试引用，删除候选待执行），其 597 行测试全绿但断言强度系统性不足（子集断言掩盖 Go 块级垃圾条目 / len>=1 掩盖 Java static 取错组 / 字符串用例规避行首 import 真实场景），证明「测试全绿 ≠ 解析正确」；global_constraint 是 spec_first 链「用户约束 → 生成 prompt」的唯一通道，注入端最严重缺陷是 GC2（compatibility/security 提取成功却在全量注入被丢弃，安全约束从未影响生成——「提取≠生效」家族，与 CD1 决策注入主线同族），提取端单句多约束丢失（GC1）+ 高频词误报（GC3）+ 性能类不可达（GC4）使约束质量整体失真，GC6 的 merge_with_decisions 是现成决策注入通道接线即补 CD1 缺口，与 AI2 构成全局约束「提取→注入→检查」三端两失效链路；git_operations 是 Agent 项目快照系统（保存/回滚/合并三能力），保存侧 --allow-empty 使空提交常态化（GO1「无变更跳过」死分支，每次生成结束都产空提交+tag）、回滚侧删除当前分支永远失败（GO2）+ current_tag 谎报 main / files_restored 恒空（GO7）+ reset --hard 无备份（GO12）——**「报告成功」与「实际结果」分离**的又一实证，回滚端点（外部暴露）行为与用户预期严重不符，finalize_session（会话结束合并）零调用（GO8，能力未接线方法级），且与 orchestrator_utils 裸 git 构成**两套 git 快照实现并存**（双份 --allow-empty，收敛对象）；shared_context 是 spec-first 链两代生成器（spec_first_generator v4.7 + spec_first_generate v4.8）的公共状态容器，**文件/依赖子系统核心数据结构未被主路径正确填充**——依赖管理整条死链（SC1：register_file 零调用 → dependencies 恒空，实际排序由 topology_scheduler + dependency_graph:622 承担，本模块拓扑排序是重复死实现）、file_type 恒 unknown 而真实类型在 dependency_graph 节点（SC2，同一文件类型双轨）、session_id 秒级冲突（SC3，SM9 家族）——「同一能力双实现」在 spec-first 链内部（shared_context 轻量旧容器 vs dependency_graph 重型新模型）再现，同时跨模块（GitOperations vs orchestrator_utils 裸 git）也存在，是 EVOLUTION §5.6 支柱 1 协议统一在状态容器层的收敛对象；dependency_graph_validator 是 spec-first 链「架构设计 → 文件计划」的依赖图验证门禁（full 重试反馈 + incremental + refactor 三模式），**验证器自身的失效语义比拦截能力更严重**——LLM 验证失败（非 JSON/issues:null/缺 passed/顶层标量）全部兜底 passed=True（DGV1 实测三路径，且 spec_first:231 见 passed 即 break 跳重试，验证失效静默放行），且缺失依赖检测目标在 _build_context 数据构建阶段就被过滤（DGV2 实测 a.py->nonexistent 边消失，:125-126 只收两端都在 nodes 的边，与系统 prompt :206「缺失依赖」要求自相矛盾），incremental/refactor 两副路径验证失败零反馈零修复（DGV3）——**AI1/AI2 架构检查门禁失效之后，第二道依赖图验证门禁的失效路径同样指向 passed 恒真**。signature_extractor 是 dependency_graph 依赖上下文注入的信息压缩层（唯一消费方 dependency_graph.py:840），**压缩层「存在≠正确」**——签名存在时完全不按预算截断（SE2 实测核心签名 5298 字节溢出 60% 预算后 remaining_budget 变负 break，非核心依赖全丢，「更紧凑」承诺反而绕过预算机制），类方法体内带注解局部变量被误判为类字段且缩进丢失（SE1 实测，注入「伪字段」污染类结构认知），.pyi/JS-TS 类方法/多行签名/嵌套类四类提取缺口（SE3-SE6）——与 DG7（预算恒 32768 兜底）叠加后依赖上下文注入内容完全不可预测，是「依赖注入质量」主线的信息压缩端实例。dependency_rules 是依赖图「文件类型→依赖类型」的规则语义层，**三张规则表结构与消费语义脱节**——PATH_TYPE_RULES 仅无 language_adapter 时生效（生产主路径 adapter 自带副本，本模块规则表大部分处「降级影子」状态，且三套副本 schemas.py 类型冲突，DR3），其唯一活跃消费是 _infer_file_type 的 startswith/endswith 匹配（嵌套目录全漏配 DR1 + endswith 后缀误报 DR6）与 _auto_add_dependencies 的类型级全连接（O(n×m) 边爆炸 DR4），EXTENSION_TYPE_MAP 缺 .py 使 Python 主语言兜底恒 utils（DR2/DR5）——「数据存在但消费语义失真」家族在规则层的实例，修复方向是统一匹配语义（采用 adapter 的子串目录匹配）+ 单一规则数据源（三副本收敛，§5.6 支柱 1）+ 兜底语义对齐。error_classifier 是错误恢复链「错误消息 → 修复策略」的策略选择输入端（唯一消费方 error_recovery:201-204 用 classification.error_type 查策略模板），**输入端双轨均失真**——规则路径对多错误拼接串按 dict 顺序匹配（EC1 实测 TypeError;NameError → NameError，与实际首个错误脱节，策略模板选错）+ 精确格式模式变体漏检 + LogicError 中文规则英文消息不可达（EC2，大量错误落 LLM 兜底）；模型路径三类失败（缺字段/多 JSON 块/非 JSON）全兜底 LogicError confidence=0.5 且 fix_strategy 指向与 DEFAULT_CODE_MODEL 不符的 deepseek-r1（EC3，分类失败被伪装成「业务逻辑错误」误导修复，DGV1 家族镜像）；classification_history 只写不读 + 全局单例无界增长（EC4）——与 RL 详档「修复循环验证端只有语法级」叠加，错误恢复链从分类输入端到验证端两端失真。test_framework_config 是测试栈的框架预设数据源（framework_detector detect 结果 / docker_runner / test_runner / orchestrator_testing 四消费方），预设数据完整（6 框架、与 output_parser 6 解析器一一对应）但**消费端只用了字段子集**——setup_commands（npm install/go mod download 等）全库零消费且 docker_runner 安装依赖硬编码 pip install -r requirements.txt（TFC1 实测链路：pkg-only 项目无 requirements.txt 依赖安装被跳过 → 容器内 npm test 在无 node_modules 的 node 镜像必然失败），非 Python 项目测试链路依赖安装整条缺失——「预设数据存在但安装命令从未执行」使非 Python 测试在「无依赖环境」下运行，与 TR1/CV2「存在≠正确」验证语义同族。project_metadata 是 Layer 2 历史匹配的数据源（feature_extractor 传统链 extract_and_save + layer2_semantic 语义/关键词链路消费），**提取端与持久化端双失真**——LLM 功能清单解析对格式偏移零容错（PM1 features:null TypeError 逃逸/非 list 静默空/多 JSON 块贪婪跨块全实测），降级路径产出伪数据（PM2 文本回退把 JSON 原文当功能项 + 文件名 stem 伪功能无标记写入且 count_with_features 计入 → layer2 把伪功能当真关联新项目，「失败兜底产生伪数据」家族 EC3/DGV1 同族），持久化端 _save 无锁非原子全量写（PM3 CS1 家族）——**「已接线但输入失真」的活跃模块代表，与「能力未接线」孤儿家族（SCT5/EC8）方向相反，警示接线 ≠ 正确**。complexity 是生成链「需求 → 架构」第一道量化输入（mixin/evaluate_mixin 双份 analyze + orchestrate_endpoints:1158 用 level 查模型分配 + orchestrator_utils cost 表输入），**规模决策层失真的源头在关键词子串匹配**——CMP1 英文短词假阳性（'ui' in build/guide/using、'api' in rapid 全实测）使纯英文需求误判前后端 → estimated_files 累加 + 等级跳升 + 下游架构约束/模型分配全链失真（BE1/FE1/PP8 子串家族第 N 例）；CMP2 成本估算双轨（complexity 的 token/cost 死字段零消费 vs orchestrator_utils._estimate_generation_cost 实际使用，CEC7/OU1 家族 + OP1 恒零叠加）；CMP3 analyze_with_llm 死方法 docstring 声称 LLM 校准实则纯关键词（SCT5 家族）；CMP4 无技术栈命中时兜底硬编码 ['Python'] 伪技术栈注入架构 prompt——规模预判是「关键词质量 → 全链路成本/模型/架构」的杠杆点，与 language_detector（LD）同属生成链入口决策。template_extractor 是领域模板数据源的**唯一写端**（feature_extractor:28 ≥15 项目自动触发 → project_metadata:193 → extract_template → _save_template 覆盖 {domain}.json，Layer 1 消费恰是此路径），**「自动质量替换人工质量」的数据源覆写缺陷**——TE1 实测：手工 banking.json 被自动萃取模板覆盖、手工版本降级为全库无消费方的 _manual 备份（与 PM3/CP1/GO2 同属写入侧破坏已有数据，但 TE1 是自动生成内容覆写人工精修内容，信任模型缺陷）；TE2 审核 LLM 失败即丢弃 + 审核标准（core>=5）与萃取标准（频率>=40%）自相矛盾致低频领域必然被拒；TE3 贪婪跨块解析失败即整链失败（MAR5 家族第 N 处）；TE4 输入截断 [:200] 使频率统计失真——领域模板「萃取→审核→入库→Layer1 消费」四环写端失真，修复方向是保护手工模板（自动模板独立路径或人工确认）+ 标准对齐。orchestrator_requirements 子包是需求联想的四层流水线（领域模板 Layer 1 → 历史语义 Layer 2 → 双模型深度联想 Layer 3 → 魔鬼代言人反向审视）与反馈追踪数据源，**最严重缺陷在对外契约断裂**——OA1 实测：独立 API endpoint 单独 `RequirementAssociationMixin()` 实例化，因 mixin 自身不定义 `_report_progress`/`architect`（宿主契约未声明）第一步即 AttributeError → 恒静默降级 skipped=True，「需求联想 API 永远返回空」，测试 FakeMixin 手工补齐遮蔽（TR2 家族：测试夹具掩盖契约缺口）；OA2 实测：反馈链路三端全断（record_feedback 方法不存在 confirm 500 / record_helpfulness 签名错位 helpfulness 500 / record_choice 全库零调用，「联想项选择反馈从未落库」）；OA3 实测：parse_llm_response 贪婪跨块 + 文本降级把整块 JSON 与 prompt 说明当功能项（PM2/MAR5 家族第 N 处）；OA8 全库确认：_cleanup 超限 LIMIT 用字节数当行数 → 超 2MB 删光全表且每请求实例化都跑 DELETE——「已接线但契约缺失」与「清理逻辑删空保留数据」是本轮两条主线，修复方向是宿主契约显式化（endpoint 复用 OrchestratorAgent 或注入存根）+ 反馈方法签名统一 + JSON 边界解析 + 清理 LIMIT 按行数。traditional_generate 是传统生成链主编排（缓存→联想增强→架构→成本审批→三路文件生成→完整性/静态/动态验证→功能清单提取→覆盖率检查→快照），**最严重缺陷在收尾功能清单提取链「输入侧与 prompt 侧双断」**——TG1 实测：project_metadata.py:99 `_extract_feature_list` 的 prompt f-string 内嵌 `{"features": [...]}` 未转义花括号，每次调用恒 ValueError（空/非空 dict 均复现），且 prompt 构造在 try 块外不被捕获 → `extract_and_save` 中断不落库，传统链每轮收尾的功能清单提取从未成功；TG2 全库确认：feature_extractor.py:17-21 从 generated_files 读 content/code 键而六处 append 均无该键 → files_dict 恒空——即使修 TG1 也凭空 file_summary 提取；TG3 实测级联：`data/vector_index/project_metadata.json` 恒空 → 模板萃取（≥15）与 Layer 2 联想（≥50）永不触发，**TE 详档 TE1「手工模板被自动萃取覆盖」触发前提被 TG1 前置阻断而休眠**（「上游阻断式休眠」模式，跨详档反向修正）；TG4 全库确认：完整性检查 is_complete 忽略 empty_files（空文件=完成，TR1 家族）；TG5 全库确认：缓存审查闸门异常/缺评审即放行（DGV1 家族）。修复方向是转义 f-string（一处改动激活历史数据写入链）+ feature_extractor 改从 output_dir 按 path 读文件 + 数据源激活后复核 TE1 真实触发风险 + is_complete 纳入 empty_files。评价/组装/会话增量三模块是 orchestrator_generation 子包收尾（评价模式 + 生成组合层 + 会话增量），**评价模式三维评价中两维失真**——EV1 全库确认：`_evaluate_risks` 用 `architecture.get("has_backend"/"has_database")` 而架构返回键集合（architect.py:537-550）无此二键 → 恒 None → missing_api/missing_db_schema 两个 high 风险恒死（实测 fullstack+无 api_spec 风险列表空、overall low），且 test_evaluate_mode.py:78/:92/:104 手工注入此二键让恒死规则「全绿」（TR2 家族：测试夹具补的恰是生产缺失的键）；EV2 实测：`_parse_evaluation_json` 校验键（score/completeness）与架构 prompt 返回键（architecture_quality 等六键）不匹配 → 架构评价恒降级 fallback score:0 → overall_assessment 只反映需求评价一半，「架构可行性评价」整体失效（LLM 契约漂移家族 CR1/OA1 同族）；EV3 全库确认：evaluate 模式 models_used 无 None 防护（use_dynamic_topology=False 时 AttributeError，:43 同处有防护对比）——**评价模式「已接线但两维失真」，与 traditional_generate「接线 ≠ 正确」主线汇合，修复方向是键名对齐 + 风险来源改用 complexity 真实字段 + 防护补齐**。orchestrator_generation 顶层（traditional_generate/mixin/evaluate_mixin/incremental_generate/coverage_checker/feature_extractor）至此全模块建档闭环。language_adapters 是语言适配层（Python/JS/Generic 三适配器 + 注册表），multi_language_parser 详档的「生产正主」体系首次建档，**缺陷集中在「项目模块判定」与「导入解析精度」**——AD1 实测：Python `is_project_module` 用前缀白名单 ['app','src','lib','pkg','internal','core']，真实项目根包名（banking_system/myapp）不在白名单恒判外部（IV5 消费侧症状的实现侧根因，依赖边丢失）；AD2 实测：JS 侧无任何白名单等价机制，项目内绝对路径导入（models/user）恒判外部且 resolve_import_to_file 返回 []（AD9，依赖边完全丢失）；AD11 实测：Python 相对导入 `from ..models` 层级被 lstrip 丢弃，在 app/api/users.py 解析到 app/api/models.py 而非回溯上层（依赖边错配）；AD12 全库确认：Generic `_file_plan_data` 是类属性 + 注册表单例（generic.py:416），dependency_graph:181 每次 build 覆写 → 多项目互相污染（SM1/MCP1 全局单例家族）——**「检测端失真」主线（LD 同族）在 is_project_module 判定层的实例，修复方向是「排除法」（非 stdlib/第三方即项目内）替代白名单 + JS 补齐对称判定 + 相对导入保留层级 + 实例级 file_plan 缓存**。spec_first_generate v0.2 重扫（第一百零七轮）：2383 行核心编排（orchestrator_generation 子包最大文件）首扫建档 10 个月后复核，**SPFG1-SPFG10 全部仍在**，新增发现集中在动态拓扑分支（默认路径 use_dynamic_topology=True）的**删除性收尾逻辑**——SPFG11 全库确认：:1202-1240「清理不符合项目语言的文件」按 `expected_extensions = 适配器扩展名 ∪ file_plan 扩展名` 判定，LLM 生成但 file_plan 未规划扩展名的 .py/.js 系文件被 `full_path.unlink()` 物理删除且普通分支无此清理（双分支行为不一致，生成产物静默丢失）；SPFG12 全库确认：:1279-1300 根目录 vs src/ 同名删根目录 + :1302-1340 功能同名跨目录按「src/>app/>src/app/>根」优先级保留一个删其余——**只看文件名不看内容，Django 根 manage.py 等合法文件被误删**（GO2「启发式删除破坏已有数据」家族在文件生成收尾的实例）；SPFG13 全库确认：:2151 is_complete 不含 empty_files（TG4 同族在 spec-first 链复现，两链语义一致地忽略空文件）；SPFG14 全库确认：:933 动态拓扑断点续传跳过文件直接 `update_file_validation(True,[])` 标记验证通过（SPFG1 的验证端放行细节）。修复方向是删除前校验 file_plan/依赖图 + 移入备份目录或人工确认 + is_complete 纳入 empty_files + 跳过前语法/占位符校验。P3 4 项：SPFG15 文件名 rename 不更新 dep_graph；SPFG16 贪婪跨块；SPFG17 四处直连 call_llm；SPFG18 硬编码 Qwen/Qwen3-8B + glm-z1-9b。spec_first_generate 至此为 orchestrator_generation 子包最后一个建档模块（重扫闭环）。


---

## A. Agent 引擎（15 子系统，已推演）

- **大系统演化文件**：[AGENT-EVOLUTION.md](AGENT-EVOLUTION.md)
- **子系统演化文件**：
  - 批1 主链路：[AGENT-EVOLUTION-BATCH1.md](AGENT-EVOLUTION-BATCH1.md)（A1 编排核心 / A2 生成路径 / A3 需求分析）
  - 批2 能力层：[AGENT-EVOLUTION-BATCH2.md](AGENT-EVOLUTION-BATCH2.md)（A4 上下文压缩 / A5 角色体系 / A6 模型路由 / A7 验证修复）
  - 批3 支撑层：[AGENT-EVOLUTION-BATCH3.md](AGENT-EVOLUTION-BATCH3.md)（A8-A15）
  - 专项：[AGENT-CONTEXT-COMPRESSION.md](AGENT-CONTEXT-COMPRESSION.md)（A4 压缩）、[AGENT-ENGINE.md](AGENT-ENGINE.md)（大系统总演化）
- **具体演化路径**：见 AGENT-EVOLUTION.md 各子系统小节（A1-A15）
- **实测复核（2026-08-05，AGENT-ENGINE v1.16 §9.1 复测 + 栈并存发现）**：**§9.1 P0 Bug 复测通过（仍在）**——§9.4 全组 8 文件 **57 passed, 2 failed 精确**，路径越界复现（test_agent.py:102，不在项目根目录 '.' 下）；根因结构仍在（ToolRegistry 单例 executor.py:86-93 + 短路 :152-153 + 闭包捕获 :205/:212）；test_executor 17 passed 因不覆盖多实例场景，暴露点在 test_agent.py。**三维并存新发现**：两套 LLM（§10.1）+ 两套 ReAct 循环（ReActEngine max_rounds 3/6 vs ReActAgent max_iterations 5/10）+ 两条工具注册表（ToolRegistry 单例 executor.py:83 vs SPECIALIST_TOOLS tools.py:1184）；ReActAgent 使用方仅 error_recovery.py:19，经 app.utils.call_llm（llm_caller.py:179）直连顶层体系。**§9.1 影响面扩展**：ReActAgent 工具表来自 executor.tool_registry（react_agent.py:131-138），单例 bug 使修复闭环工具写入也落错目录。Backlog 增补 #11（§9.1 修复须覆盖 ReActAgent 消费路径）/ #12（两套 ReAct 循环收敛到 ReActEngine）。
- **实测复核（2026-08-05，AGENT-ENGINE v1.15 未来方向+结构发现）**：**§11 两处修正**——① `_tool_search_files` 返回 `{"success": True, "matches": [...]}` **无 `results` 键**（tools.py:830-834），react_engine:504 `result.get("results")` 对**所有工具**恒 0（连搜索工具也失真，原「仅搜索工具适用」不准确）；② 内置工具返回结构两式：read 系列（业务数据/error）与 search_files（success/业务数据/error）。**spec_first 双文件澄清**：`orchestrator_generation/spec_first_generate.py`（2383 行，SpecFirstGenerateMixin 编排）vs 顶层 `spec_first_generator.py`（536 行，SpecFirstGenerator 核心生成器，refinement_loop:521/incremental_modify:242 实例化），非近似重复；v1.10「spec_first:2003」实测指向子包文件精确。**未来方向现状**：§5.1 学习闭环四组件工厂零生产调用（纯孤岛）；§4.6 历史项目接入已有单文件粒度 `skipping_existing_file` 机制（spec_first_generate.py:351-378）；§6.1 角色硬编码/specialists.py 纯导出；§6.3 动态工具无实现。**正面确认**：MCP 闭包 `_mcp_tool_fn(project_path="", _sn, _tn, **kwargs)`（mcp_client.py:372）显式兼容 project_path 注入。Backlog 增补 #9/#10（全工具 results 恒 0、search_files success 结构并入统一 Schema）。
- **实测复核（2026-08-05，AGENT-ENGINE v1.14 运行验证链路复核，回应「云端 docker 运行验证是否舍弃」）**：**决策层与代码层区分**——§2.1「舍弃云端真实验证」是决策定案的未来收敛方向，当前代码尚未收敛：`orchestrator_testing.py:35` 仍调 `_run_tests_in_docker`（docker 优先、fallback 本地 TestRunner），`docker_runner.py`（802 行，app/utils/，非 app/agent/ 分析范围）仍活跃非孤儿。**修正 2 处断言**：① `_run_tests_in_docker` 调用行实测 **:35**（v1.8 改 :36 为回归错误）；② §2.1「test_runner.py:237 执行 mvn verify」定位修正——test_runner.py 全文件无 mvn 字样，`mvn verify` 定义于 `test_framework_config.py:49`，经 `_build_test_command`（test_runner.py:241）组装，非 Python 分支（:237）在宿主原目录执行。复核通过：docker_runner 3 生产使用方（orchestrator_testing:213/code_tasks:102/project_tasks:88）、`_run_tests_in_docker` 定义 :211、BATCH3 已正确标注 app/utils/ 路径。
- **实测复核（2026-08-05，AGENT-ENGINE v1.13 工具契约推演）**：§11 新增——**ReAct 工具成功判定与返回契约脱节**：`_execute_tool`（react_engine.py:202-225）以「是否抛异常」判定 success（:215 正常返回即 True），不看返回内容；内置读工具业务失败 `{"error":...}`（tools.py:167）、MCP 工具 `{"success":False}`（mcp_client.py:383）均被判定成功（仅 MCPError 异常终止）。**三套工具返回结构并存**：内置读（业务数据/error）、内置写（success/error，tools.py:369）、MCP（success/result）。失真点：react_engine:504 `result.get("results")` 仅搜索工具适用、specialist_base:273 写追踪依赖 `result.success` 隐式契约。Backlog 增 3 项（统一工具返回 Schema #6 / 统计失真 #7 / 隐式契约显式化 #8）。正面确认：`_mcp_tool_fn` 默认参数捕获规避 late binding、MCP 前缀规避重名、工具合并结构兼容。
- **实测复核（2026-08-05，AGENT-ENGINE v1.12 复验）**：**修正 1 处断言**——`AIReviewer.review_code` 从「孤儿」修正为「生产未接线、测试已覆盖」（`tests/unit/test_review_result_parsing.py` 5 处直接调用，app/ 生产仅 multi_model_agent:209 用 review_plan）；**补发现**——multi_angle_review 输出 `reviews` 数组为第四种审查结构，审查体系实为 4 套（CodeReviewer/AIReviewer/multi_angle_review/cross_validator）。其余断言复核通过：LLMClient.call 返回 str（llm_client.py:106）、Aicode.py:675/GirlAi.py:511 下标无 try 兜底、multi_angle_review:195/:285 直连 call_llm、CodeReviewer fallback 含 needs_fix/version_issues、orchestrator_files:738 依赖 needs_fix。
- **实测复核（2026-08-05，AGENT-ENGINE v1.11 四步法重推演）**：**PPTAgent 三连推演**（BATCH2 v1.8，Backlog 10 项）——`modify_outline` 孤儿方法、tests 零覆盖、`ppt_system_prompt` 钩子零注册、`quality` 死参、单点依赖 aiGeneratorPptx；**字段级消费追踪**发现 `type` 字段渲染层整体忽略（7 类契约未兑现）、`slide_type` 语义两套并存（title/... vs cover/...，HTML :676 封面失效）、`notes` 零消费、chart 零支持。**agent 全系统数据流/契约推演**（§10，Backlog 5 项）——两套 LLM 调用体系返回契约双轨（顶层 call_llm dict vs LLMClient str，26 直连文件解析健壮性参差：ppt_agent .get 兜底 vs Aicode.py:675 下标无兜底）；**三套审查体系并存**（CodeReviewer→Dict / AIReviewer 独立类不继承 Specialist→ReviewResult / multi_angle_review 直连 call_llm）；**`review_code` 双契约**（needs_fix/version_issues 在 ReviewResult 缺失，消费方 orchestrator_files:738 依赖 needs_fix）；**AIReviewer.review_code 生产代码零调用**（app/ 仅 multi_model_agent:209 用 review_plan，但 tests/unit/test_review_result_parsing.py 5 处覆盖——非孤儿，属生产未接线）。**方法论固化**：四步推演流程（模块断言→数据流字段消费矩阵→语义契约核对→Backlog）。
- **实测复核（2026-08-05，AGENT-ENGINE v1.10 + BATCH1 v1.3 + AGENT-EVOLUTION v1.7）**：**演化拆分建议归属修正**——`_initialize_components` 定义于 `mixin.py:39`（非 spec_first，拆分时迁入 spec_initializer）；`_select_engineer` 仅定义于 `orchestrator_files.py:577`（spec_first 经继承调用，**非重复定义**）；`_recover_invalid_content_orchestator`（orchestrator_files:584）与 `_recover_invalid_content`（spec_first:2003）为两套不同名同类实现；`refine` 仅 `refinement_loop.py:94`。**拆分可行性验证通过**：cross_validator 四部分（DEFAULT_CRITICAL_PATTERNS:74 / 一致性:282 / 符号:328/:763 / import 契约）、tools.py 文件(10)/git(4)/搜索(3)/执行(2)四域、dynamic_model_router 指标(ModelPerformanceTracker:164)/熔断(:466)/学习(LearningRouter:323)/配置(:130/:143/:735)四部分全部成立。**BATCH2/3 全量行数复核零偏差**：BATCH2 27 模块（A5=11/A6=4/A7=12）与 BATCH3 38 模块（A8=5/A9=9/A10=7/A11=5/A12=5/A13=4/A15=3，A14 mcp_client 513 行以文字列出）全部精确；file:line 复核（error_recovery(gen):17 动态 import、spec_first:14/:165 critical_decision 接线、orchestrate_endpoints:742、multi_model_agent:42 noqa、test_runner:237）全部精确。**模块数口径修正**：AGENT-ENGINE「76 模块」→「77 顶层 + 3 子包（去重 98，40,880 行）」，AGENT-EVOLUTION「112 模块」→「98」（15 子系统标注和与实测去重数一致）。
- **实测复核（2026-08-05，AGENT-ENGINE v1.9 + BATCH1-3 v1.2 + AGENT-EVOLUTION v1.6）**：补全 19 处模块行数（A6/A7/A10 全部从「-」填实）；`critical_decision.py` **已接线**（spec_first:14/:165 + orchestrate_endpoints:742，非待确认）；BATCH1 行号校正（`_try_react_auto_fix` 定义 :9、traditional 调用 :294/:297）；A1 八模块行数复核精确；docker_runner 三使用方（orchestrator_testing:213/code_tasks:102/project_tasks:88）、dynamic_model_router:880 Kolors 注释精确。**补充自第四轮**：收集错误 8 个（`_LayeredModelRouterCompat` 缺失 2 测试、`_REACT_MODE_BY_COMPLEXITY` 缺失、aiofiles 未装 4 测试、jwt 引用错误库）；用例数单测 1506 / E2E 413。
- **实测复核（2026-08-05，AGENT-ENGINE v1.9 + AGENT-EVOLUTION v1.5）**：收集错误实测 **8 个**——`_LayeredModelRouterCompat` 缺失被 **2 个测试**引用（test_multi_model_agent.py:19 + test_orchestrator.py:16）；`test_specialist_base.py:20` 引用不存在的 `_REACT_MODE_BY_COMPLEXITY`（specialist_base 仅 `_REACT_MODE="full"`:26/`_REACT_MAX_ROUNDS=3`:27，与「复杂度矩阵不存在」同源）；aiofiles 未声明且未装致 4 个测试收集失败（test_mcp_admin_api/test_model_admin_api/test_model_manager_api/test_system_monitor，源码 app/db/log_server.py、app/utils/agent_core.py 使用）；test_security_services.py:12 `import jwt` 引用 PyJWT（项目统一 `from jose import jwt`）。用例数实测更新：单测收集 **1506**、E2E **413**。模型配置断言行号全部精确（dynamic_model_router:735 `_load_roles_assignment`、services/model_config_manager:232 `_sync_to_agent_config`、model_admin:67/:83、react_agent:72 max_iterations=10、MODEL_REGISTRY 17 模型、EMBEDDING_DIM=768）。
- **实测复核（2026-08-04，AGENT-ENGINE v1.8 + BATCH1-3 v1.1）**：**「5×5 角色复杂度矩阵」不存在**（dynamic_model_router:639 docstring「不再依赖复杂度」，全库零实现）；tools.py 工具数 **20**；两个同名 `error_recovery.py` 厘清（`orchestrator_generation/` 33 行 ErrorRecoveryMixin，`_try_react_auto_fix`:9 且显式 `max_iterations=5`；`app/agent/` 797 行 ErrorRecoveryLoop:38/validate_and_fix:137/模型降级:458）；docker_runner **非孤儿**（3 个生产使用方，废弃前须迁移）；绕过 LLMClient 直连 call_llm 实为 **26 处**；react_engine **已接线**（4 处引用）、coverage_checker **已接线**（mixin.py）；tracing 接线 **11 处**、orchestrator_progress 引用 **8 处**；双模型对抗 `validate_and_select` 在 `cross_validator.py:141`；pytest 冲突（requirements 9.0.3 vs test 7.4.3，实测基线 8.4.2）；9.1 闭包 Bug 复测仍在（单测 2 failed 路径越界）；端点实测 257；孤儿实测：strategy_learner/user_preference_learner/fix_pattern_cache/cloud_learning_hub/multi_language_parser 零引用，task_planner 疑孤儿（仅 noqa F401）

## B. 前端 Agent 界面（3 子系统）

- **大系统演化文件**：[AGENT-FRONTEND.md](AGENT-FRONTEND.md)
- **子系统**：B1 布局与排版 / B2 Diff 与文件预览 / B3 对话流
- **演化路径**：布局修正（P0 右栏固定宽）→ Diff/标签/目录树（P0/P1）→ 对话流重构（P0/P1）→ 交互协作 + 性能可观测（P2/P3）

## C. 对话接口 Chat（2 子系统）

- **大系统演化文件**：[CHAT.md](CHAT.md)（v5.0，2026-08-04 重写为四阶段范式）
- **子系统**：C1 命名与语义 / C2 对话能力
- **演化路径**（v5.0 四阶段对齐）：
  - 阶段一 修复止血（P0）：`/code`→`/chat` 更名 alias 兼容（含 4 处压缩函数引用迁移）→ 处置 resume 孤儿端点 → 确认历史管理已具备（仅命名对齐）
  - 阶段二 统一收敛（P1）：三套会话体系统一（History/ConversationMessage/AicloudSession）→ `compress_history` 全链路接线（Aicode/Agent/kolors/PPT）→ 模型选择走 LearningRouter + 名单三源合一
  - 阶段三 智能增强（P2）：搜索决策 LLM 化 → 断点续传持久化 → 非图片文件解析补全 + 视觉常量统一
  - 阶段四 平台化（P3）：对话即 Agent 统一入口 → 跨会话长记忆 + Skill 化
- **实测修正（2026-08-04，CHAT.md v4.0/v5.0）**：历史管理端点已存在（auth.py /history、/conversation/history、/conversations），此前「仅删除」为误判；`compress_conversation_history` 为跨模块共享函数（kolors_api 3 处 + aiGeneratorPptx 1 处复用）；`compress_history`（LLM 摘要）**零调用方**，Agent 编排侧实际用 `truncate_history` 纯截断；resume 断点续传为**孤儿端点**（前端零调用 + 内存态缓存）；三套会话体系并存；`dynamic_model_router` 被 Agent 编排 + 模型管理/健康检查/负载侧引用，**Aicode 不引用**；CodeRequest 无 model validator；模型名单三处并存；搜索摘要方案 `search_and_format_with_summaries` 已实现未接线（Aicode 用纯 snippet）；前端代码混淆导致 `/api/v1/n/` 为误报（真实路径 `/aicloud/*`）

## D. PPT 生成（3 子系统）

- **大系统演化文件**：[PPT-FEATURE.md](PPT-FEATURE.md)
- **子系统**：D1 契约与入口 / D2 大纲与模板 / D3 视觉与配图
- **演化路径**：字段契约修复（P0）→ API 拆分（P1）→ 大纲/模板统一（P1/P2）→ 接线动画/布局/图片升级（P2）→ 渲染回检 + Skill 化（P2/P3）

## E. 图片生成（2 子系统）

- **大系统演化文件**：[IMAGE-GENERATION.md](IMAGE-GENERATION.md)
- **子系统**：E1 生成链路 / E2 能力归一与增强
- **演化路径**：缓存接线 + 占位端点移除（P0）→ 双模式 + 快捷端点对齐（P1/P2）→ ImageProvider 归一（P1）→ 模型路由/回检/Agent 集成（P1/P2）

## F. 注释与文档一致性（2 子系统）

- **大系统演化文件**：[COMMENT-NORMALIZATION.md](COMMENT-NORMALIZATION.md)
- **子系统**：F1 误导消除 / F2 规范与自动化
- **演化路径**：常量/端点/字段一致性扫描（P1）→ 规范定义 + ruff/ESLint/CI（P1）→ 收敛双写 + 注释校验化（P2）

## G. 上下文压缩（专项）

- **大系统演化文件**：[AGENT-CONTEXT-COMPRESSION.md](AGENT-CONTEXT-COMPRESSION.md)
- **演化路径**：token 口径统一 → 持久化/窗口感知 → LLM 语义压缩接线 + ContextCompressor 归一 → 预防式压缩 + 跨会话记忆
- **归属**：同时作为 Agent A4 子系统（见 AGENT-EVOLUTION.md A4）

## H. 服务与工具层（H1-H14，已推演）

- **大系统演化文件**：[SERVICES-EVOLUTION.md](SERVICES-EVOLUTION.md)
- **子系统演化文件**：见 SERVICES-EVOLUTION.md 各子系统小节（H1-H14，每子系统含现状基线/演化目标/分阶段路径/风险）
- **演化路径**：沙箱/工作流/文件/任务/认证/APIKey/视觉统一（P2）→ 模型管理/MCP/安全基线（P2）→ 记忆/推送/Nginx/可观测性（P3）

| 子系统 | 模块（实测行数） | 演化要点 | 优先级 |
|--------|-----------------|---------|--------|
| H1 (G1) AI Cloud 沙箱 | aicloud.py 942 | 沙箱执行统一 docker_runner、审查收敛、审计/知识库 RAG | P2/P3 |
| H2 (G2) 工作流引擎 | workflow.py 676 | 节点与 Agent 工具对齐、可视化编辑 | P2/P3 |
| H3 (G3) 文件上传 | file_upload.py 467 | 解析统一、存储可配置 | P2/P3 |
| H4 (G4) 任务队列 | task_queue.py 352 + task_manager.py 330 | 状态机统一、WS 进度复用 | P2 |
| H5 (G5) 认证与用户 | auth.py 550 + user_manage.py 295 | 权限模型收敛、用户偏好打通 | P2/P3 |
| H6 (G6) API Key | apikey.py 585 | 用量配额 + 健康度入路由 | P2 |
| H7 (G7) 视觉理解 | vision_api.py 310 | 与对话统一、多模态工具注册 | P2/P3 |
| H8 (G8) 虚拟姬 GirlAi | GirlAi.py 855 | 记忆接入 agent_memory | P3 |
| H9 (G9) GitHub 集成 | github.py 260 | 产物自动推送、git_operations 统一 | P3 |
| H10 (G10) Nginx 管理 | nginx_api.py 503 + nginx_ai.py 100 | 配置模板统一、AI 生成走 Agent 链路 | P3 |
| H11 (G11) 模型管理 v2 | model_admin.py 315 + model_config_api.py 362 | 配置单一来源、与路由全量打通 | P2 |
| H12 (G12) MCP 管理 | mcp_admin.py 232 + mcp_client.py 513 | 工具注册进 SPECIALIST_TOOLS | P2 |
| H13 (G13) 守护与监控 | guardian_router.py 858 + health.py 196 + prometheus_metrics.py 211 | 可观测性统一 | P3 |
| H14 (G14) 安全体系 | security/csrf/guardrails/rate_limiter | 安全基线固化、扫描入 CI | P2 |

## 优先级矩阵（P0/P1 汇总）

| 优先级 | 演化点 |
|--------|--------|
| **P0** | faiss 安装、ToolRegistry 闭包 Bug、test_runner 收敛、Java/Go 静态验证、文生图缓存接线、占位端点、/chat 端点、PPT 字段契约、右栏固定宽度、行级 Diff、对话流模型 |
| **P1** | spec_first/cross_validator 拆分、token 口径、压缩持久化、阈值窗口、compress_history 接线、模型配置三源合一、图片模型路由、spec-first 写索引、工具中间件、chat 更名、会话压缩、模型选择、PPT API 拆分、大纲统一、ImageProvider 归一、注释扫描/规范、右栏/左栏布局 |
| **P2** | 其余（统一收敛、孤儿接线、回检闭环、H 组大部分） |
| **P3** | 平台化（长记忆、多 Agent、H 组远期） |

## 演化顺序建议

1. **第一轮（修复）**：P0 全部——契约/缓存/闭包/faiss/验证收敛/布局/diff
2. **第二轮（拆分统一）**：P1——大文件拆分、token 口径、压缩接线、模型配置合一、对话能力接入
3. **第三轮（智能增强）**：P2——回检闭环、孤儿接线、ImageProvider、H 组子系统逐项
4. **第四轮（平台化）**：P3——长记忆、多 Agent、Skill 化、H 组远期
