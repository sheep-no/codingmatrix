# 需求实施计划

- [x] 1. 更新 Chat 请求契约和模型选择
  - [x] 让 `search_mode` 支持 `auto/on/off`，兼容旧布尔字段并移除前端强制默认模型（R1、R3、P4）。
  - [x] 保留显式模型优先级，空模型交由后端自动选模。
  - [x] 编写并运行联网意图和查询边界回归测试。

- [x] 2. 实现上下文阶段、受界搜索和附件正文解析
  - [x] 将 `on`、`off`、`auto` 接入 `_build_context`，为搜索构造有界查询并保留来源摘要（R1、R5、P1、P2）。
  - [x] 复用 `knowledge_processor.parse_document` 完成普通文档解析和缓存，向上下文加入明确错误（R2、P3）。
  - [x] 编写并运行搜索策略、查询裁剪和解析失败测试。

- [x] 3. 接入流式阶段状态并清理混合入口
  - [x] 在现有 SSE JSON 行中发送真实解析、搜索、回答阶段状态，前端显示状态、错误与来源（R4、R5）。
  - [x] 隐藏并移除 `useHybrid` 联动和请求字段，项目模式禁用联网选择，保留深度思考。
   - [x] 前端请求、模式、来源恢复和阶段事件测试。

- [x] 4. 检查点
   - [x] 后端 15 项、聊天前端 13 项针对性测试通过。
   - [x] 完整前端 32 个文件、150 项测试通过；Vite 生产构建通过（既有大分包提示）。
   - [x] 预览首页和经前端访问后端健康接口均返回 200；开发后端已重启加载改动。

## 搜索深度扩展

- [x] 更新浅搜索和固定两轮搜索需求与接口设计。
- [x] 实现输入选择、请求参数传递和两轮搜索执行。
- [x] 验证结果驱动的第二轮查询、来源去重与失败回退。
- [x] 前端 152 项测试通过，生产构建通过；搜索深度的 mock 回归已完成。最新后端回归与真实联网结果见下方实测记录。

## 后续阶段

- [x] 修正真实搜索发现的附件主题后置和第二轮首条结果偏置，运行回归及真实搜索复测。

- 联合回答的独立作答、交叉评审与整合，以及质量/成本对照评估。
- 扫描型 PDF OCR、复杂附件实体消歧与模型原生推理预算控制。
- 多轮搜索新增有效证据、时效性及真实模型端到端效果验证。

## 2026-09-08 真实搜索实测

使用 `/tmp/opencode/chat_search_live.py` 调用实际 `search_with_sources`，包装函数仅记录调用；附件使用合成 TXT 和内存 SQLite 的 File 表。此验证覆盖上下文构建、解析缓存和搜索阶段，未覆盖浏览器上传接口、真实模型回答或回答质量。

| 场景 | 调用次数 | 耗时 | 去重后网页来源 | 第二轮新增 URL |
| --- | --- | --- | --- | --- |
| shallow | 1 | 1.07 秒 | 3 | 不适用 |
| multi | 2 | 0.56 秒 | 3 | 0 |
| attachment_multi | 2 | 0.60 秒 | 3 | 0 |

- 两个多轮场景的第二次查询均发生变化，合并来源没有重复 URL。
- 修复前附件查询返回“核查”的词典解释；主题前置后返回 Python 官网、下载页等相关来源。附件正文进入上下文，解析缓存已写入。
- 第二轮选用了匹配 release 主题的下载页摘要，但摘要包含 2023 年旧版本信息；两轮结果相同。本次数据无法证明多轮搜索提高最新版本回答准确率，查询选择仍是词语匹配启发式。
- 单次耗时受供应商和缓存影响，用例之间的耗时差异不构成性能结论。
- 最新命令 `python3 -m pytest tests/unit/test_chat_answer_quality.py -q`：21 项通过，包含本轮新增的主题优先及问题保留回归。
- 原始日志：`/tmp/terminal_term_1788878323649_453.log`（修复前）、`/tmp/terminal_term_1788878609184_454.log`（修复后）。
- 修复后后端已重启，终端为 `term_1788878704770_455`；前端入口 `/api/v1/health` 返回 200，平台预览连接正常，`git diff --check` 通过。本轮仅修改后端及测试、SDD，沿用前轮前端测试和构建结果。
