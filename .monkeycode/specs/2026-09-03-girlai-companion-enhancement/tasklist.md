# 需求实施计划

- [x] 1. 建立结构化伙伴回合基础契约
  - 创建 `CompanionTurn`、情绪、意图、记忆候选和能力降级的 Pydantic schema，覆盖需求 3.1、5.3。
  - 创建伙伴回合服务接口和模型上下文适配接口，复用 `DynamicModelRouter` 与 `girlai_state_adapter`，覆盖需求 1.1、8.1。
  - 为主模型结构化输出增加默认值、版本号、字段范围和解析失败降级，覆盖需求 3.2、3.4、5.5。
  - [x]* 1.1 编写 schema 边界和结构化输出解析单元测试，覆盖设计属性 10。
   - [x]* 1.2 编写结构化回合字段完整性属性测试，覆盖需求 5.3。

- [x] 2. 实现伙伴上下文构建和模型编排
  - 创建 `companion_context_service`，组合角色设定、近期消息、摘要、授权记忆和当前输入，覆盖需求 1.1。
  - 实现上下文预算裁剪和来源标记，按近期消息、相关记忆、摘要和角色设定优先级处理，覆盖需求 1.2、1.3。
  - 创建 `companion_model_service`，为对话、情绪、意图和记忆筛选任务选择模型并保存模型上下文，覆盖需求 5.1、5.3。
  - 创建 `companion_orchestrator`，编排上下文读取、模型调用、结构化结果解析和状态写入，覆盖需求 1.4、5.2。
  - [x]* 2.1 编写上下文预算、授权筛选和 fallback 单元测试，覆盖设计属性 2、10。
   - [x]* 2.2 编写跨会话上下文隔离和模型上下文恢复集成测试，覆盖设计属性 1、6。

- [x] 3. 增加结构化伙伴对话 API
  - 在 `app/api/v1/GirlAi.py` 增加 `/companion/turn` 和 `/companion/state` JWT 接口，复用现有输入长度、角色解析和用户归属校验。
  - 将现有 GirlAI 对话入口映射到伙伴编排服务，保留历史、摘要和统一 `sessions/messages` 双写兼容行为，覆盖需求 1.4。
  - 统一返回 `conversation_id`、`turn_id`、`state_revision` 和 `degraded_capabilities`，覆盖需求 4.1、5.3。
  - 为接口增加请求幂等键和陈旧 revision 冲突响应，覆盖设计属性 4。
   - [x]* 3.1 编写 API schema、认证、归属校验和错误响应测试，覆盖需求 1.4、5.5。
   - [x]* 3.2 编写 legacy 历史与统一状态事务一致性集成测试，覆盖设计属性 3。

- [x] 4. 实现记忆候选和用户授权管理
  - 扩展 `UserPreference` 或等价统一 checkpoint 表达 `candidate`、`confirmed`、`deleted` 状态、来源、可见性和最近使用时间，覆盖需求 2.1-2.4。
  - 创建 `companion_memory_service`，实现候选记忆生成、确认、修订、删除和检索授权。
  - 增加记忆分页查询、确认、删除 API，并执行当前用户归属校验，覆盖需求 2.1、2.2、2.3、2.5。
  - 将已确认且允许伙伴使用的记忆接入上下文构建，删除后立即停止检索，覆盖需求 1.3、2.3。
  - [x]* 4.1 编写记忆状态转换、授权筛选和用户隔离单元测试，覆盖设计属性 2。
  - [x]* 4.2 编写记忆删除后的后续回合检索集成测试，覆盖需求 2.3。

- [x] 5. 实现情绪与意图识别
  - 创建标准化情绪标签、强度、意图标签和置信度模型，覆盖需求 3.1。
  - 接入轻量分类模型和置信度阈值配置，低置信度时使用中性策略，覆盖需求 3.2。
  - 为需要关怀的情绪状态增加关怀语气与工作选项策略，覆盖需求 3.3。
  - 将识别结果保存到伙伴回合状态和统一会话事件，解析异常写入可追踪降级事件，覆盖需求 3.4、5.3。
  - [x]* 5.1 编写标签、阈值、关怀策略和解析失败单元测试。
   - [x]* 5.2 编写分类模型故障时文字主链路可用性属性测试，覆盖设计属性 10。

- [x] 6. 增强前端伙伴交互界面
  - 创建 `useGirlAiCompanion`，管理伙伴状态、回合响应、revision 合并、能力降级和本地缓存。
  - 在 `VirtualGirl.vue` 中接入 `CompanionTurnPanel` 和 `MemoryConsentPanel`，覆盖需求 2.1、4.1。
  - 扩展 `src/utils/api/girl.js` 的伙伴状态、回合和记忆接口。
  - 保留现有角色选择、历史、拖拽、缩放、最小化、自动隐藏和 Picture-in-Picture 行为。
  - [x]* 6.1 编写回合状态、记忆确认和能力降级前端测试。
   - [x]* 6.2 编写页面重载、revision 冲突和 SSE/异步事件顺序 E2E 测试，覆盖设计属性 6、10。

- [x] 7. 增加语音适配接口和能力降级
  - 定义标准化语音转写和语音输出状态 schema，关联同一 `turn_id`，覆盖需求 4.2-4.4。
  - 增加语音转写 API，将转写文本复用文字伙伴回合流程，覆盖需求 4.2、4.3。
  - 增加语音输出能力状态和供应商适配接口，语音服务失败时回退文字链路，覆盖需求 4.4、4.5。
  - 在前端加入能力检测和文字回退状态展示，覆盖需求 4.1、4.5。
   - [x]* 7.1 编写语音 schema、超时和文字回退单元测试。
   - [x]* 7.2 编写语音转写到伙伴回合的集成测试，覆盖需求 4.2、4.3。

- [x] 8. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - P0 完成后确认结构化回合、模型上下文、legacy 双写和文字降级链路可独立运行。
  - P1 完成后确认记忆授权和情绪意图能力满足用户归属与授权约束。
  - P2 完成后确认前端状态能够跨请求保持一致。
  - P3 完成后确认语音适配、现有 GirlAI 交互和全量回归测试通过。
