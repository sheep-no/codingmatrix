# 需求实施计划

- [x] 1. 建立结构化伙伴回合基础契约
  - 创建 `CompanionTurn`、情绪、意图、记忆候选、工具请求和能力降级的 Pydantic schema，覆盖需求 3.1、4.1、8.3。
  - 创建伙伴回合服务接口和模型上下文适配接口，复用 `DynamicModelRouter` 与 `girlai_state_adapter`，覆盖需求 1.1、8.1。
  - 为主模型结构化输出增加默认值、版本号、字段范围和解析失败降级，覆盖需求 3.2、3.4、8.5。
  - [ ]* 1.1 编写 schema 边界和结构化输出解析单元测试，覆盖设计属性 10。
  - [ ]* 1.2 编写结构化回合字段完整性属性测试，覆盖需求 8.3。

- [x] 2. 实现伙伴上下文构建和模型编排
  - 创建 `companion_context_service`，组合角色设定、近期消息、摘要、授权记忆、活动任务和当前输入，覆盖需求 1.1。
  - 实现上下文预算裁剪和来源标记，按近期消息、相关记忆、摘要和角色设定优先级处理，覆盖需求 1.2、1.3。
  - 创建 `companion_model_service`，为对话、情绪、意图和记忆筛选任务选择模型并保存模型上下文，覆盖需求 8.1、8.3。
  - 创建 `companion_orchestrator`，编排上下文读取、模型调用、结构化结果解析和状态写入，覆盖需求 1.4、8.2。
  - [x]* 2.1 编写上下文预算、授权筛选和 fallback 单元测试，覆盖设计属性 2、10。
  - [ ]* 2.2 编写跨会话上下文隔离和模型上下文恢复集成测试，覆盖设计属性 1、6。

- [x] 3. 增加结构化伙伴对话 API
  - 在 `app/api/v1/GirlAi.py` 增加 `/companion/turn` 和 `/companion/state` JWT 接口，复用现有输入长度、角色解析和用户归属校验。
  - 将现有 GirlAI 对话入口映射到伙伴编排服务，保留历史、摘要和统一 `sessions/messages` 双写兼容行为，覆盖需求 1.4。
  - 统一返回 `conversation_id`、`turn_id`、`state_revision` 和 `degraded_capabilities`，覆盖需求 7.1、8.3、8.5。
  - 为接口增加请求幂等键和陈旧 revision 冲突响应，覆盖设计属性 6。
  - [x]* 3.1 编写 API schema、认证、归属校验和错误响应测试，覆盖需求 1.4、8.5。
  - [ ]* 3.2 编写 legacy 历史与统一状态事务一致性集成测试，覆盖设计属性 3。

- [ ] 4. 实现记忆候选和用户授权管理
  - 扩展 `UserPreference` 或等价统一 checkpoint 表达 `candidate`、`confirmed`、`deleted` 状态、来源、可见性和最近使用时间，覆盖需求 2.1-2.4。
  - 创建 `companion_memory_service`，实现候选记忆生成、确认、修订、删除和检索授权。
  - 增加记忆分页查询、确认、删除 API，并执行当前用户归属校验，覆盖需求 2.1、2.2、2.3、2.5。
  - 将已确认且允许伙伴使用的记忆接入上下文构建，删除后立即停止检索，覆盖需求 1.3、2.3。
  - [ ]* 4.1 编写记忆状态转换、授权筛选和用户隔离单元测试，覆盖设计属性 2。
  - [ ]* 4.2 编写记忆删除后的后续回合检索集成测试，覆盖需求 2.3。

- [ ] 5. 实现情绪与意图识别
  - 创建标准化情绪标签、强度、意图标签和置信度模型，覆盖需求 3.1。
  - 接入轻量分类模型和置信度阈值配置，低置信度时使用中性策略，覆盖需求 3.2。
  - 为需要关怀的情绪状态增加关怀语气与工作选项策略，覆盖需求 3.3。
  - 将识别结果保存到伙伴回合状态和统一会话事件，解析异常写入可追踪降级事件，覆盖需求 3.4、8.3。
  - [ ]* 5.1 编写标签、阈值、关怀策略和解析失败单元测试。
  - [ ]* 5.2 编写分类模型故障时文字主链路可用性属性测试，覆盖设计属性 10。

- [ ] 6. 接入受控只读工具和审批链
  - 创建 `companion_policy_service`，注册只读工具白名单、参数 schema、资源归属和执行权限，覆盖需求 4.1、4.2。
  - 实现工具请求展示、一次性授权、执行结果摘要和回合审计记录，覆盖需求 4.1-4.3。
  - 将文件写入、命令执行和外部副作用请求转入现有 Agent 审批链，覆盖需求 4.4、8.5。
  - 增加工具批准 API 和待审批状态响应，执行失败时保留普通对话链路，覆盖需求 4.5。
  - [ ]* 6.1 编写工具白名单、参数安全、归属校验和审批状态测试，覆盖设计属性 4、5。
  - [ ]* 6.2 编写只读工具审计和副作用工具审批集成测试，覆盖需求 4.3、4.4。

- [ ] 7. 关联统一任务和 checkpoint
  - 创建 `companion_task_service`，将用户确认的任务意图映射到统一 `tasks`、`task_events` 和 `checkpoints`，覆盖需求 5.1、5.2。
  - 增加伙伴任务查询和从对话回合确认创建任务 API，覆盖需求 5.1、5.3。
  - 保存任务摘要、来源回合、等待用户输入或审批状态和可恢复进度，覆盖需求 5.3-5.5、8.4。
  - 将任务状态变化和 checkpoint 事件接入伙伴上下文，支持会话重载恢复。
  - [ ]* 7.1 编写任务状态转换、事件顺序和 checkpoint 恢复测试，覆盖设计属性 7。
  - [ ]* 7.2 编写跨请求任务与对话状态一致性集成测试，覆盖需求 5.2-5.4。

- [ ] 8. 实现主动提醒服务
  - 创建提醒偏好、静默时间、频率限制、渠道和去重键的数据模型或统一状态表示，覆盖需求 6.1、6.2、6.5。
  - 创建 `companion_reminder_service`，根据任务事件和用户授权生成、延迟、合并和处理提醒。
  - 增加提醒偏好更新 API 和提醒处理状态接口，覆盖需求 6.2、6.3。
  - 接入现有调度器或后台任务执行入口，失败时保存可重试事件，覆盖需求 6.4、8.4。
  - [ ]* 8.1 编写静默时间、频率限制和事件去重属性测试，覆盖设计属性 8。
  - [ ]* 8.2 编写提醒创建、延迟、处理和重试集成测试，覆盖需求 6.1-6.4。

- [ ] 9. 增强前端伙伴交互界面
  - 创建 `useGirlAiCompanion`，管理伙伴状态、回合响应、revision 合并、能力降级和本地缓存。
  - 在 `VirtualGirl.vue` 中接入 `CompanionTurnPanel`、`MemoryConsentPanel` 和 `CompanionTaskPanel`，覆盖需求 2.1、5.3、7.1。
  - 扩展 `src/utils/api/girl.js` 的伙伴状态、回合、记忆、工具、任务和提醒接口。
  - 保留现有角色选择、历史、拖拽、缩放、最小化、自动隐藏和 Picture-in-Picture 行为。
  - [ ]* 9.1 编写回合状态、记忆确认、任务确认和能力降级前端测试。
  - [ ]* 9.2 编写页面重载、revision 冲突和 SSE/异步事件顺序 E2E 测试，覆盖设计属性 6、10。

- [ ] 10. 增加语音适配接口和能力降级
  - 定义标准化语音转写和语音输出状态 schema，关联同一 `turn_id`，覆盖需求 7.2-7.4。
  - 增加语音转写 API，将转写文本复用文字伙伴回合流程，覆盖需求 7.2、7.3。
  - 增加语音输出能力状态和供应商适配接口，语音服务失败时回退文字链路，覆盖需求 7.4、7.5。
  - 在前端加入能力检测和文字回退状态展示，覆盖需求 7.1、7.5。
  - [ ]* 10.1 编写语音 schema、超时和文字回退单元测试。
  - [ ]* 10.2 编写语音转写到伙伴回合的集成测试，覆盖需求 7.2、7.3。

- [ ] 11. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - P0 完成后确认结构化回合、模型上下文、legacy 双写和文字降级链路可独立运行。
  - P1 完成后确认记忆授权、情绪意图和只读工具能力满足用户归属与审批约束。
  - P2 完成后确认任务恢复、主动提醒和前端状态能够跨请求保持一致。
  - P3 完成后确认语音适配、现有 GirlAI 交互和全量回归测试通过。
