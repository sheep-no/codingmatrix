# 需求实施计划

- [x] 1. 建立大纲草稿、版本和审批接口
  - 创建 `OutlineDraft`、`OutlineSlide`、内容块和素材意图模型，覆盖需求 1.1、1.3、需求 8.3。
  - 实现主题/材料生成草稿、草稿读取、编辑、页面增删重排和批准接口，覆盖需求 1.1-1.4。
  - 为批准操作增加页面字段校验，覆盖需求 1.5。
  - 为 `/generate-text`、`/generate-from-text` 和现有任务入口建立兼容映射，保留现有调用方行为。
  - [x] 1.1 编写大纲版本、审批快照和字段校验单元测试，覆盖设计属性 1。
  - [x] 1.2 编写大纲接口集成测试，覆盖文本输入和文件输入共用审批流程。

- [x] 2. 增加大纲、质量报告和产物的状态存储
  - 创建数据库迁移和模型，保存大纲草稿版本、批准时间、场景、模板和页面结构，覆盖需求 1.3-1.4、8.3。
  - 扩展统一 State/Checkpoint 的 PPT 上下文，保存 `outline_version`、`quality_mode`、规划器版本和质量阶段，覆盖设计属性 1、4、8。
  - 将 PPTX、预览图、布局元数据和质量报告登记为 Artifact，并建立任务与 Artifact 的关联。
  - 增加质量问题、修复动作、重排次数和降级阶段的持久化字段，覆盖需求 7.6-7.7、8.1、8.5。
  - [x] 2.1 编写数据库模型、迁移和状态序列化单元测试。
  - [x] 2.2 编写 Artifact 版本关联和 Checkpoint 恢复集成测试。

- [x] 3. 实现场景识别、模板推荐和设计令牌解析
  - 增加商务汇报、数据报告、产品路演、学术报告、教育培训和通用演示场景模型，覆盖需求 2.1、2.5。
  - 扩展 `TemplateManager` 的场景候选排序和至少三个模板候选返回，覆盖需求 2.2。
  - 扩展 `TemplateConfig` 与模板预设的颜色、字体、间距、形状、图片和图表令牌，覆盖需求 2.3。
  - 实现旧模板字段到新令牌的默认值迁移，并保存模板版本。
  - [x] 3.1 编写场景置信度、候选排序和令牌解析单元测试。
  - [x] 3.2 编写模板一致性属性测试，验证同一任务所有页面引用同一令牌版本。

- [x] 4. 建立语义页面规划器和容量预算
  - 扩展视觉决策模型，增加页面类型、核心结论、内容块、素材意图和容量预算，覆盖需求 1.1、3.1、4.1。
  - 扩展 `LayoutType` 与 `LayoutDecider`，实现封面、目录、章节、要点、图文、数据、对比、时间线、流程、总结和结束页的兼容映射。
  - 实现硬约束过滤、语义匹配评分、模板匹配评分、空间利用率评分和连续布局惩罚，覆盖需求 4.2-4.5。
  - 实现列表数量、标题长度、内容块数量和页面容量预算检查，覆盖需求 3.2-3.5。
  - [x] 4.1 编写页面类型识别、容量预算和布局评分单元测试。
  - [x] 4.2 编写页面顺序稳定性和内容块顺序保持属性测试，覆盖设计属性 3。

- [x] 5. 重构 PPT 生成编排为可追踪阶段
  - 创建 `PPTGenerationOrchestrator`，编排大纲快照、规划、素材、渲染、规则质检、重排、视觉复审和交付阶段。阶段契约、取消、失败、恢复能力和 Celery 生产入口接入已完成。
  - 在现有任务状态和 `task_events` 中记录阶段名称、页面进度、版本信息和取消状态，覆盖需求 9.1、9.5。
  - 让生成任务接收 `outline_id`、`outline_version`、`template_version` 和 `quality_mode`，覆盖需求 1.3、6.4、8.3。
  - 保持 PPTX、HTML、Markdown 和 PDF 的现有格式分流行为。
  - [x] 5.1 编写阶段状态转换和事件顺序单元测试。
  - [x] 5.2 编写任务取消、恢复和快照隔离集成测试，覆盖设计属性 1、需求 9.4-9.5。

- [x] 6. 实现语义页面渲染器和素材规则
  - 统一页面元数据、类型归一、图片等比适配、缺图回退和图表选择规则已完成，并已接入现有 PPTX 布局计划与令牌感知渲染。
  - 将标题、核心结论、来源、页码、背景和装饰收敛到公共渲染层，确保三级视觉层级和网格间距，覆盖需求 4.3-4.4。
  - 实现时间线、流程、目录和结束页渲染器，覆盖需求 4.1 和阶段 2 目标。
  - 统一图片等比缩放、裁切焦点、圆角、遮罩、色调和失败回退，覆盖需求 5.1、5.2、5.4-5.5。
  - 为结构化数据选择图表类型并显示来源占位信息，覆盖需求 5.3。
  - [x] 6.1 为每种页面类型编写渲染元数据和关键元素布局测试。
  - [x] 6.2 编写图片宽高比、缺图回退和图表来源测试，覆盖设计属性 7。
  - [x] 6.3 完成五类商业叙事角色、结构化商业字段及 `business`/`creative` 独立构图，并验证字段实际落版。
  - [x] 6.4 完成 `modern`、`minimal`、`tech` 独立封面与五类角色构图，补齐 `tech` 设计令牌；五主题几何签名测试通过，真实视觉复审评分分别为 `8.3/10`、`8.6/10`、`8.0/10`。
  - [x] 6.5 完成 `academic` 研究简报封面与五类角色构图，增加研究问题、证据注释、假设对照、研究协议和来源占位表达；专项回归 `88 passed`，路线页二轮视觉评分 `8.2/10`，全稿无元素越界。
  - [x] 6.6 完成 `education` 教学工作坊封面与五类角色构图，增加学习目标、课堂证据、练习卡、课程路径和课后行动；专项回归通过，6 页真实样稿无元素越界。
  - [x] 6.7 完成 `medical` 临床简报封面与五类角色构图，增加临床信号、证据评估、方案取舍、照护路径和行动结论；专项回归通过，6 页真实样稿无元素越界。
  - [x] 6.8 完成 `elegant` 董事会备忘录封面与五类角色构图，增加主信号、证据注释、路径取舍、纵向里程碑和董事会决议表达；统一生成测试 `24 passed`，6 页真实样稿无元素越界，证据页与路线页二轮视觉评分为 `9.0/10` 和 `8.5/10`。

- [x] 7. 实现规则质量检查器和自动重排引擎
  - 创建统一 `QualityIssue`、`QualityReport` 和页面质量分模型，覆盖需求 8.1、8.3。
  - 实现文本溢出、元素重叠、对比度、内容密度、图片变形、布局重复和安全区检查，覆盖需求 7.1、7.3-7.5。
  - 创建 `AutoReflowEngine`，按内容拆页、布局切换、尺寸间距、字号、颜色裁切和装饰顺序执行白名单修复，覆盖需求 7.2。
  - 强制每页最多 2 轮自动重排，保留最佳得分页并标记人工复核，覆盖需求 7.6-7.7。
  - 实现正文 14 磅、标题 24 磅和对比度阈值约束，覆盖需求 3.4、7.4。
  - [x] 7.1 编写各项规则的边界单元测试和质量分计算测试。
  - [x] 7.2 编写自动重排次数、字号下限和安全区约束属性测试，覆盖设计属性 5-6。

- [x] 8. 接入标准模式和精修模式
  - 在批准大纲后增加标准模式和精修模式选择器，并展示处理阶段差异，覆盖需求 6.1。
  - 标准模式执行规则质检和自动重排后生成质量报告，覆盖需求 6.2。
  - 将 `PPTVisualAnalyzer` 接入精修阶段，输出视觉问题、置信度和白名单修复动作，覆盖需求 6.3。
  - 限制视觉复审并发数量、支持阶段恢复，并在视觉服务失败时降级为标准模式，覆盖需求 6.5、9.4。
  - [x] 8.1 编写标准/精修阶段包含关系属性测试，覆盖设计属性 4。
  - [x] 8.2 编写视觉复审成功、低置信度和服务降级集成测试。

- [x] 9. 改造 Web 三步生成流程和质量展示
  - 将 `PPTGenerate.vue` 改为输入、编辑大纲、选择质量模式并生成的三步流程，覆盖需求 1.2、6.1。
  - 增加页面类型、核心结论、内容块、素材意图和页面级校验问题编辑展示，覆盖需求 1.2、1.5、8.2。
  - 扩展 `src/utils/api/ppt.js` 的大纲 CRUD、批准、按版本生成和质量报告接口。
  - 在 PPT 预览页展示整体质量分、逐页质量分、问题页码、修复动作和人工复核标记，覆盖需求 8.1-8.4。
  - 支持指定页面重新生成并保留其他页面版本，覆盖需求 8.4。
  - [x] 9.1 编写前端大纲编辑、批准禁用和质量报告展示测试。
  - [x] 9.2 编写生成流程端到端测试，覆盖草稿到下载链路。

- [ ] 10. 建立回归基线并完成兼容验收
  - 已增加稳定的语义布局基线 manifest、像素差异比较工具和 PDF/PNG 渲染环境；固定样本基线仍需在目标模板集上生成。
  - 增加中英文、长标题、极端数值、缺图、透明图和 5/10/30/50 页整稿测试样本。
  - 验证文本溢出严重问题为 0、严重重叠问题为 0、对比度阈值和图片比例阈值，覆盖验收指标。
  - 验证现有 PPT 专项测试、前端测试、生产构建和格式下载接口持续通过。
  - [x] 10.1 编写语义视觉回归 manifest、像素差异工具和差异报告测试。
  - [x] 10.2 执行完整后端、前端和接口回归测试并记录结果：后端 `1892 passed, 2 skipped`，前端 `35 passed`，生产构建通过；真实 PPTX -> PDF -> PNG 实测通过。
  - [x] 10.3 执行商业结构专项回归和双主题逐页视觉验收：专项测试 `60 passed`；修复后的 `creative` 路线页评分 `8.8/10`，无截断、重叠或边界溢出。

- [x] 11. 检查点 - 确保所有测试通过,如有疑问请询问用户
  - 阶段 1 完成后确认大纲审批、标准模式、首批页面类型和规则质检可独立交付。
  - 阶段 2 完成后确认全部页面类型、模板候选和视觉回归基线覆盖。
  - 阶段 3 完成后确认精修模式、降级策略和指定页再生成可追踪。

- [x] 12. 统一最终页数语义与模板令牌渲染
  - 将页数控件明确为包含封面的最终总页数，并为初始大纲创建 `N-1` 个可编辑内容页。
  - 让批准大纲生成任务以当前内容页数量加封面计算最终页数。
  - 在直接生成和文件生成边界限制内容页预算，消除 `N+1` 页和重复封面。
  - 将 `TemplateManager` 解析的设计令牌覆盖到实际 PPTX 渲染样式，并统一规范模板 ID 与视觉主题 ID。
  - [x] 12.1 增加页数契约、重复封面和大纲编辑后页数测试。
  - [x] 12.2 增加设计令牌实际影响生成文件样式的测试。
