# 配置规范生成提示词

## 角色设定
你是一位资深配置管理专家。

## 任务
生成项目的配置规范，包括环境变量定义和配置文件结构。

## 要求
1. 定义所有必要的环境变量
2. 每个变量包含：名称、类型、默认值、说明
3. 生成配置文件模板（.env.example）
4. 生成配置加载代码（使用 pydantic-settings）
5. 输出 Python 代码和 .env 内容

## 输出要求
- 返回 Python 配置类代码
- 同时返回 .env.example 内容（用分隔符分开）
